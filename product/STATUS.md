# Keel — Status

<!-- keel:generated project prj_01KZKMPVHJNCCQH3JQNAXJJ03M 2026-08-13T13:10:53Z -->
> **Generated by `keel render-status`. Do not edit — Keel is the source of truth.**

## At a glance

| | |
|---|---|
| **Status** | active |
| **Active phase** | Phase 11 — Hardening: deep engineering review and strengthening |
| **Open work** | 9 task(s), 0 urgent, 1 blocked |
| **Open questions** | 11 |

---

## Phases

| Phase | Status | Target | Tasks done |
|---|---|---|---|
| Phase 0 — Spine | `shipped` | 2026-08-09 | 17 / 17 |
| Phase 1 — Daemon | `shipped` | 2026-08-09 | 19 / 19 |
| Phase 2 — Plugin | `shipped` | 2026-08-09 | 15 / 15 |
| Phase 3 — Desktop | `shipped` | 2026-08-09 | 11 / 11 |
| Phase 4 — Integrations | `complete` | — | 3 / 3 |
| Phase 5 — Remote | `cut` | — | 1 / 1 |
| Phase 6 — Make the tracker real | `shipped` | — | 15 / 15 |
| Phase 7 — Clean up the footprint | `shipped` | — | 7 / 7 |
| Phase 11 — Hardening: deep engineering review and strengthening | `active` | — | 31 / 33 |
| Phase 12 — Search that reads the whole document | `complete` | — | 4 / 4 |
| Phase 8 — The working loop | `shipped` | — | 23 / 23 |
| Phase 9 — One database | `shipped` | — | 8 / 8 |

---

## What's next

- **KEEL-136** The pre-commit check reads the wrong tree from a git worktree — nothing is blocking it · p2
- **KEEL-171** A question or decision can be created with no prose in it at all — nothing is blocking it · p2
- **KEEL-172** keel_create cannot record a metric observation through its own schema — nothing is blocking it · p2
- **KEEL-173** Kill the twelve mutation survivors, and stop the end-to-end tests timing out instead of failing — nothing is blocking it · p2
- **KEEL-179** A doctor test fails only in a full suite run and passes on its own — nothing is blocking it · p2

**Blocked**

- **KEEL-168** Security prerequisites before any browser-served UI or write endpoint — blocked by “Does a browser-served write/intake endpoint require amending hard constraint 7?”

---

## Tasks

### blocked (1)

- **KEEL-168** Security prerequisites before any browser-served UI or write endpoint `p3`

  - One piece of this shipped early, in cd00558, because it is not hypothetical: the desktop app fetches blobs from the daemon today, so `/api/blob/{id}` now carries `X-Content-Type-Options: nosniff` and `Content-Security-Policy: default-src 'none'; sandbox`. The SVG case is the reason — an SVG is a document that may contain `<script>` and is served with an image media type, so a diagram an agent wrote while reading prose it did not write is stored XSS the moment something renders it as a document.  Two more of the listed defenses also landed under other tasks rather than here: `Origin: null` is refused (KEEL-158), which was the null-origin gap this task names, and the origin check now runs before the rate limiter.  What is still genuinely gated on KB amending hard constraint 7: the per-session token from a 0600 file on mutating endpoints, allowlisting blob and image read roots, and the advisory lock that stops a browser write path becoming a second writer. None of those should be built before there is a decision that a write endpoint exists at all — half of an auth scheme is worse than none, because it looks like one. <sub>— claude · 2026-08-12</sub>


### todo (8)

- **KEEL-179** A doctor test fails only in a full suite run and passes on its own `p2` · testing, bug

  `doctor::tests::documents_with_no_vectors_are_reported_without_failing` failed in two of four `cargo test --workspace` runs today and passed in the other two. Run on its own, or with `doctor::tests` as a filter, all nine pass every time.
  
  The assertion that goes is the last one:
  
  ```
  panicked at crates/keel-cli/src/doctor.rs:717:9:
  degraded search is not a broken store
  ```
  
  That is `report.is_healthy()`, not the embeddings check — the embeddings check is `Degraded` as expected and the test gets that far. So some *other* check in `examine` goes unhealthy, and only when the rest of the binary's tests are running beside it.
  
  The test builds its own tempdir and its own store, so the interference is coming from something with reach beyond the fixture. The leaked-store sweeper is the obvious suspect — `e15dcc7` fixed it "only worked where TMPDIR ends in a slash", so it has recently started working in more cases than it used to, and a sweeper that removes another test's store mid-run would produce exactly this. Worth checking before looking anywhere else.
  
  Whatever the cause, printing the unhealthy check rather than asserting a bare boolean would have named it in the failure output instead of leaving this to be reconstructed. That is probably the first change to make.
  
  Not caused by the archive work in KEEL-175 — it fails identically with and without it, and the check that goes is not the one that touched.

  - Tried to reproduce this and could not, which is itself the finding.  **23 consecutive clean runs** of `cargo test -p keel-cli --bin keel` — 6, then 12, then a pair run concurrently with `keel-core`'s lib tests to see whether two cargo processes sharing TMPDIR would trip it. Nothing failed. It had failed 3 times in roughly 7 runs earlier the same day.  What changed between then and now is not the code. Every one of the three failures happened inside the window when a second session was building, committing and rewriting files in this same working tree — at one point `enums.rs` was mid-edit and would not compile. A test binary running while the tree it was built from is being rewritten underneath it is a sufficient explanation for an intermittent failure that vanishes once the tree is still, and it is the only thing that meaningfully changed.  That is a hypothesis, not a diagnosis. I could not prove it, and I would rather leave this open than close it on 23 runs and a plausible story.  The one thing I did change is the assertion. It was `assert!(report.is_healthy(), "degraded search is not a broken store")` — a bare boolean, so the failure output said nothing about which of forty checks had gone. A `problems(&report)` helper now lists the offending check names and their detail in the panic message. That is why this took so long to get anywhere: three failures, and not one of them said what actually broke.  The sweeper suspicion in the task body is worth dropping. `scripts/sweep-build-artifacts.sh` is run by hand, not by the suite, and it only takes directories untouched for an hour — so it cannot take a running test's store out from under it. It was the obvious suspect and it is the wrong one.  If this returns, the panic message will name the check and this becomes a five-minute job. <sub>— claude · 2026-08-13</sub>

- **KEEL-173** Kill the twelve mutation survivors, and stop the end-to-end tests timing out instead of failing `p2` · testing
- **KEEL-172** keel_create cannot record a metric observation through its own schema `p2` · mcp, bug
- **KEEL-171** A question or decision can be created with no prose in it at all `p2` · bug, mcp

  `dispatch.rs:1099` is `if let Some(text) = body.filter(|_| entity_type.has_document())`. Body is optional, so a create with no body writes the row, skips the document, and reports success.
  
  For a task that is survivable — 8G makes `summary` required, and the row still says something. For a question or a decision there is no summary column at all. The document is the entire content.
  
  Three rows are in this state right now, all from `ses_3f1a60af` on 2026-08-12, each with exactly one event and no document ever written:
  
  - `que_01KZSQHFQ454` — Enforce the single-writer rule with an advisory lock file, or rely on the health probe alone?
  - `que_01KZSQHK2C0C` — Does a browser-served write/intake endpoint require amending hard constraint 7?
  - `dec_01KZSQJ05N4T` — The write-path atomicity fix: &Connection primitives, transaction-of-one, one typed composite on Store
  
  The titles are good. Each names a real decision, and a decision row that records what was chosen and nothing about why is the one shape the decision log exists to prevent.
  
  There is a second route to the same state, and it is worth fixing together because it is silent: the style check runs *after* the row is created, at `dispatch.rs:1104`. A refused body therefore leaves the row and loses the prose, and the caller sees an error that does not say a row landed. That happened while filing the question about this — the entity was created at 01:06:48, the body was refused, and the document only arrived at 01:07:09 on a retry that the idempotency key turned into an attach rather than a duplicate. Without the retry it would have stayed headless. KEEL-146 is the transactional fix and this is a live reproduction of it.
  
  Two parts, then:
  
  1. Make `body` required for the types whose content it is. That changes the MCP tool schema, so it needs KB's agreement — but it is 8G's argument applied to the types where the omission costs more.
  2. Until then, at minimum the refusal must not leave the row behind (KEEL-146).

- **KEEL-137** The daemonless fallback creates an empty store instead of saying there isn't one `p3` · bug, cli

  Found while fixing KEEL-133. `directly()` in `crates/keel-cli/src/work.rs` calls `Store::open`, which creates and migrates when the file is absent. Running `keel ready keel` against the default home on a machine whose store is still `keel.duckdb` left an empty `~/.keel/keel.sqlite` behind, and then reported:
  
  ```
  Error: argument `project`: no project matches `keel`. Expected: one of: . Call keel_projects to list them
  ```
  
  The empty `one of: ` is the tell. Nothing is wrong with the project name; there is no store.
  
  Creating on demand is right for `keel bootstrap` and `keel fixture`, which are asked to make one. It is wrong for a read that fell back.

- **KEEL-136** The pre-commit check reads the wrong tree from a git worktree `p2` · tooling

  Hit while committing KEEL-132 from `.claude/worktrees/competent-mayer-aa62e7`.
  
  `scripts/pre-commit` runs `keel generate "$project" --check` with no `--repo`, so the check resolves the project's recorded `root_path` — the main checkout. From a worktree the files being compared are not the files being committed.
  
  Both directions are wrong, and the second is the one that matters:
  
  - **False alarm.** Regenerating in the worktree left it clean, and the hook still reported `stale product/SPEC.md` because the main checkout has the old file. The commit went through with `--no-verify` after verifying by hand with `keel generate keel --repo "$PWD" --check`.
  - **False pass.** The same lookup means a genuinely hand-edited file in a worktree is compared against the main checkout's copy. If the main checkout happens to be current, the check passes and the edit is committed — which is the exact failure the hook was written to replace, since the mechanism it replaced was deleted for silently doing nothing.
  
  The fix is one flag: pass `--repo "$(git rev-parse --show-toplevel)"`, which is the tree the commit is in and equals `root_path` when there is no worktree. The header comment already argues at length that a check which cries wolf gets bypassed with `--no-verify`, and that this is the same disease as a hook that quietly does nothing. This is that, from a direction it did not anticipate.

  - The write side has the same bug, and it is worse than a false alarm. `keel generate` with no `--repo` also resolves `root_path`, so running it from a worktree writes into the main checkout — the files land as uncommitted changes on whatever branch that checkout has out, and `git status` in the worktree you ran it from says clean.  That happened here at 23:47, closing KEEL-133 from `recursing-blackburn-0e4d48`: five files written into `/Users/h8hcn/development/keel` on `phase-9-one-database`, including the 649-line `product/SPEC.md` from your KEEL-132 work, which was in the store but not in that checkout. Reverted by hand with `git checkout --` plus an `rm` of the new decision file. Nothing was lost, because every one of them is a render of the store — but a session that did not think to look in the other tree would have left them there, and the next person to `git status` the main checkout would find a stranger's diff.  So `--repo "$(git rev-parse --show-toplevel)"` fixes the hook, and the same defaulting belongs in `generate` itself: when the command is run inside the project's repository, the tree you are in is the tree you mean. `--show-toplevel` gives the worktree root, which is the wanted answer in both cases.  KEEL-138 said this separately and is closed as a duplicate of this row. <sub>— claude · 2026-08-11</sub>

- **KEEL-135** SPEC §6 still says nine MCP tools and lists nine `p3` · docs

  Left deliberately untouched by KEEL-132, which was scoped to the storage engine. This is a different kind of staleness in the same document and it should not ride along on an unrelated change.
  
  §6.1 opens with "**Few tools, rich arguments.** Nine tools, not forty CRUD endpoints. Models choose correctly among nine." §6.2's table lists those nine. The daemon serves thirteen: `keel_note`, `keel_ready`, `keel_claim` and `keel_close` are missing. §12's Phase 1 row also says "9 MCP tools".
  
  The interesting part is not the count. The ceiling was nine, went to ten when `keel_note` earned a slot, and to thirteen when the three work verbs did under TQ-31 — and each rise needed KB's agreement and an argument at least as good as the last one. That progression is recorded in the doc comment on `tools::all()` and in the standing contract, but the spec still reads as though nine were a designed limit rather than the first of four positions. Whoever fixes this should carry the argument across, not only the rows.

- **KEEL-96** Pruning does not cover adopted files under product/ `p3`

  `keel generate` prunes orphaned files under `.keel/` using the previous manifest (KEEL-96 / TQ-28), but adopted prose files in `product/` are not covered.
  
  An adopted file orphans when its artifact is archived — `product/QUESTIONS.md` and `product/DECISIONS.md` both did on 2026-08-10 and were removed by hand with `git rm`.
  
  Why it was left: deleting inside `product/` is a larger act than deleting inside `.keel/`. A mirror file is Keel's own bookkeeping; an adopted file is a document someone may have 50 KB of prose in. The manifest does not currently record adopted paths, so the safe set is not even known.
  
  What it needs: record adopted paths in the manifest, then prune them on the same rule. Worth pairing with a louder confirmation than mirror pruning gets — or reporting rather than deleting, which is the asymmetry TQ-28 already accepts for `--check`.


### done (165)

- **KEEL-178** The B-57 commit is red: 28 tests still set a milestone status that is now derived `p1` · testing, bug

  Ran `cargo test --workspace --no-fail-fast` against `1299bdb`, which says in its own message: "the full test suite has NOT been run since the change. Do not trust it until it has." It is red, and every one of the 28 failures is the same sentence:
  
  ```
  invalid milestone field `status`: `active` is worked out from the phase's tasks
  and edges, so it cannot be set. Move the tasks and it follows.
  Expected: open, paused, shipped or cut
  ```
  
  The validation is doing exactly what B-57 asked for. What was missed is every fixture and test that still declares `active`.
  
  - `keel-core` lib: `store::patch::tests::every_entity_type_can_be_patched` — the patch round-trip sets `milestone.status` to a value that is no longer settable.
  - `keel-daemon` integration: 27 failures, including all four use-case tests (`uc1_agent_orientation_in_one_call` through `uc4_customer_feedback_triage`), the digest tests, the local-API parity tests and the house-style tests. These fail during setup rather than on their own subject — the fixture builds a milestone before it gets to what it is testing — so the count overstates how much is actually broken.
  
  Clippy and `cargo fmt --all --check` are both clean, which is what the commit message claimed and it was right about.
  
  Three stored rows also hold statuses the new enum cannot parse: `Phase 4 — Integrations` at `planned`, `Phase 11 — Hardening` at `active`, and `Phase 12 — Search that reads the whole document` at `active`. Migration 3 is described as rewriting stored derived values to `open` — worth confirming it covers `planned` as well, which is neither a declaration nor a derived value the migration was obviously written for.
  
  Separately and not part of this: `doctor::tests::documents_with_no_vectors_are_reported_without_failing` fails under a full-suite run and passes on its own or with the other `doctor::tests`. That one predates B-57 and has its own task.

- **KEEL-177** Derive whether a phase is planned, active or complete instead of storing it `p2` · tracker

  - Stopped mid-task on a dying battery, at commit 1299bdb. That commit compiles and clippy is clean, but **the test suite has not been run against it** — treat it as unverified until it has.  The design is written up as B-57 and does not need re-deciding. What is built: the stored enum shrunk to declarations (`open`, `paused`, `shipped`, `cut`), `MilestoneState::derive` doing the work from a `TaskTally` plus the blocks edges, `Store::milestone_states` computing a whole project in two queries, migration 3 rewriting the old stored values and backfilling `shipped_at`, the digest and tracker reading derived state, `apply_changes` refusing to write a derived value, and two fsck checks with corruption tests.  Four things left, in the order to pick them up:  1. **Unit tests for `MilestoneState::derive`.** There are none at all, and every other piece rests on it. Worth doing before anything else, including before running the suite — the table of cases is small and the function is pure. 2. **`cargo test --workspace`.** The `keel_context` snapshots will almost certainly differ, because the digest's `active_milestone` now joins every live phase rather than naming one. Read that diff rather than accepting it blind. 3. **The desktop roadmap.** `Roadmap.tsx` still reads `m.status`, which now says `open` for everything undeclared. The derived state has to reach it — most likely as a `state` field on the milestone JSON the API returns, computed server-side, since the app has no task counts to derive from. 4. **The live store needs `keel migrate` with the daemon stopped.** It is on schema 1 and the code ships three. Back it up first.  One thing to know before touching the data again: Phase 5 is `cut` and Phase 4 stays `planned` — both decided by KB after a session set Phase 5 to `shipped` by counting closed tasks, when its one task was closed `wont_do`. `done` and `wont_do` both empty the column and mean opposite things. That mistake is why `shipped` and `cut` stayed declarations, and it is written into B-57. <sub>— claude · 2026-08-13</sub>

- **KEEL-176** fsck and doctor should prove the passage index matches the store `p2` · search, storage

  The mechanism that keeps passages in step with documents is a transaction on the write path. The check that proves the mechanism is working is a separate thing, and on this project it is the more valuable of the two — every bug in this area so far has been something that silently did nothing while looking fine.
  
  `documents.body_hash` already exists. Carrying it on the passage row makes coherence a query rather than a promise, in both directions:
  
  - **Missing** — a document that is `status = 'current'`, not archived, and has no passages at the current `body_hash`. Means an edit landed and the re-embed did not, or embedding failed and degraded as designed.
  - **Stale or orphaned** — passages whose `doc_id` is no longer current, whose entity is archived, or whose `body_hash` no longer matches. Means a delete was missed.
  - **Wrong model** — passages whose `embedding_model` is not the one now configured. Not an error; it is the input to a re-embed pass, and TQ-3 is the open question about when that runs.
  
  Report the counts in `doctor` with the remedy, the way the existing embeddings check does, and the offending ids in `fsck`. A clean store has to say it is clean, or a passing check means nothing.
  
  The failure case matters as much as the happy one: write a test that edits a document without re-embedding and asserts the check catches it. A coherence check that has only ever seen a coherent store is the same kind of reassurance as the pre-commit hook that called a command that had been renamed.

- **KEEL-175** Semantic search has no archived filter, and the test that should catch it can't `p2` · search, storage, bug

  A bug with no symptoms yet, and the backfill is what gives it symptoms. Cheapest possible moment to fix it.
  
  The keyword half is right by construction: `fts_source` is maintained by triggers on `documents`, archived rows are already gone from it, and the comment at `store/search.rs:353` says exactly that — "no query in this file carries a `WHERE archived_at IS NULL` that someone could later forget."
  
  The semantic half is that forgotten filter. `search_semantic` reads `documents` directly:
  
  ```
  WHERE status = 'current' AND embedding IS NOT NULL AND length(embedding) = ?
  ```
  
  No archive predicate. `reembed_missing` has none either, so a backfill embeds archived documents and hands them to the fusion, where BM25 refuses to return them and cosine does not. The two halves would disagree about what exists.
  
  On the live store that is 13 of 135 current documents — 10 specs, 2 decisions, 1 question — and 163,127 of 520,489 characters, 31% of the text. Two of them are "Keel — Decision log" and "Keel — Status", generated rollups of rows that are already indexed one by one, so they would double-count the decision register even if they were live.
  
  **The test that looks like it covers this cannot.** `archived_rows_leave_the_index` archives a *task*. Tasks have no row in `documents` and therefore no vector, so the semantic half is never reached and the assertion holds no matter what the code does. The replacement has to archive a document-bearing type — a spec or a decision — with an embedder attached.
  
  Fix in three places: the archive filter on the passage query, the same filter on whatever backfills, and the test. Under the chunking decision the filter has a better home than a `WHERE` clause anyone can forget — archiving deletes the passages, the way archiving already removes rows from `fts_source`.

  - Correcting the framing this task was written with, including the sentence in its own body.  Both this task and the note on KEEL-131 say the keyword half is "right by construction" and only the semantic half is missing an archive filter. That is wrong, and it was wrong because I believed the comment above `search_keyword` instead of checking it. **Archived documents have been coming back from keyword search all along.**  The `_fts_archived` trigger is emitted inside the loop over the six tables whose text is their own row. The five prose types index through `documents` and were never in that loop, so they had the path into the index and no path out. Querying the live store directly:  ``` spec|Keel — Status spec|Keel — Decision log spec|Keel — Open questions and risks spec|Keel — the way forward spec|Keel — fundamental problems, for outside review ```  Every one archived, every one returned. So this is a live bug in the half that has been running for months, not a latent bug in the half that has never run.  There were three holes, not one:  1. No archive trigger for the five prose tables — archiving a spec removed it from nothing. 2. No guard on `documents_fts_ai`/`_au`. Nothing in `docs.rs` refuses a revision on an archived entity, so the next edit put it straight back, with no event and no error. 3. The backfill would have undone the archive. Archiving now clears the vector, which makes the row indistinguishable from one that was never embedded, so `reembed_missing` would have re-added it on every pass.  The vector is cleared on archive rather than filtered out in `search_semantic`, deliberately. `embedding IS NOT NULL` is already in that query, so there is no new predicate to forget — which is the thing the comment in that file warns about and the thing it turned out not to have done.  `documents_missing_embeddings` also had to exclude archived from **both** halves of its ratio. Without that, `doctor` would report "13 of 135 have no vector" permanently, since those thirteen can never be embedded again. A check that cannot go green is a check people stop reading. <sub>— claude · 2026-08-13</sub>
  - Stopping short of closing this, because another session is editing the same working tree and the tree does not currently compile.  `ListAgents` shows a peer session, `keel-d9`, started 48 minutes ago. It has written B-57 ("A phase's state is derived; only shipped, cut and paused are declared") and B-58 since I wrote B-55 and B-56, and it is mid-refactor of `MilestoneStatus`: `Active` is gone from the enum while `entity.rs:2033` still calls for it, and `render_status.rs` and `lib.rs` carry imports of a name that no longer resolves. `cargo clippy --workspace --all-targets -- -D warnings` fails on three errors, none of them in anything I touched.  `git status` shows thirteen modified files. Four of them are files I also changed this session — `store/schema.rs`, `store/mod.rs`, `doctor.rs` and `tests/documents.rs` — so this is not two sessions working politely in different corners.  What is verified and what is not:  - Everything except the `doctor.rs` delegation passed a full `cargo test --workspace --no-fail-fast` run — 288 in `keel-core`, 51 in `keel-cli`, every other suite green — and migration 2 was applied to a copy of the live store, taking archived rows in `fts_source` from 13 to 0 with live rows untouched. - The `doctor.rs` change that makes `embedding_coverage` delegate to `Store::documents_missing_embeddings`, and the test that goes with it, were written after that run started and have never been through a green suite.  So the work is done and the evidence is incomplete, which is not the same as done.  One thing for whoever owns B-57: three milestone rows now hold a status the new enum cannot parse — `Phase 4 — Integrations` and `Phase 11 — Hardening` at `planned` and `active` respectively, both predating today, and `Phase 12 — Search that reads the whole document` at `active`, which I created this morning before B-57 existed. Presumably the migration covers them; noting it in case it does not. <sub>— claude · 2026-08-13</sub>
  - This is now live on the real store, not just proven on copies.  Migrations 2 and 3 applied to `~/.keel/keel.sqlite`, which went from schema 1 to schema 3. Afterwards: unparseable milestone statuses 0, archived rows in the keyword index 0, shipped phases with no date 0, and all 376 entities still there.  The proof is the query that started this. This morning `"maintained" AND "claude"` returned nine specs, five of them archived — `Keel — Status`, `Keel — Decision log`, `Keel — Open questions and risks`, `Keel — the way forward`, `Keel — fundamental problems`. The same query against the running daemon now returns ten hits and not one of them is archived, while `Keel — Phase 10` is still there because it is live. A control query for `embedding` returns ten live rows, so this is the archived documents leaving rather than search having stopped working.  Worth recording how nearly that check fooled me: the first two attempts printed `0 hit(s)` for both the test *and* the control, which reads as a clean pass on the test and would have been reported as one if I had not run the control. The API wraps its payload in `data` and I was parsing the top level, so every query returned nothing. A search fix verified with a query parser that always returns empty is the same joke as the archive trigger written for six tables out of eleven.  Sequence, for anyone repeating it: stop the daemon, `keel migrate`, `./plugin/install.sh`, restart. The reinstall is not optional — the installed binary predated the migrations and the store refuses a binary older than itself, so the daemon would not have come back up without it. A raw snapshot was taken first at `~/.keel/backups/pre-migration-2026-08-13.sqlite`; `keel backup` could not be used, because the new CLI refuses to open a store with migrations outstanding. <sub>— claude · 2026-08-13</sub>

- **KEEL-174** Long documents are embedded on their first 1,700 characters and nothing says so `p2` · search, storage

  Found while costing KEEL-131, before running the backfill.
  
  `Document::searchable_text_of` is `title + "\n\n" + body`, and that string goes to `bge-small-en-v1.5` in one piece. The tokenizer config sets `model_max_length: 512` and fastembed takes `max_length.min(model_max_length)`, so everything past roughly 1,700 characters of English prose is dropped before the model sees it. There is no chunking anywhere in the write path or the re-embed path.
  
  Measured on the live store, current revisions only:
  
  - 55 of 135 documents (41%) are over the window — 19 of 21 specs, 17 of 57 decisions, 19 of 56 questions.
  - Technical Specification: 67,594 chars, ~2.5% embedded.
  - Status: 56,299 chars, ~3.0%.
  - Phase 8: 34,275 chars, ~5.0%.
  - Build journal: 32,131 chars, ~5.3%.
  
  Decisions and questions mostly fit — they average about 1,250 characters. Specs do not, and specs are the documents most worth finding by meaning.
  
  The failure mode is the familiar one: search returns results, they look reasonable, and nothing anywhere says the semantic half only read the opening of half the corpus.
  
  Two ways out, and they are not exclusive. The real fix is chunking: split on headings into passages under the window, embed each, and keep the best-scoring passage per document at query time. That is a schema change — one document currently owns one `embedding` column, and chunking means a row per passage — so it needs a decision first. The cheap one is a `doctor` check counting documents over the window, which costs nothing and makes the gap visible while the real fix waits.
  
  Related: TQ-3 (re-embedding when the model changes) touches the same migration path, and `embedding_model` / `embedding_version` are already on the row.

- **KEEL-170** A task with a summary but no body shows "No description." in the app `p2` · bug, desktop

  `apps/desktop/src/screens/Task.tsx:232` is `task.body ? <Markdown/> : <Empty message="No description." />`. The string `task.summary` does not appear anywhere in that file — zero occurrences.
  
  Both fields are legal and both are used, which is why this went unnoticed:
  
  - `body` is optional, and its own tool description calls it "the short detail field — anything long-form belongs in a spec".
  - `summary` is **required** since 8G, and is described as what a colleague reads cold.
  
  So a session that fills only the required field produces rows that are complete by the store's rules and blank on the screen. Measured across the store: 145 tasks predating 8G have a body and no summary, and 31 Phase 11 tasks have a summary and no body. Both render as half a page — the older ones show a description and no summary anywhere, the newer ones show "No description."
  
  Worth reading the comment that sits fifteen lines above the bug, about the partial-load banner: "a page that quietly does less while looking complete is the failure this project treats as the serious one." That is this, in the same component.
  
  Suggested shape: render `body` when present, fall back to `summary`, and label which one it is showing rather than presenting them as the same thing. The list and board views already show `summary`, which is why these tasks look normal until you open one.

- **KEEL-169** Warn on the environmental conditions that quietly corrupt a local SQLite store `p3`
- **KEEL-167** Guard against unbounded WAL growth under the long-lived SSE reader `p3`
- **KEEL-166** Reduce the digest's N+1 traversal and full-table scans before it matters `p3`
- **KEEL-165** Add the end-to-end binary and MCP-protocol conformance test tiers `p3`

  - A cost this tier introduced, found later by mutation testing and worth knowing before adding more end-to-end tests: the daemon-start wait is 30 seconds and the stream reads are 5–10. Those are generous so the tests do not flake, and the price is that a *broken* daemon takes the full timeout to fail rather than failing fast. Under `cargo mutants` that turned 24 of 113 mutants into timeouts instead of verdicts — nearly a fifth of a two-hour run spent learning nothing — and it means a real CI failure would surface in minutes rather than seconds. Recorded properly on KEEL-173.  Separately, and the reason two health tests failed for a day: the *first* HTTP request a test process makes costs 12.7 seconds on macOS, against 0.9 milliseconds for the second. That is reqwest building its client and reading the system trust store. Any test that wraps its first request in a timeout is measuring that rather than the daemon. `health.rs` now warms the client in its fixture and the numbers are in its module doc. <sub>— claude · 2026-08-12</sub>

- **KEEL-164** Strengthen the QA process: Linux CI, snapshot gate, coverage and mutation as discovery `p3`

  - The three scheduled jobs — coverage, mutants, fuzz — have never actually run. They are configured and the fuzz crate compiles, but nothing has executed them yet: the schedule fires Sunday 06:00 UTC, and the discovery jobs are gated on `schedule` or `workflow_dispatch` so a push does not start them. Worth triggering once by hand from the Actions tab to find out whether the tool versions and flags are right, rather than discovering it from a red weekly run. <sub>— claude · 2026-08-12</sub>
  - Correcting the note above, which was true and badly understated. It is not that the three scheduled jobs have never run — **no CI has ever run on this project at all.**  `git remote -v` is empty. There is no remote configured, and `gh repo list kiritbasu` finds no keel repository. The workflow file has been in the tree since it was written and has never been executed by anything, including the `check` and `deny` jobs that predate Phase 11.  A second defect, independent of the first: the trigger was `push: branches: [main]` and this repository's trunk is `master`, with no `main` branch in existence. So even once there is a remote, a push to trunk would not have started it. Fixed in f65be15 by listing both.  What this means for what I closed KEEL-164 as: the Linux matrix entry does not test anything on Linux, the snapshot gate gates nothing, and the discovery jobs are a description of intent. The configuration is right as far as anyone can tell by reading it, and reading it is the only way it has ever been checked. Everything I verified, I verified by running it locally on macOS.  Publishing the repository is KB's call, not something to do on the way past — it is a local-first personal project and pushing it anywhere is a decision about the project, not about CI. <sub>— claude · 2026-08-12</sub>
  - The discovery jobs ran, locally on macOS, since there is no CI to run them on. First actual numbers this project has had.  **Coverage** — 87.36% line, 84.61% region, 82.99% function, across 34,459 regions. The floor is more useful than the total. Worth knowing, roughly worst first:  - `keel-github/src/main.rs` 0% (3 regions — a stub, so this is honest rather than alarming) - `keel-daemon/src/main.rs` 21.6% — the binary's shutdown path, signal handling and checkpoint-on-exit. The new end-to-end tier covers startup; nothing covers the way it stops. - `keel-embed/src/lib.rs` 21.9% — the real embedder, which needs a 130 MB model download to exercise - `keel-daemon/src/http.rs` 58.6% — mostly the REST endpoints the desktop app uses, which no test opens - `render_status.rs` 60.5%, `dispatch.rs` 75.0%  Everything the review called load-bearing is high: `graph.rs` 96%, `safe_path.rs` 98%, `store/entity.rs` 93.5%, `fsck.rs` 91%, `next.rs` 91%, `protocol.rs` 97%.  **Fuzzing** — three targets, 60 seconds each, no crashes. `confine_path` 1,362,773 runs; `fts_query` 2,082,078 runs; `parse_id` pending at the time of writing. A minute each proves very little, but it is a minute more than these had.  **Environment, which cost the first two attempts.** This machine has two Rust installs. Homebrew's `cargo`/`rustc` at 1.91.0 shadow rustup's shims on PATH and are what actually build Keel; rustup's `stable` is 1.63.0 from August 2022 and cannot parse this workspace's manifest at all (`resolver = "3"`). So `cargo +nightly` fails outright and rustup components are invisible to the cargo on PATH. Anything that needs a rustup toolchain has to run as `PATH="$HOME/.cargo/bin:$PATH" cargo +nightly …`, which is how coverage was measured. Worth either updating rustup's stable or deciding Homebrew is the toolchain and saying so somewhere. <sub>— claude · 2026-08-12</sub>
  - Completing the fuzz numbers from the note above: `parse_id` finished at 6,787,783 runs in 61 seconds. All three clean — 10,232,634 inputs total across the targets, `fuzz/artifacts/` empty, no crash written. <sub>— claude · 2026-08-12</sub>

- **KEEL-163** Stop doc/code drift with guard meta-tests, and fix the stale docs found `p3`
- **KEEL-162** Tidy the modularity seams the review flagged `p3`
- **KEEL-161** Clear the small correctness papercuts found in review `p3`
- **KEEL-160** Cover the untested surfaces: SSE stream, embedder fallback, CLI verbs, malformed MCP `p3`

  - The CLI verb tests found a real bug on their first run, which is the best argument for the task. `call_daemon` unwraps `structuredContent` out of a tool result; the fallback that opens the store directly did not. So whenever no daemon was listening — the exact situation the fallback exists for — `keel ready` looked for `ready` at the top level, found the envelope, and printed `nothing ready` against a store with sixteen ready tasks. `claim` and `close` printed a bare task id where the title belonged. Fixed as `keel_mcp::payload`, one named function shared by the daemon's REST surface and both CLI paths. <sub>— claude · 2026-08-12</sub>

- **KEEL-159** Shrink the daemon's store-lock scope so one slow call can't freeze it `p2`
- **KEEL-158** Harden the daemon's HTTP edge: body limit, origin ordering, drop Origin: null `p2`

  - Stopped mid-task: the machine's disk filled up. `/System/Volumes/Data` hit 100% with the cargo target directory at 13 GB, and the shell can no longer create even a temporary file, so nothing further can be built or run.  The code changes are written and uncommitted in the working tree: the `Origin: null` branch removed from `origin_ok`, the origin check moved ahead of the rate limiter in `mcp_endpoint`, `MAX_BODY_BYTES` plus the `explain_body_limit` middleware in the router, and five tests in `crates/keel-daemon/tests/http_edge.rs`. `cargo build -p keel-daemon` passed before the disk filled; the tests have never been run.  To resume: free space (`cargo clean` in this repo reclaims ~13 GB), then `cargo test -p keel-daemon --test http_edge`, `cargo clippy --workspace --all-targets -- -D warnings`, and commit. <sub>— claude · 2026-08-12</sub>
  - A loose end the origin check still has, found while removing the `null` branch: `is_local_origin` matches the host exactly against localhost, 127.0.0.1 and ::1, so Tauri on Windows — which serves the webview from `https://tauri.localhost` — would be refused. On macOS the scheme is `tauri://localhost` and it passes, which is why nothing has broken. Not fixed here because the desktop app is macOS-only today, but it will bite the first time someone builds for Windows. <sub>— claude · 2026-08-12</sub>

- **KEEL-157** Complete the MCP response snapshots and guard the surface against drift `p2`
- **KEEL-156** Write the property tests the contract has always required `p2`
- **KEEL-155** Write generated files atomically (temp file + rename) `p2`
- **KEEL-154** Make schema migration deliberate, not a side effect of opening the store `p2`
- **KEEL-153** Keep the event feed alive across a clock step (sleep/wake, NTP) `p2`
- **KEEL-152** Extract keel-embed so keel-core stops opening a network socket `p2`
- **KEEL-151** Fix the embedding operational story: backfill, don't block boot, signal degradation `p2`
- **KEEL-150** Add `keel doctor` — one command that surfaces silent divergence `p2`
- **KEEL-149** Fix the backup manifest race and verify the snapshot after creating it `p2`
- **KEEL-148** Add integrity checks: PRAGMA integrity_check plus the missing fsck orphan checks `p2`
- **KEEL-147** Make readiness ranking fail closed when a blocker row is unreadable `p2`
- **KEEL-146** Make the composite create (entity + body + image) one transaction in core `p1`

  - Two things fell out of this that were not in the task.  `create` is now `prepare_create` plus `insert_created`, because the composite create needs the same validation, idempotency and numbering rules and a second copy of them would differ within a release. `Prepared` is the enum in between.  And the house-style check moved ahead of the create in dispatch. It used to run after the row had landed, which is the second half of KEEL-171 — a refused body left a headless row and the caller saw an error that did not say a row had been written. That half is closed by this commit.  One observable change worth knowing: a design created with an image is now version 1, not version 2. The bump came from the update() that recorded the blob pointer, and nothing asserted on it. <sub>— claude · 2026-08-12</sub>

- **KEEL-145** Build the fault-injection testkit for the resilience regression tests `p1`

  - The authorizer fires at *prepare* time, not execute. Keel's write path uses `Connection::execute`, which prepares every call, so "the nth insert" and "the nth prepare of an insert" are the same number — but a future caller that prepares once and steps many times would see the fault once and look like it never fired. That is what `FaultCounter::assert_fired` is for. <sub>— claude · 2026-08-12</sub>

- **KEEL-144** Fix the oldest-first event reads that already truncate history today `p1`

  - `EventScope` is an enum rather than two `Option` parameters because "no project and no entity" and "every project" would otherwise be the same call with two meanings.  `changes::by_session` was the subtle one. It merges notes into the events afterwards, so asking for exactly `limit` events would be too few — a note can displace an event before the cut. It asks for ten times the limit, floored at 1,000, which cannot plausibly be displaced, and `truncated` still reports honestly.  `recent_activity` in the digest keeps using the forward feed when a `since` is supplied: that is a bounded window, so reading it forwards cannot lose the newest rows. <sub>— claude · 2026-08-12</sub>

- **KEEL-143** Close the two-writer footgun: lock-free health, honest fallback, probe before direct write `p1`

  - The probe is a TCP connect rather than a GET /api/health, and that is the load-bearing choice. ureq reports connection-refused and a read timeout as the same `Transport` error kind, so distinguishing them means digging into the io error's source — whereas `TcpStream::connect_timeout` answers the actual question ("is anything holding this port") directly and cannot be confused by a slow handler.  "Routed through the same write funnel" is implemented as one guarded open, not as routing the commands through the daemon's MCP API. `note`, `archive` and `task add` still write via keel-core when no daemon is running, which is legitimate and is the case the CLI exists for; what changed is that they now refuse when a daemon is up. Sending them over HTTP instead would be a larger change and would make the CLI useless without a daemon.  `--force` is global on the Cli struct rather than per-subcommand, because adding it to five variants that already have three flags each would have been worse. It is a flag and not an environment variable so that using it shows up in a shell history. <sub>— claude · 2026-08-12</sub>
  - The health tests as first written were flaky. They held the store lock for 600ms and asserted health returned inside 400ms; that passed in isolation and failed once under a full `cargo test --workspace`, where the machine is loaded enough that 400ms of wall clock means nothing.  Rewritten in f9f75e9 to hold the lock until the test releases it, with channels for the handover and a two-second timeout as the assertion. Deterministic in both directions: without the fix, health waits on a lock nothing will free and the timeout always fires.  Worth remembering as a pattern — a wall-clock budget tight enough to be meaningful is tight enough to be flaky under load, and a test that cries wolf teaches people to re-run rather than look. <sub>— claude · 2026-08-12</sub>

- **KEEL-142** Confine entity-controlled file paths to the project, in keel-core `p1`

  - Resolving both sides is not optional and it is easy to get half right. On macOS a `tempfile::tempdir()` lives under `/var`, which is a symlink to `/private/var` — so canonicalising the target and comparing it against an unresolved root refuses every legitimate path in every test. The failure would have looked exactly like the check working.  `resolve_existing` canonicalises the deepest ancestor that exists and re-appends the rest, because the file about to be generated does not exist yet and `canonicalize` fails outright on a missing tail.  One residual risk, deliberately left: `root_path` is only checked for shape, not resolved. A caller that sets it to `/` would make generate write `product/STATUS.md` at the filesystem root. That fails on permissions for a non-root user and there is no `..` involved, so it is a much narrower hole than the one closed here — but it is not closed. <sub>— claude · 2026-08-12</sub>

- **KEEL-141** Make entity writes atomic, and emit the missing revision event `p1`

  - The Revised event takes its provenance from the `Document` (author, session_id, surface) rather than from a new parameter on `write_revision`. Those three were already decided at the boundary and recorded on the revision, so taking them from anywhere else would let a revision and its event disagree about who wrote it — and it kept the trait signature unchanged, which mattered because `DocumentStore::write_revision` has four callers.  Worth knowing for whoever reads the changelog next: every existing revision in the store predates this, so the changelog is only complete from here forward. Backfilling would mean inventing events with timestamps taken from `documents.created_at`, which is possible but is history nobody observed. <sub>— claude · 2026-08-12</sub>

- **KEEL-140** Push write primitives down to &Connection (the transaction substrate) `p1`
- **KEEL-139** Run the deep engineering review and expert panel; land the prioritized hardening backlog `p1`

  - Review round 1 complete — all six dimension reviewers reported; full reports saved in scratchpad/review-{architecture,codequality,security,performance,resilience,test-qa}.md. Headline convergence: the entity write path (create/update/archive/link/unlink in keel-core/src/store/entity.rs) runs multiple SQL statements with NO transaction, while write_revision (docs.rs:311-350) is properly transactional. Flagged HIGH independently by architecture (A1), code-quality (C1) and resilience (R1); a crash between the row INSERT and the event append leaves a row with no event, and the idempotency retry returns created:false before ever writing the event, so the event is permanently lost — silent history loss, and fsck has no check for it. Security found a real HIGH (S1): entity-controlled mirror_path/status_path/root_path get no path validation and are joined onto repo root at `keel generate`, so a prompt-injected write can plant attacker content at e.g. ~/.zshenv → code execution; the orphan-delete path was already hardened (mirror.rs:394) but the adopted-write path (generate.rs:213) was not. Also HIGH from resilience: single-writer rule has zero enforcement (R2 CLI falls back to direct store write on timeout; R3 no lock file/health probe). Other notable: document revisions append no event (Action::Revised is dead code, A2); proptest is declared-but-unused (T1/C3); P1 "read oldest-first, take tail" is already wrong today at 804 events for the 409 StaleVersion payload; no PRAGMA integrity_check anywhere (R6); mutex-under-load starvation (R7); 0 of 234 docs embedded with no backfill path (P6/R11). Expert panel (storage, security, architecture, SRE, QA) now running over these to weigh severity and shape fixes before the backlog is written. <sub>— claude · 2026-08-12</sub>

- **KEEL-134** The live store is still DuckDB — Phase 9 shipped in the tree but never on this machine `p1` · phase9, ops

  Found while working KEEL-132, by checking where a write actually landed.
  
  `~/.keel` holds `keel.duckdb` (40 MB), `keel.duckdb.wal` and `lance/`. There is no `keel.sqlite`. The running daemon is `~/.local/bin/keel-daemon`, installed 2026-08-11 21:04 — before `37a2341`, which is the commit where everything moved to SQLite. `~/.local/bin/keel` is the matching CLI, and its help still reads:
  
  ```
  backup         Back up both engines to Parquet
  ```
  
  So the tree is SQLite-only — `Cargo.lock` has no `duckdb` entry at all — while the store this project runs its own life on is the one Phase 9 replaced. `keel.duckdb.wal` had been written to seconds before this was noticed, by this session's own `keel_claim`.
  
  Nothing is broken and nothing is lost. What it means is narrower and worth stating exactly: **Phase 9's exit criteria are met by the code and not by the machine.** "`keel migrate` on the real store, verified by row count per table and hash per document" was run and verified, but against a copy — the live store was never cut over. Anyone reading STATUS.md would reasonably think otherwise.
  
  Two consequences for whoever picks this up:
  
  - **Do not run a CLI built from the current tree against `~/.keel` before migrating.** It will look for `keel.sqlite`, not find one, and create an empty store. `keel generate` would then rewrite every file in the repo from nothing. Use the installed binary until the cutover is done.
  - The measurements that justified Phase 9 — the search stall in particular — are still being paid on this machine, so anything measured here before the cutover is measuring the old engine.
  
  The sequence is `keel backup` on the old binary, stop the daemon, `keel migrate`, install the new binaries, start, and check that a write reaches `keel.sqlite`.

  - Cutover done. The sequence in this row's body was followed exactly, with one addition: an inventory taken from the backup's Parquet files before anything was written, so the migration could be checked against something that does not go through the DuckDB bindings.  - `keel backup` on the old binary — 1431 rows to `~/.keel/backups/2026-08-12T00-06-47Z`. - Daemon stopped. It checkpointed its 4 MB WAL and exited clean, so the file migrated from was quiescent. - Raw copy of `keel.duckdb` and `lance/` taken with a SHA-256, as insurance for the migration window. - `keel migrate` from a throwaway build at ab66018: **1599 rows across 18 tables, 234 document hashes and 1 blob hash — identical**. - Verified a third time by counting every table with `sqlite3` and diffing against the Parquet inventory. All eighteen agree. `pragma integrity_check` — ok. - New binaries built from this worktree and installed.  The store is 40 MB of DuckDB plus a Lance directory down to a **4.6 MB** SQLite file. `keel` went from 44 MB to 6.8 MB and `keel-daemon` from 75 MB to 38 MB, which is the vendored DuckDB leaving the binary.  This note is itself the check that a write reaches `keel.sqlite`: it went through the new daemon over MCP.  One thing the inventory settled on the way past: 234 documents and **zero** embeddings, straight out of the Parquet. That is KEEL-131 confirmed from the data rather than inferred — the daemon was started without `--embeddings`, so it stays true until someone runs it. <sub>— claude · 2026-08-12</sub>
  - How to go back, if it ever matters. The installed `keel` can no longer read DuckDB, so the recovery path is not obvious enough to leave unwritten:  1. `git worktree add <dir> ab66018` — the last commit that has both engines and `keel migrate`. 2. `CARGO_PROFILE_DEV_DEBUG=0 cargo build -p keel-cli` — about five and a half minutes, 2 GB of target, and it statically links its own DuckDB so it cannot mismatch the file it reads. 3. `restore` from `~/.keel/backups/2026-08-12T00-06-47Z`, which is the pre-cutover Parquet of both engines.  Do not use the brew DuckDB for this. `duckdb` 1.5.5 is on this machine and `keel-core` at that commit has a `bundled` feature you can turn off to link a system library — but the bindings are for a different version, and `migrate --verify-only` reads *and* writes through them, so a systematic misread would verify clean. Building the bundled one is the whole point.  The brew CLI is still the right tool for one thing, and it is what made this cutover checkable: Parquet is version-independent, so `duckdb -c "select count(*) from read_parquet(...)"` over the backup gives an inventory that does not go through the Rust bindings at all. That is what the eighteen row counts were checked against.  Two things found while verifying, both now moot but worth having said:  - The **old** binary's backup manifest omitted `notes` from its counts — 1431 reported against 1615 actual, the difference being 168 notes and the 16 migration rows. `notes.parquet` was written correctly, so no data was at risk; only the number was wrong. The new backup counts all 18 tables and reports 1600, so the bug went out with the code path. - A post-cutover backup is at `~/.keel/backups/2026-08-12T00-25-15Z` and is a plain `keel.sqlite` from `VACUUM INTO`, restorable with the binary that is actually installed. The old Parquet one needs step 1 above first, which is the argument for having taken this one. <sub>— claude · 2026-08-12</sub>

- **KEEL-133** keel ready says "nothing ready" when no daemon is listening `p2` · bug, cli

  Reproduces on the pre-Phase-9 binary against the DuckDB store, so it is not a Phase 9 regression. Found while verifying KEEL-129.
  
  In `crates/keel-cli/src/work.rs`, `ready()` gets `structured` from one of two places. `call_daemon` unwraps `structuredContent` before returning it. The direct fallback — `directly(...)` plus `keel_mcp::dispatch` — returns the whole tool-result envelope `{content, isError, structuredContent}`. The prose renderer then does `structured.get("ready")`, which is `None` for the envelope, so it falls into the `items.is_empty()` branch and prints "nothing ready".

  - All three work verbs had the mismatch, not just `ready`. `claim` and `close` read `reference` off the response and fell back to the argument when it was missing, so daemonless they printed whatever you typed — `keel claim tsk_01KZ… ` answered with the ULID and an empty title, and `close` never mentioned the `duplicates`/`supersedes` edge it had just drawn. `ready` was the loud one only because its missing field is a list, and an absent list renders as "nothing ready".  `lint` is clean: it reads through `read_daemon`, has no fallback at all, and bails when nothing is listening. There is no `keel activity` command — the activity feed is read over `/api/activity` in gate.rs, which unwraps `data` itself.  The root cause was two copies of "call the daemon, else open the store": `ready` had its own and it was the one missing the unwrap. There is now one `run_tool`, and the unwrap lives inside `directly` so a future caller cannot forget it. `keel_mcp::structured` is the single implementation, shared with the daemon's `as_api`, which had a third copy of the same two lines. <sub>— claude · 2026-08-11</sub>
  - Verifying this against the real store is not currently possible and that is worth knowing before someone tries. `~/.keel` still holds `keel.duckdb`, the daemon on 7654 is the binary installed at 21:03 on 2026-08-11 — before SQLite landed — and the current tree cannot read DuckDB at all. Commit 8150c26 already says the cutover has not happened.  So the daemonless path was checked against a store bootstrapped into a temp home instead: `keel bootstrap --home <tmp>` then `keel ready keel --daemon <dead port>` prints five rows where it used to print "nothing ready", and `claim` and `close` by ULID both answer with `KEEL-36` and its title.  One side effect to know about: running the daemonless path with the default home created an empty `~/.keel/keel.sqlite`, because `Store::open` creates and migrates a store that is not there. It holds no rows, so the deleted migration would not have refused it as a populated target — but a fallback that silently creates an empty store, and then says "no project matches `keel`", is a worse answer than "there is no store at this path". <sub>— claude · 2026-08-11</sub>

- **KEEL-132** SPEC.md sections 1 to 7 still describe the two-engine store `p2` · docs, phase9

  Phase 9's removal covered the decision log — D-1 is marked superseded by D-1a with the measurements that overturned it. It did not cover the body of the document, and the body is where the design is actually described.
  
  What is now wrong rather than merely dated: the architecture diagram, the storage layout in §2 and §3, the search design in §5 which describes `lance_hybrid_search` and a BM25 index that is rebuilt, and the backup format in §11 which describes a Parquet export plus a Lance dump.
  
  This is worse than a gap. A missing section reads as missing; a detailed description of a system that no longer exists reads as current, and it is the first thing a new session or a new person is pointed at.
  
  **Mark what changed rather than silently rewriting it.** The same treatment D-1 got: the old design was correct when it was written, and the reasoning that replaced it is only legible beside what it replaced. A spec that reads as though it always said SQLite loses the most interesting thing about the project's history.
  
  Worth pairing with a read of `PHASE-9.md`, which already contains the survey, the measurements and the fourteen-candidate comparison — much of what §2 and §5 need is written, in the right words, in another document.

  - §13 was left untouched on purpose and now needs KB. Six rows in the decision table argue from something that is gone: D-1a's closing clause says "§3 onwards still describes the old shape", which this revision made false; D-2 and D-2b say "dataset" where they mean a table; D-4's rationale is the DuckDB 1.5.x/DuckPGQ version conflict; D-5's is Quack; D-6's is "storage engines are Rust-native", which is now a C amalgamation compiled into the binary. Every one of those decisions still reaches the right conclusion — that is the interesting part — so nothing was quietly reworded to match. A blockquote under the table says which rows and why, and TQ-37 asks KB what to do about it. <sub>— claude · 2026-08-11</sub>

- **KEEL-131** Semantic search has never run — there are 227 documents and no embeddings `p2` · storage, daemon, search

  Found while verifying the Phase 9 migration. The store holds 227 documents and **not one has an embedding**; `~/.keel/models` is empty, so the model was never fetched.
  
  Nothing is broken in the sense of erroring. Search has always returned results. They have always come from the keyword index alone, and nothing anywhere says the other half is not running — which is exactly the silent degradation the search work was written to avoid, arriving from a different direction.
  
  This is not something the migration lost. The verification compares embedding width per document on both sides and found no difference: there was nothing there to drop.
  
  Two parts, and the second is the slow one.
  
  **Attach an embedder where the store is opened.** `SqliteStore::with_embedder` exists and `search` now consults it — the gap is that the daemon never passes one. `DuckStore` has the same shape and the same gap, so this is not a regression introduced by the move.
  
  **Backfill the 227 documents.** A pass over every current revision, embedding the body and writing the vector. Slow the first time because the model has to download, and worth a progress line rather than a silent wait. TQ-3 is the related open question — what happens when the model *changes* — and this does not settle it; it only fills what is empty.
  
  Done when a search whose answer shares no words with the query returns the right document, and `SearchSource::Semantic` appears in a real result rather than only in a test.

  - The machinery for this landed in b26805f (KEEL-151): `keel reembed --missing`, a startup warning naming the count, and a background model load so the first download does not block the listener.  **The backfill itself has not been run**, and this task should stay open until it has. Two reasons it was left: the running daemon holds the store, and `keel reembed` now refuses a direct write while a daemon is listening — correctly, since a second writer is exactly what KEEL-143 closed. And the daemon needs restarting onto the new binary anyway before it will attach an embedder at all.  The sequence, when someone is at the machine:  ``` keel doctor            # names the count: 133 current documents, none with a vector # restart the daemon onto the new binary, with embeddings enabled keel reembed --missing # first run downloads ~130MB keel doctor            # embeddings should now read ok ``` <sub>— claude · 2026-08-12</sub>
  - Costed the backfill before starting it, and found a second problem underneath it.  The install cost is smaller than the task implies. ONNX Runtime is already linked into the binary — `ort-sys` pulls a 77 MB prebuilt static archive at build time into `~/Library/Caches/ort.pyke.io`, and that has been happening on every build since Phase 9. Nothing about turning embeddings on adds a build step. The only new cost on a fresh machine is the model: `Xenova/bge-small-en-v1.5` is `onnx/model.onnx` at 133.1 MB plus ~0.9 MB of tokenizer and config, so ~134 MB into `~/.keel/models`. The vectors themselves are nothing: 135 current documents × 384 × 4 bytes = 207 KB against a 4.9 MB store.  Part one of this task is already done and the body is out of date — `KeelState::load_embedder_in_background` attaches a `FastEmbedder` on a thread after the socket is bound. What is left is the backfill and the first download.  The problem underneath: **there is no chunking, and `bge-small-en-v1.5` truncates at 512 tokens.** `Document::searchable_text_of` is `title + "\n\n" + body` and goes to the model whole. At roughly 3.3 chars per token that is about 1,700 characters. Measured against the live store, 55 of 135 current documents — 41% — are over that line: 19 of 21 specs, 17 of 57 decisions, 19 of 56 questions. The four largest documents get 2.5%, 3.0%, 5.0% and 5.3% of their text embedded. The Technical Specification is 67,594 characters and the model will see the first 2.5% of it.  So the backfill will work, and semantic search over decisions and questions should be good, and semantic search over specs will match on opening paragraphs only — and it will do that silently, which is the same failure shape this task was raised to fix, one level down. Worth a follow-up task for chunking, or at minimum a `doctor` check that counts documents over the window. <sub>— claude · 2026-08-13</sub>
  - Traced every consumer of the search path, because it is worth knowing what the backfill actually turns on.  There is exactly one call site in the workspace: `keel_search` in `keel-mcp/src/dispatch.rs:509`. Nothing else in `keel-core`, the daemon or the app calls `search`. It is reached from two surfaces — the MCP tool, and `/api/search`, which the app's Search screen uses and which goes through the same tool.  The two paths differ in one way. The MCP path precomputes the query vector in `AppState::embed_query` before taking the store lock; `/api/search` calls the two-argument `dispatch`, which passes `None`, so the query gets embedded inside the critical section. About 50 ms of model inference in the lock every other request queues behind. Worth knowing before anyone calls the app path slow.  Nothing else depends on embeddings, and some things that sound like they might do not: the command palette matches titles client-side off `/api/entities`, the digest and `keel_ready` are direct queries and the blocks-edge ranking, and duplicate detection on create is `title_similarity` — Jaccard over tokens, no vectors anywhere near it.  One gap this task's done criteria should cover: **`--embeddings` is off by default and nothing passes it.** No env var, no default in the Tauri shell, nothing in `scripts/`. So a backfill alone leaves new writes unembedded and the query half dark — the flag has to be turned on wherever the daemon is actually started, or made the default. <sub>— claude · 2026-08-13</sub>
  - The two halves of hybrid search disagree about archived rows, and turning embeddings on is what makes that matter.  The keyword half is correct and correct by construction: `fts_source` is maintained by triggers on `documents`, archived rows are already absent from it, and `search.rs:353` says so — "no query in this file carries a `WHERE archived_at IS NULL` that someone could later forget."  The semantic half is that forgotten filter. `search_semantic` reads `documents` directly with `WHERE status = 'current' AND embedding IS NOT NULL AND length(embedding) = ?` and no archive predicate, and `reembed_missing` has none either. So a backfill run today would embed every archived document and hand them to the fusion, where BM25 correctly refuses to return them and cosine happily does.  Measured on the live store: **13 of 135 current documents belong to archived entities — 10 specs, 2 decisions, 1 question — and they are 163,127 of 520,489 characters, 31% of the text.** Among them are "Keel — Decision log" (26,840 chars) and "Keel — Status" (56,299), which are generated rollups of rows that are already indexed individually, so embedding them would double-count the whole decision register even if they were live.  The test that looks like it covers this cannot. `archived_rows_leave_the_index` archives a **task**, and tasks have no row in `documents` and therefore no embedding — so the semantic half is never exercised and the assertion passes whatever it does. The test needs a document-bearing type.  Excluding archived leaves 122 documents and 357,362 characters, which is the real size of the backfill. <sub>— claude · 2026-08-13</sub>
  - I started a second daemon against the live store by accident, and it migrated the store out from under the one that was already running.  The command was meant to point at a scratch copy to test semantic search before touching anything real. I passed `--bind` and `--embeddings` and forgot `--home`, so it opened `/Users/h8hcn/.keel`, applied migration 4 there, and ran alongside the installed daemon — two writers on one store, which is the thing hard constraint 1 exists to forbid, and a schema change under a live reader, which is the thing the migration guard exists to forbid. The guard cannot catch it: it refuses a *binary older than the store*, and this was a binary newer than it, arriving by the one route nobody guards — a second process opening the same default home.  What I noticed it by was `/api/health` reporting `schema: 4` when the live store should have been at 3. Had the numbers happened to agree, nothing about the output would have looked wrong.  No damage. `pragma quick_check` is `ok`, 376 entities and 243 documents are all present, and migration 4 is the one I had already verified on two copies — so the store went where it was going anyway, just by accident and in the wrong order. The stale daemon was stopped, the binaries reinstalled, and the sequence finished properly.  Two things worth taking from it. The first is that `--home` defaulting silently to the live store is fine for a person typing one command and sharp for anything scripted; every scratch invocation in this session that *did* carry `--home` was safe, and the one that dropped it was not. The second is that the health endpoint reporting the schema is what made this visible in seconds — that field was added by the B-57 work earlier today and it earned itself immediately. <sub>— claude · 2026-08-13</sub>

- **KEEL-130** 9C — Take DuckDB and Lance out of the repository entirely `p1` · phase9, 9c, tooling, storage

  Last, deliberately. Nothing here happens until KEEL-127 has verified the migration and KEEL-129 has walked every surface.
  
  Everything that has to change, found by searching the tree today:
  
  **Dependencies.** The workspace `duckdb` entry in the root `Cargo.toml`, the per-crate entries in `keel-core`, `keel-cli` and `keel-daemon`, the `bundled` feature `keel-core` owns, and whatever falls out of `Cargo.lock`.
  
  **Code.** `store/duck.rs` — 2,119 lines — goes. `store/docs.rs`, `store/schema.rs`, `store/rows.rs` and `store/mod.rs` are rewritten or renamed by the tasks before this one, and this is where the last references go. `DuckStore` is exported by name from `store/mod.rs`, so the rename reaches every caller.
  
  **Error variants.** `error.rs` has storage errors phrased in DuckDB's terms. An error that names an engine nobody is running is worse than a generic one.
  
  **Prose in doc comments.** Twenty-odd files describe the design as "DuckDB plus Lance", including the module doc on `store/mod.rs` and the `SearchSource` variants. These are the ones most likely to be missed, because they compile.
  
  **The profile settings that exist only for DuckDB.** `[profile.dev] debug = "line-tables-only"` and `[profile.dev.package."*"] debug = false` were added because DuckDB's debug info filled a disk. With the vendored C++ gone, full debug info is affordable again and the comment explaining the workaround should not outlive the problem.
  
  **`plugin/install.sh`, `scripts/gate-run.sh` and `.github/workflows/ci.yml`.** The install script and CI both carry DuckDB build handling.
  
  **The desktop app.** `Home.tsx` and two test files mention the engines in user-facing or fixture text.
  
  **`deny.toml`**, if either engine has an entry there.
  
  **The contract itself.** `product/CLAUDE.md` names the three traits as "DuckDB entity CRUD" and "Lance documents and blobs", hard constraint 1 says "no other process writes to DuckDB", and the definition of done has a note about the vendored DuckDB filling the disk. Those are edited in Keel and regenerated, never in the file.
  
  One thing that is not removal and is worth doing in the same pass: KEEL-119, the test suite leaking a 4.8 MB DuckDB store per killed run into `TMPDIR`. A SQLite fixture is small enough that the leak stops mattering, which is most of that task's argument answered by this one rather than by fixing it.

  - The full inventory, taken from the tree on 2026-08-11 rather than from memory, so whoever picks this up is not searching from scratch. **52 files** mention DuckDB or Lance outside the new `store/sqlite` tree.  **Deleted outright:** `store/duck.rs` (2,119 lines), `store/docs.rs`, `store/schema.rs`, `store/rows.rs`, `backup.rs`. Then `sqlite_backup.rs` is renamed to `backup.rs` and `store/sqlite/*` is promoted out of its subdirectory.  **Dependencies:** the workspace `duckdb` entry, per-crate entries in `keel-core`, `keel-cli`, `keel-daemon` and `keel-mcp`, the `bundled` feature `keel-core` owns, and the fallout in `Cargo.lock`.  **The two profile settings that exist only for DuckDB.** `[profile.dev] debug = "line-tables-only"` and `[profile.dev.package."*"] debug = false` were added because DuckDB's debug info filled a disk. With the vendored C++ gone, full debug info is affordable again, and a comment explaining a workaround should not outlive the problem it worked around.  **`Error::Storage`** wraps a `duckdb::Error`. It collapses into `Error::Sqlite`, and `Error::sqlite` should take its name back.  **The prose is the part that gets missed, because it compiles.** The module doc on `store/mod.rs` describes the design as DuckDB plus Lance. `SearchSource::Keyword` documents itself as "the DuckDB BM25 index" and `Semantic` as "the Lance vector index". `document.rs`, `entity.rs`, `enums.rs`, `fsck.rs`, `lint.rs`, `note.rs`, `render_status.rs`, `style.rs` and `types.rs` all carry references in comments.  **Outside Rust:** `.github/workflows/ci.yml`, `plugin/install.sh`, `scripts/gate-run.sh`, and `apps/desktop` in `Home.tsx` plus two test files.  **The contract itself**, which is edited in Keel and regenerated, never in the file: `product/CLAUDE.md` names the three traits as "DuckDB entity CRUD" and "Lance documents and blobs", hard constraint 1 says "no other process writes to DuckDB", and the definition of done carries a note about the vendored DuckDB filling the disk. SPEC D-1 says "DuckDB + Lance, no SQLite" and is now superseded rather than wrong — it should be marked, not deleted, because the reasoning behind it was sound at the time and the survey that overturned it is what makes the change legible.  **Nine `keel-core` integration tests and two `keel-daemon` ones** construct a `DuckStore` directly. They are the migration's specification, so they get repointed at `SqliteStore`, never rewritten to accommodate it.  **Two things to fix that were found while building and could not be fixed in place**, because the files belonged to another agent at the time: `store/sqlite/docs.rs` carries its own copy of `TIMESTAMP_FORMAT` and `parse_ts` — `rows.rs` now exposes them as `pub(super)`, so the copy should go. And `scripts/sweep-build-artifacts.sh` names `keel.duckdb` when looking for leaked test stores; it already also checks `keel.sqlite`, but the comment explaining the 237 MB static library stops being true.  **What the removal is actually worth, measured:** `libduckdb.a` is 237 MB and every test binary links it statically, so thirty test binaries are 8.5 GB. SQLite's amalgamation is roughly 1.5 MB. A cold release build was 22m11s and is 23s. <sub>— claude · 2026-08-11</sub>
  - `keel migrate` was deleted along with DuckDB rather than kept working. It had already done its job — it ran against the real store and verified clean under KEEL-127 — and keeping it alive meant keeping a 237 MB dependency in the tree forever to serve a one-time move that has already happened. That dependency is the entire cost this task existed to remove, so the migration could not survive it.  **Anyone who still needs to migrate an old DuckDB-and-Lance store can check out `37a23412b97661f5bdc91d550b986730c06a5176`**, the commit immediately before the removal. That tree has both engines, `crates/keel-core/src/migrate.rs`, and the `keel migrate` subcommand, and it is the last commit where they exist.  Three things did *not* go with it, deliberately. `rows.rs::parse_ts` and `get_odate` still accept DuckDB's looser rendering — RFC 3339 with a variable-width fraction, sometimes a space where the `T` should be — because the migration wrote those values and they are sitting in real stores on disk today. Refusing them now would make already-migrated rows unreadable. `backup.rs` still recognises a directory holding `duckdb/` and `lance/` and says so, because someone restoring an old backup deserves a sentence explaining why it cannot be read rather than a confusing parse error. And `store/schema.rs` keeps the account of why the numbering restarts at 1 with everything in it, which is the answer to "where are migrations 2 through 16". <sub>— claude · 2026-08-11</sub>
  - Naming, measured results, and two things worth knowing.  **`SqliteStore` became `Store`.** Naming a type after its engine earns its keep only while there are two engines to tell apart. With one, `keel_core::Store` says what it is, and the three traits already say which part of it does what. The `store::sqlite` tree was promoted into `store`, so `store::sqlite::entity` is now `store::entity`.  **`Error::Sqlite` and `Error::Storage` collapsed into `Error::Storage`, constructor `Error::storage`.** Same argument: a caller cares that storage failed, not which library said so. The `#[source]` still carries the concrete `rusqlite::Error`, because a source that has lost its type is a source nobody can match on.  **Numbers.** Cold release build 1m26s and 1m32s on two consecutive clean runs, 8 cores — so the under-90s exit criterion is met on a quiet run and sits right on the line otherwise. `target/` was 696 MB before the clean, against the 17 GB the sweep script used to describe. 657 workspace tests pass, down from 696: every one of the 39 lost was a unit test inside a deleted file — 17 in `migrate.rs`, 7 each in the old `store/docs.rs` and `store/schema.rs`, 4 each in the old `backup.rs` and `store/rows.rs`. Both insta suites have zero diffs, and the desktop app's 220 tests pass.  **Two things found on the way.** `tests/readable_ids.rs` had a test asserting that a migration's `substr` length matched `MAX_PROJECT_KEY` — it was reading the *DuckDB* schema, and promoting the SQLite one into `store::schema` made it fail. The duplication it guarded no longer exists, since the SQLite schema is one initial migration with nothing to backfill, so it was replaced with a test that ties the derived-key length back to the constant.  And `product/SPEC.md` could not be written back through `keel_write_doc` at all: §3.4 contained the banned word `leverage`, so the document had been unwritable since the house-style gate landed. Reworded to "the decision in the spec with the widest reach". Worth knowing that a style rule added later can silently strand an existing document — nothing warns you until the next write, which may be months away. <sub>— claude · 2026-08-11</sub>
  - A blemish in the SPEC's revision history, recorded so nobody wastes time on it later. Revision 4 of `spc_01KZKMPVNTZAZHC9HY1TSNZNGM` has the body `probe` and is five bytes long. I wrote it while trying to work out why a full-size write was returning HTTP 400 — I sent a tiny body to see whether the size was the problem, forgetting that a successful write is a write. It was not the size; it was the house-style gate refusing a banned word in the existing text.  Revision 5 restores the full document with D-1 superseded, so the current revision and every generated file are correct. Nothing was lost, because revisions are append-only and revision 3 is still readable. But `keel_get` with `version: 4` will return five bytes, and a diff across that boundary looks alarming.  The lesson generalises: there is no dry-run on `keel_write_doc`, and a probe against a real document is a real revision. Probe against a scratch artifact instead. <sub>— claude · 2026-08-11</sub>
  - **A sequencing mistake worth recording, because it was mine and it is the kind that is easy to repeat.**  I briefed this task to delete `keel migrate` along with the engine it reads, on the reasoning that its job was done — it had run and verified clean. That reasoning was wrong in one word. It had run against a **byte-identical copy** of the live store, not the live store. Copying proves the code; it does not perform the cutover.  So the live `~/.keel` is still DuckDB, still served by the previously installed daemon binary, and the only tool that can move it now lives one commit back at `37a23412b97661f5bdc91d550b986730c06a5176`. That is workable and it is written into the commit message, but it is a worse position than doing it in the other order, and there was no reason to do it in this one.  The general shape: **"verified on a copy" and "done" are not the same claim, and a removal that happens between them strands the thing that closes the gap.** The migration should have been the last thing deleted, after the store it exists to move had actually moved.  Nothing is lost and nothing is at risk — the old store is untouched and working, the branch is unmerged, and the migration is one `git checkout` away. It cost a worse cutover procedure rather than any data.  **What the cutover actually needs, for whoever does it:** stop the daemon, build from `37a2341`, run `keel migrate` against `~/.keel`, confirm the verification is clean, install the binaries built from this branch, restart. The safety copy taken at the start of this session is at `/tmp/keel-backup-before-migration` and already contains a migrated `keel.sqlite`, so the first run has a known-good result to compare against. <sub>— claude · 2026-08-11</sub>

- **KEEL-129** 9C — Every surface still works, checked rather than assumed `p1` · phase9, 9c, testing

  The test suite is the migration specification and it is not the whole story. Tests run against a fixture; the real store has 122 tasks, 62 documents, seven sessions of events and one design image, and it is where the awkward rows live.
  
  What gets exercised against the migrated store, by hand, once:
  
  `keel generate keel` and then `keel generate keel --check` — the check has to be clean, which proves every renderer reads the new store identically.
  
  `keel render-status`, `keel fsck`, `keel lint`, `keel ready`, `keel claim`, `keel close`, `keel note`, `keel backup` and `keel restore`.
  
  The daemon: every route under `/api`, the MCP endpoint, and the SSE event stream, which is the one that depends on the event cursor staying monotonic.
  
  The app: every screen, with the browser console watched for errors. The Library's design thumbnail is the blob path end to end, and search is the screen most likely to be subtly wrong rather than broken — a fusion with an inverted sign returns results in a plausible-looking order.
  
  The two snapshot suites are the contract and their diffs get read rather than accepted. A snapshot that changes because the store changed is a bug: the MCP surface is not supposed to know what is underneath it.
  
  The measurement from KEEL-123 gets re-run here, same script, and the before and after go on the milestone.

  - The switchover found two things the SQLite store was missing, both silent.  **`write_revision` never embedded.** `DuckStore::write_revision` computed a vector from the store's embedder on the way in; the SQLite one wrote whatever `document.embedding` already held, which is always `None` for every caller. So with the SQLite store attached to a real embedder, no document would ever get a vector and semantic search would return nothing — while keyword results kept arriving and nothing said the other half had stopped. `documents.rs::embeddings_are_written_when_an_embedder_is_attached` caught it. The fix is the same block DuckDB had, ported.  **No older-binary guard.** DuckDB refused to open a store whose migration ledger is ahead of the binary — the check written after KEEL-95, where a daemon 84 seconds behind wrote a NULL into a column it did not know about. SQLite's `migrate()` just skipped ahead and opened. `roundtrip.rs::a_binary_older_than_the_store_refuses_to_open_it` caught it. Ported, with the same message.  Both are the case for repointing the existing tests rather than writing new ones: neither gap is something a test written against the new store would have thought to look for. <sub>— claude · 2026-08-11</sub>
  - The KEEL-123 measurement, re-run on the same terms: debug build, a copy of the real store, 20 rounds. Before is the second table in KEEL-123's note, which was taken exactly this way.  ```                      DuckDB      SQLite tasks                31.2ms      16.8ms milestones           19.8ms       1.8ms note counts          14.2ms       2.9ms ranking + blocked   508.0ms      23.6ms digest depth=full   999.0ms      46.0ms every note body      19.8ms       8.6ms search               45.9ms       2.9ms   (max 3.5ms) board load serial   573ms        45.1ms ```  The one worth pointing at is search's *max*, not its mean. DuckDB's full-text index does not track inserts, so the first search after any write rebuilt the whole thing; FTS5 is maintained by triggers, so there is nothing to rebuild and the worst sample is 3.5 ms against a 2.9 ms mean. The stall KEEL-123 went looking for does not exist any more rather than being smaller.  Payload is unchanged, as expected — the same JSON either way. <sub>— claude · 2026-08-11</sub>
  - Two things the switchover changes that are nobody's bug and both worth knowing.  **The single write path is now a convention, not a lock.** `keel note add` opens the store directly rather than going through the daemon. Under DuckDB that failed while the daemon was up — the lock refused it — so hard constraint 1 was enforced by the engine. In WAL mode a second writer just works, and I watched it work: a note written from the CLI landed in a store the daemon had open. Nothing was lost, because SQLite serialises the two writers properly. But the constraint now holds only because the code happens to be arranged that way, and the six non-locking steps of a Keel write — validation, provenance, the event, the revision, the embedding, the index — are exactly what a second writer can skip.  **Search returns the same documents in a slightly different order.** Same query, same corpus, both keyword-only: the result sets match, the ranking does not, because BM25 term statistics come from the index and DuckDB's FTS and FTS5 tokenise differently. Not a fusion sign error — the semantic half was returning nothing on both, since neither store has embeddings yet (KEEL-131). <sub>— claude · 2026-08-11</sub>
  - The switchover landed and every surface was exercised against a migrated copy of the real store rather than against a fixture. 696 workspace tests pass, 220 desktop tests pass, and **both insta snapshot suites have zero diffs** — which is the result worth having on its own: the MCP surface is byte-identical across an engine swap, so it never knew what was underneath it. That is the Phase 0 trait boundary being collected on.  `keel generate keel --check` is clean, and 66 of the 67 generated files are byte-identical to the DuckDB-generated ones once the timestamp banner is stripped. The one difference is `product/STATUS.md`, and it is real state change rather than drift.  **Two silent regressions the repointed tests caught**, both of the same shape — code that keeps working and quietly does less:  The SQLite store never computed embeddings on write. `DuckStore::write_revision` embedded from the store's embedder; the SQLite one only stored a vector a caller supplied, and no caller supplies one. Semantic search would have been dead in the way that returns keyword results and reports nothing.  The older-binary guard was missing. DuckDB refused to open a store whose migration ledger runs ahead of the binary — the check written after KEEL-95, where a daemon 84 seconds behind wrote NULLs into a column it did not know about. SQLite opened it happily.  Neither needed a new test. Both fell out of repointing existing ones, which is the argument for repointing rather than rewriting.  **The performance criterion, re-measured on the same script and the same store copy:** board load **573 ms → 45.1 ms** serial; ranking and blocked 508 → 23.6 ms; digest 999 → 46 ms; search 45.9 → **2.9 ms with a max of 3.5 ms**. The BM25 rebuild stall is gone rather than reduced — the max is now within a hair of the mean, which is what "no rebuild" looks like from outside.  **One consequence to be aware of rather than pleased about.** In WAL mode a second process can open the store while the daemon holds it. `keel note add` opens it directly and now simply works; DuckDB's lock used to refuse. **Hard constraint 1 — the daemon owns the single write path — is now a convention rather than something the engine enforces.** Nothing was lost, and the six of seven reasons for the single write path are unchanged, but the guard rail that made violating it impossible is gone. Worth a decision on whether to enforce it some other way or to accept it.  Two smaller things found: search returns the same documents in a slightly different order, which is two BM25 implementations and two tokenisers rather than a fusion sign error — the semantic half returns nothing on *both* stores because neither has embeddings (KEEL-131). And `keel ready` prints "nothing ready" with no daemon running while `--json` shows the tasks; confirmed pre-existing against the stashed pre-Phase-9 binary, so it is not a regression from this work. <sub>— claude · 2026-08-11</sub>

- **KEEL-128** 9B — Backup becomes one file, and restore stops needing two halves `p1` · phase9, 9b, storage, cli

  `VACUUM INTO`, measured at 1.2 ms against a live database. It takes a consistent snapshot without stopping the daemon, which the current two-format export cannot promise: the DuckDB export and the Lance dump are two operations, and a write landing between them produces a backup that is internally inconsistent and passes every check.
  
  The backup round-trip test already exists and already asserts equality rather than eyeballing it — back up, wipe, restore, diff. It keeps working, and it gets shorter.
  
  The half of `restore` that refuses a backup missing its Lance directory is deleted, not left in place returning false. Dead validation of a condition that can no longer occur reads as a live safety check to whoever finds it next.
  
  Worth checking while here: whether anything in `~/.keel/backups` is in the old two-part format, and what a person with old backups is told when they try to restore one. Refusing clearly beats reading it wrongly.

- **KEEL-127** 9B — keel migrate: read the old store, write a new one, prove they match `p0` · phase9, 9b, storage, cli

  It never modifies the old store. It reads and writes a new file, so a failed migration costs nothing and can be retried.
  
  KEEL-95 is the live lesson this is written against: a migration added a column, a daemon 84 seconds behind wrote a NULL into it, and every decision in another project became unreadable. B-44 came out of it — a column added by a migration is NULL for every writer that has not restarted yet. A whole-engine migration is that hazard at its largest, which is why nothing is done in place and why the daemon has to be stopped first.
  
  **Verification is the deliverable, not the copy.** Per table, a row count on both sides. Per document, a content hash on both sides. Per blob, a byte hash. It reuses the assertion the backup round-trip test already makes rather than inventing a second way to say the same thing. If anything differs, it names what and exits non-zero.
  
  Things that are easy to get silently wrong in a copy like this and each deserve their own check:
  
  Archived rows. Soft delete means nothing is ever gone, so a migration that skips `archived_at IS NOT NULL` loses the audit trail and every count still matches on the live rows.
  
  Links, both stored directions. Only `blocks` is ever stored and `depends_on` is swapped on write. A migration that re-normalises on the way in would double-swap and invert the whole graph.
  
  The event log and its ordering, since the cursor has to stay monotonic across the move.
  
  Embeddings. They can be regenerated, but a run of `fastembed` over the whole corpus is slow and pointless if the vectors copy cleanly — copy them, and verify by dimension and a sample hash.
  
  Task numbers and ranks. `KEEL-42` means the same thing forever and a migration that renumbers breaks every reference in every document.

  - The verification only means something if it recomputes. Comparing the old store's body_hash column against the new one's is vacuous: the column is copied verbatim, so it agrees with itself whatever happened to the prose beside it. verify hashes the title and body that actually landed and compares that against the hash the old store recorded, and does the same for blob bytes. The two tests that prove it edit the target's body and bytes while leaving the stored hashes alone, and both were caught. <sub>— claude · 2026-08-11</sub>
  - One legacy shape the new schema cannot hold, so the migration checks for it before writing anything: two tasks or two decisions in the same project with a NULL number. Both read back as 0, and SQLite's unique index over (project_id, number) treats NULLs as distinct but two zeroes as a collision. Left alone it would fail halfway through the copy with `UNIQUE constraint failed: tasks.project_id, tasks.number`, which names the index and not the cause. Two queries up front turn that into a sentence saying which project to fix. This is KEEL-95 and B-44 again, one size larger. <sub>— claude · 2026-08-11</sub>
  - Two things about the SQLite schema changed under this task while it was being written, and both turned a planned loss into no loss at all. The events table gained summary and meta, so migrated history keeps the sentence a person reads instead of rendering blank in the changelog and the activity feed. And the keyword index became external-content FTS5 maintained by triggers on the tables themselves, which means a row-level copy that never touches the write path still arrives with a populated index — and archived rows stay out of it, because the same triggers say so. There is now a test asserting a migrated row is findable by keyword. <sub>— claude · 2026-08-11</sub>
  - Left in_progress rather than closed, deliberately. The library half is finished and tested — keel_core::migrate::{migrate, verify} in crates/keel-core/src/migrate.rs, seventeen tests green under `cargo test -p keel-core --lib migrate`. What this task asks for and does not yet have is the `keel migrate` command wired into the CLI, and a run against the real store with the row counts and content hashes checked by the command rather than by eye. That was held back on purpose: the CLI is being edited elsewhere this session. Whoever picks it up needs a CLI subcommand that opens the old store, opens a fresh SQLite file beside it, calls migrate then verify, prints the report and exits non-zero when it is not clean. Stop the daemon first — DuckDB has one write lock. <sub>— claude · 2026-08-11</sub>
  - Run against the real data, and it is clean. Not the fixture — a byte-identical copy of the live `~/.keel`, taken first so the running daemon never had to stop and a failed run could cost nothing.  **1,514 rows across 18 tables, 227 document hashes and 1 blob hash, all matching. 1.9 seconds.** That is the exit criterion: verified by row count per table and hash per document, by the command rather than by eye.  The store goes from **40 MB to 4.3 MB** — 34 MB of DuckDB and 5.5 MB of Lance become one 4.3 MB file.  Spot-checked afterwards rather than trusting the report alone. All 137 task numbers survived and `KEEL-124` still reads "9B — The SQLite store: schema, EntityStore and GraphStore", which is the thing that must never drift because it is cited in prose nothing rewrites. All 741 events kept their summary. 48 `blocks` links, **0 `depends_on`** — the graph was copied verbatim rather than re-normalised, which would have double-swapped it and inverted every edge. 294 rows in the keyword index, populated by the triggers during a copy that never touched the write path.  **The finding worth acting on, and it is about the live system rather than the migration: there are 227 documents and not one of them has an embedding.**  `~/.keel/models` is empty, so the embedding model was never downloaded. That means the semantic half of hybrid search has never run on this store — every result Keel has ever returned came from the keyword index alone. The verification compares embedding width per document on both sides and reported no difference, so this is not something the migration dropped; there was nothing there to drop.  It matters more now than it did, because it is exactly the failure mode the search work was warned about: results keep arriving, they are merely worse, and nothing in the output says half the system is not running. Worth its own task — the fix is to attach an embedder where the daemon opens the store and backfill the 227 documents, and neither belongs in this one. <sub>— claude · 2026-08-11</sub>

- **KEEL-126** 9B — Search on FTS5 and sqlite-vec, with an index that does not need rebuilding `p0` · phase9, 9b, storage

  This is the one part of the migration that is not a straight substitution, and it is also the part that fixes the stall in KEEL-123.
  
  **The keyword half.** FTS5 with triggers on the source tables, so the index updates when a row changes. That is the whole point: today `refresh_entity_index` runs `CREATE OR REPLACE TABLE fts_entities` over every searchable table and then rebuilds the BM25 index from scratch, because DuckDB's full-text index does not update when its table changes. Measured cost of that: 217 ms on the first search after a write, against a 13 ms mean.
  
  FTS5 has its own `bm25()` scoring function, which returns a negative number where lower is better. The fusion currently expects higher-is-better. Getting that sign wrong ranks the worst match first and looks like a plausible ordering, so it needs a test that asserts an obviously-best document comes first rather than one that asserts a score.
  
  **The vector half.** `sqlite-vec`, brute force over a few thousand vectors, measured at 1–3 ms. It is 0.1.9 and its author says to expect breaking changes. That is survivable here because the vectors are stored as ordinary blobs rather than inside a proprietary index — replacing the search is about fifty lines, and the embeddings do not have to be regenerated to do it. Worth writing that into the doc comment so the next person knows the escape route exists.
  
  **The fusion stays.** Reciprocal rank, because BM25 scores and vector distances are not on comparable scales. `k_inner = k_outer * 4` stays too — retrieving exactly k from the index and filtering by project afterwards is how you return three results when forty exist.
  
  `SearchSource::Keyword` currently documents itself as "the DuckDB BM25 index" and `Semantic` as "the Lance vector index". Both need rewording, and `Both` — an independent keyword and semantic match agreeing — stays the strongest signal available.

  - The vector half is `sqlite-vec`'s `vec_distance_cosine` scalar function over the `documents.embedding` column, scanned in full — no `vec0` table. A `vec0` table is a second copy of every vector and needs a trigger in the schema to stay in step; without that trigger the only way to use it is to repopulate at search time, which is the rebuild this task removes wearing a different hat. Brute force over a few thousand vectors is 1-3 ms and cannot go stale.  One trap found while building it: `vec_distance_cosine` raises an error when the two vectors differ in length, and that error fails the whole query, so a single document left behind by a model change would take out semantic search for everything. `length(embedding) = ?` in the WHERE clause skips those rows instead. There is a test for it. <sub>— claude · 2026-08-11</sub>
  - `SqliteStore` has no embedder, so the trait's `search` is keyword-only today. The struct is `{ conn, path }` and adding a field means editing `mod.rs`, which this task was scoped away from. The semantic half is reachable now through `SqliteStore::search_with(&query, Some(&embedder))`, and the fix is the three lines `DuckStore` already has: an `embedder: Option<Arc<dyn Embedder>>` field, `with_embedder`, and an `embedder()` accessor. Then `search` passes `self.embedder()` instead of `None`.  Worth doing before the daemon switches stores, because the failure is silent: search would keep returning results, just keyword ones, and nothing in the output would say the semantic half had stopped running. The doc comment on `search` says so too. <sub>— claude · 2026-08-11</sub>

- **KEEL-125** 9B — DocumentStore on SQLite: revisions, blobs in the same transaction `p0` · phase9, 9b, storage

  Revisions become a table. Blobs become a table with a `BLOB` column, which is the change that removes the awkward part of the current design: an image and the artifact it belongs to are written in one transaction rather than in two stores that can disagree.
  
  Measured in the Phase 9 survey: a 5 MB blob writes in 49.6 ms and reads in 10.8 ms. That is fast enough that incremental blob I/O is not needed and should not be reached for.
  
  The revision chain is the thing with a property test on it already, and it stays: version numbers are assigned by the store, the previous revision stays readable by version, and writing content identical to the current revision is a no-op rather than a new version.
  
  Two behaviours that are easy to lose in a translation and are load-bearing:
  
  `write_revision` returns the document with whatever version the store actually assigned, not the one the caller hoped for. Callers depend on that.
  
  `diff` is a store operation, not a UI one. It satisfies REQ-2 at the API layer, so it stays in the trait rather than moving into a renderer.
  
  Search is not in this task — it is the next one, because it is the only part where SQLite is not a straight substitution.

  - Two things the next person needs. First, `store/sqlite/docs.rs` has no `impl DocumentStore for SqliteStore` — the trait needs `search`, which is KEEL-126, so the six methods are inherent methods on `SqliteStore` with the trait's exact names. Whoever lands search writes the impl block as six one-line delegations, and each one must use the fully-qualified form `SqliteStore::revision(self, …)`; `self.revision(…)` inside the impl resolves to the trait method being written and recurses until the stack goes.  Second, the timestamp format `%Y-%m-%dT%H:%M:%S%.6fZ` is written out a second time in docs.rs because `rows::ts` and `rows::parse_ts` are private to that module and this task could only touch one file. Making them `pub(super)` and deleting the copy is a five-minute cleanup; there is a test in docs.rs holding the fixed-width and sort properties in the meantime.  Measured on this machine, file-backed store in WAL: a 5 MB blob writes in 55.2 ms and reads in 2.1 ms. The survey said 49.6 ms and 10.8 ms, so the write matches and the read is faster than expected — nothing here needs incremental blob I/O. <sub>— claude · 2026-08-11</sub>

- **KEEL-124** 9B — The SQLite store: schema, EntityStore and GraphStore `p0` · phase9, 9b, storage

  `rusqlite` with the bundled amalgamation. The schema in `store/schema.rs` is 937 lines of DuckDB DDL and most of it translates directly — the work is in the places where it does not: DuckDB's types, its sequences, and anything relying on `v_entities`, the UNION view over all thirteen tables.
  
  The trait boundary is what makes this a branch rather than a rewrite. `EntityStore` and `GraphStore` are the two being implemented here; `DocumentStore` is the next task. No raw SQL exists outside their implementations, which was insisted on in Phase 0 and has never been collected on until now.
  
  **Graph traversal is the part to be careful about, and the contract already says why.** An inverted traversal returns an empty result that looks exactly like a legitimate "nothing is linked here". SQLite supports recursive CTEs, which is the thing that ruled out Turso. `product/SPEC.md` §3.3 is the only authority on direction and gets read again rather than translated from what the DuckDB implementation happens to do — a bug carried across faithfully is still a bug.
  
  **Optimistic concurrency is per-row and must stay per-row.** The version check and the increment happen in one statement under one transaction, or two writers both read version 7 and both succeed.
  
  The event cursor has to stay monotonic. It is what `keel_activity` pages from and what the FTS watermark compares against.
  
  Done when the existing test suite passes unchanged: round-trip for every entity type, two simultaneous writers with zero lost updates, the same create twice returning `created: false`, a stale-version update rejected with the current state, and one test per relation asserting both directions.

  - Re-verified every storage claim against running code on this machine before writing a line of the store, which is what TQ-7 asked for and what the Phase 9 survey could not do — its numbers came from a two-core probe box. `rusqlite` 0.40.2 bundling SQLite 3.53.2, `sqlite-vec` 0.1.9.  **Cold release build, from an empty target directory: 23.1 seconds.** Every dependency, including the bundled amalgamation. DuckDB's measured equivalent is 22m11s. The exit criterion asks for under 90 seconds and this is a quarter of it.  All seven capabilities the survey claims are real:  - Recursive CTEs walk outbound and inbound, with depth and path carried. This is what ruled Turso out and it works. - FTS5 with `content=` and triggers found a row written in the previous transaction in **68 µs with no rebuild**, and an update correctly removed the stale term from the index. That is the 217 ms stall gone rather than reduced. - `vec0` returned the correct nearest neighbour over 384-dimension vectors in 307 µs. - A 5 MB blob wrote in **2.1 ms and read in 351 µs** — the survey said 49.6 ms and 10.8 ms, so it is twenty times better than budgeted on this hardware. - `INTEGER PRIMARY KEY AUTOINCREMENT` gave 1, 2, 4 after a delete. The cursor stays monotonic and never reuses. - Per-row optimistic concurrency in one statement: the first writer changed one row, the stale writer changed zero. That is the whole mechanism, and it needs no lock. - `VACUUM INTO` produced a 6.9 MB file in 64 ms. The survey's 1.2 ms was an empty database; 64 ms with a 5 MB blob in it is the honest number.  **The one that matters for KEEL-123.** In WAL mode, a reader hitting the store *while a 10,000-row write transaction was open* answered in **12 µs** with the correct pre-transaction snapshot, against 152 µs on a quiet store. It did not block. So the write path stops being something the app can queue behind at all.  **One thing that has to change and is not a detail.** `sqlite-vec` registers through `sqlite3_auto_extension`, which needs an `unsafe` block, and the workspace sets `unsafe_code = "forbid"`. `forbid` exists precisely so an `#[allow]` cannot override it, so this is not something to work around locally — it has to become `deny` with exactly one documented exception at the registration site. Recording it here rather than doing it quietly: it relaxes a stated engineering standard, and the whole reason to write it down is that the next person should find an argument rather than a surprise. <sub>— claude · 2026-08-11</sub>
  - Done, and the finding worth keeping is not about SQLite.  **The `events` table was built without `summary` or `meta`, and nothing failed.** Every write succeeded, every row count was right, and every event read back with an empty sentence. `render_status` renders that summary into the changelog and the daemon's activity feed returns it, so after the migration both would have gone blank — for *new* history only, while everything written before the move still read correctly. A changelog that is right up to a date and empty after it looks like a project that stopped happening.  Forty-two tests passed over that hole. All of them asserted on rows, statuses, versions and counts; none asserted on the sentence a person reads. That is the shape of the gap, and it is worth remembering as a class rather than as one bug: a test suite built around invariants can be complete about structure and silent about content.  It was found because the agent implementing `EntityStore` hit the missing columns, could not fix them in a file it did not own, and **wrote the gap down as a gap** — with the consequence spelled out and the two-line fix named — rather than quietly setting the fields to empty and moving on. That is the whole difference between a five-minute fix and an empty changelog discovered in a month.  Fixed in migration 1, wired through the insert, the projection and the reader, with a regression test asserting the summary is non-empty and names what was created, and that `unlink`'s `{"removed": true}` marker survives — without it an unlink is indistinguishable from a link in the log.  **Three other things from building this that the next person should not re-derive.**  Helpers are free functions rather than inherent methods on `SqliteStore`. Four modules — `entity`, `graph`, `docs`, `search` — each add their own `impl SqliteStore` block, and two of them defining `count` or `query_one` is a compile error. Free functions taking `&SqliteStore` are what let four files be written in parallel without colliding.  `v_entities` spells "no project" differently in the two engines. DuckDB yields NULL for a global term; the SQLite view yields `''`, mirroring the `COALESCE(project_id, '')` unique index. A direct port would have called `EntityId::parse("")` and failed every note against a global term.  Optimistic concurrency is enforced by the `UPDATE`'s own `WHERE version = ?` and an assertion on the changed-row count, then a re-read to tell a stale version from a missing row. Checking the version in Rust and then writing unconditionally leaves the exact race REQ-7 exists to close. <sub>— claude · 2026-08-11</sub>

- **KEEL-123** 9A — A performance budget with numbers, and the stall that has never been measured `p1` · phase9, 9a, daemon, desktop, testing

  The measurement is on the Phase 9 milestone as a note. The short version: steady-state reads are fine — tasks 7 ms, notes 3 ms, search 13 ms, digest 51 ms over 53 rounds. The stall is the first read after a write, caught live at 217 ms against a 13 ms mean at the exact second this session wrote a decision.
  
  The cause is `refresh_entity_index` in `store/docs.rs`. It rebuilds the entire BM25 index on the first search after any write, because DuckDB's full-text index does not update when its table changes. FTS5 does, so the swap removes this rather than optimising it — which is the argument for measuring before rather than fixing here.
  
  What this task delivers:
  
  **A budget, written down, with the numbers behind it.** What a board load may cost, what a search may cost, what the first read after a write may cost. Numbers taken on the current store so the same script can be re-run after the swap and the two compared.
  
  **A test that fails when it regresses.** Not a benchmark suite. One test asserting the reads a board makes stay under the budget, so a change that makes them ten times slower is a red build rather than something noticed months later.
  
  **The two cheap wins that have nothing to do with the engine.** The board waits on `/api/context?depth=full` — 56 KB and 128 ms in the browser — and reads one field out of it. `/api/ready` returns the same ranking in 17 ms and 716 bytes. And the board fetches 164 KB of tasks and 153 KB of notes to render 122 cards.
  
  Deliberately not on this list: the double-fetch seen in the browser. That is React StrictMode double-invoking effects in dev and it does not happen in a production build. It is written down on the milestone note so nobody else spends an hour rediscovering it.

  - The budget, with the numbers behind it. `scripts/measure-performance.sh` produces this table and takes `--base`, `--project`, `--rounds` and `--label`, so the run after the SQLite swap sits next to this one. It reports mean and max over N rounds and discards a warm-up round, because the first read after a write pays for the whole BM25 rebuild and attributing that to whichever read happened to be first would make the table depend on the order of the loop.  Two runs, and they answer different questions.  **The live daemon** — release build, live store, 122 tasks, other sessions using it at the time. This is the scale a person actually sees.  ``` tasks               7.8ms   186.3KB milestones          3.6ms     6.8KB every note body    14.4ms   163.1KB digest depth=full 154.6ms    27.4KB ready              58.8ms     1.1KB search              8.9ms     8.6KB ```  **A rebuilt daemon on a copy of that store** — debug build, so every number is several times the release one, but all of them are on the same build and the same data, which is what makes the comparison mean something. Twenty rounds.  ``` board   tasks               31.2ms   186.3KB board   milestones          19.8ms     6.8KB board   note counts         14.2ms     3.9KB board   ranking + blocked  508.0ms     1.2KB was     digest depth=full  999.0ms    27.4KB was     every note body     19.8ms   163.1KB other   search              45.9ms     8.6KB ```  So the board load is 573 ms serial and 198 KB, against 1019 ms and 190 KB for the two calls it no longer makes. Payload across the whole load: 384 KB → 198 KB.  The two changes separately. `/api/ready?blocked=true` is half the time of the digest it replaces and a twenty-third of the bytes. `/api/notes?counts=true` is 3.9 KB against 163.1 KB — the same read against the store with the bodies left behind, which is the part the browser was waiting on rather than the query.  One cost I introduced and did not hide: asking `/api/ready` for the blocked set walks the `blocks` edges a second time, because the ranking inside the tool has already walked them and thrown that half away. 364 ms without, 508 ms with, on the same build. Worth it against 999 ms and 27 KB, and the cheaper version means either the daemon stops going through the tool — so the app's ranking stops being the tool's by construction — or `keel_ready` grows a stuck list no model asked for. If it ever is worth it, the fix is a `blocked` field on `keel_core::Ready` carrying what the ranking already computed. <sub>— claude · 2026-08-11</sub>
  - Two findings that were not in the brief and are worth the next session having.  The board was never ranking more than three tasks. `/api/context` builds `next_up` from `keel_core::next::rank` and then takes the first three — the cap is in `impl From<NextUp> for NextUpJson` in `keel-mcp/src/context.rs`, with a comment saying a ranked list of thirty is the same "you work it out" the counts were. So the board's rank map, which feeds the default `sort=next`, has only ever had three entries and everything else fell through to priority. Switching to `/api/ready?limit=3` keeps that exactly, and it is now written where a reader can see it rather than inherited from a truncation two crates away.  The digest's `depth` does almost nothing for cost. `next_up`, the glossary and the questions are built whatever the depth is; `depth` only trims `recent`, `specs`, `decisions` and `attention` after the fact, and only when the rendered prose is over budget. `depth=brief` measured 12 KB against `full`'s 27 KB and was no faster at all. So "ask for a smaller digest" was never the fix — the fix was to stop asking for a digest.  Also, on what I did not change: `/api/notes?counts=true` counts in the daemon after the same `notes_in_project` read, so the saving is the transfer and the parse, not the query. Doing it as `SELECT entity_id, count(*)` would need a new method on `DocumentStore`, and Phase 9 is rewriting that layer this week — a method added now is a method someone reimplements on SQLite in three days for a saving of about five milliseconds. <sub>— claude · 2026-08-11</sub>

- **KEEL-122** CLAUDE.md tells every session to use a task status that does not exist `p2` · docs, phase8

  `product/CLAUDE.md`, under Tracker discipline: "Never delete a task. Mark it `dropped` with a reason."
  
  There is no `dropped` status and there never has been. The enum is `todo`, `in_progress`, `review`, `done`, `wont_do` (crates/keel-core/src/enums.rs:136). A session following that instruction gets an enum rejection listing five values, none of them the word it was told to use.
  
  The intended value is `wont_do`. This is a one-word correction to a line sitting in the contract loaded into every session, where it has been failing silently for as long as it has been written down.
  
  Found while answering TQ-32, and independent of it — the triage decision does not change this either way.
  
  Done when the line names a status that exists.

- **KEEL-121** 8F — Store the project's own noun, and read the glossary before the built-in list `p3` · mcp, storage, phase8, 8f

  Split out of KEEL-116, which delivered the alias layer and its exit criterion. Two of 8F's three layers are not built.
  
  **The display noun.** A `milestone_noun` column on projects, set to "Phase" here, used by the board, `render-status` and the digest — so the interface says the project's word rather than Keel's. Today the alias is reported back on a create but nothing stores it, so the app still says "milestone" everywhere and a session learns the vocabulary one create at a time instead of being told once.
  
  **The glossary as the alias source.** `term` is already one of the thirteen types and glossary terms are the one thing `keel_context` never truncates, precisely so a session uses the project's words. The alias table should consult the glossary before falling back to the built-in list, and creating a project should seed its milestone noun as a term. That generalises past milestones for free: a project that says "customer feedback" or "incident" gets the same treatment without a code change.
  
  Deferred rather than rushed because it is a migration plus four read sites, and the exit criterion was already met without it.
  
  Done when the board and `render-status` say "Phase" because the project says so, and a term defined in the glossary is accepted as a type alias.

  - Claimed and released without building it. What I found while scoping it changes the task, so it is worth recording before someone starts.  **The two layers are not the same size, and the second is not the "for free" the task claims.**  The display noun is straightforward: a `milestone_noun` column on projects, a migration, and four read sites — the board, the roadmap, `render-status` and the digest. Nothing surprising.  The glossary layer has a hole in it. "Have the alias table consult the glossary before falling back to the built-in list" needs a glossary term to *declare* which type it means, and a term cannot: it has `term`, `definition` and `aliases`, and none of them names a type. So there are only two ways to do it, and both are bigger than the task suggests:  - **Parse the definition.** A term whose definition mentions exactly one of the thirteen type names resolves to that type. That is prose parsing in a validation path, and it would make "a phase is a milestone with a demo at the end" and "a phase is not a milestone" resolve identically. - **Add a column** — `means VARCHAR` on terms — so a term can say it is a spelling of a type. That is a storage-format change, which needs KB, and it is the honest version.  **The narrow version that needs neither**: the project's own `milestone_noun` is consulted as an alias for `milestone` before the built-in list. That delivers this project's case, which is the case the whole of 8F was written for, and it does not generalise past milestones. If generalising matters, it is the `means` column and it should be asked for rather than smuggled in.  **Also worth knowing:** `parse_with_alias` is a pure function on `EntityType` with no store access, so any store-backed resolution is a new function beside it rather than a change to it. The tool description's enum list is generated from `EntityType::wire_names()`, so a project-specific alias will never appear in the schema a model reads — the narration on the create response is the only way a session learns the word, which is what KEEL-116 already established.  Nothing was written. The claim is released so the row does not read as work in progress. <sub>— claude · 2026-08-11</sub>
  - Done, both layers, including the one I had scoped out and flagged as needing KB. Asking to finish it was the answer to that, so `Term::means` is a column now — B-50 carries the argument.  **Verified end to end on a scratch store, not only in tests.** A project created with `milestone_noun: "Cycle"` stored the noun and seeded the term in one call; `keel_create(type: "cycle")` came back with *"You said 'cycle' — in Keel that is a milestone, because this project's glossary says so"*. Then a term "Incident" declaring `means: task` with an alias "outage", and `keel_create(type: "outage")` created a task. That last one is the whole point: "outage" is a word Keel has never heard of and will never need to.  **Keel's own project says "Phase" now.** The tracker writes "Active phase", "## Phases" and a "Phase" column header; the board's filter menu reads "Phase" and "Any phase"; the digest's first paragraph says "active phase: Phase 8 — The working loop".  **What I got wrong when scoping this, and it is worth having on the record.** I wrote that the narrow version — the project's noun consulted directly — would deliver this project's case and not generalise, and that generalising meant the `means` column. Both true. What I did not see is that the two are not alternatives: the noun is what the *interface* says and `means` is what the *input* accepts, and a project can legitimately have one without the other. So both are consulted, in that order, and the doc comment says why rather than leaving it looking like belt and braces.  **The ceiling is the thing to watch here.** This is the first feature where a *stored row* introduces vocabulary — written by whoever uses Keel rather than by whoever reviews a change. `means` is an `EntityType`, so a fourteenth type is unrepresentable, and a test walks every word the glossary knows to assert it.  **Two refusals worth having:** - A term named after a canonical type cannot redirect it. `keel_create(type: "task")` means a task in every project forever, and the canonical name is checked first. - A `milestone_noun` that is another type's name is refused where it is set. Allowing it would make every `keel_create` naming that type ambiguous, and the resolution order hides the problem rather than surfacing it — the canonical name wins, so the noun would silently do nothing.  **One ordering change in `keel_create` worth knowing about:** the project is now resolved before the type. That looks wrong and is necessary — a project's glossary is what decides whether "outage" means anything, so the type cannot be resolved without knowing who is asking.  **Seeding happens on update as well as create.** Only on create would have meant an existing project could set the noun, watch the interface change, and never see the word in the digest — which is the one place a session reads the vocabulary before it needs it.  18 tests across `vocabulary.rs` and its integration file; four snapshots reviewed and all four diffs were the new nullable fields and nothing else. <sub>— claude · 2026-08-11</sub>

- **KEEL-120** Plain English on every prose field, not just milestone summaries `p1` · mcp, storage, phase8

  B-46, the plain-English house style, extends the milestone rule to decision bodies, question bodies, specs, feedback, notes, task bodies and titles.
  
  Three layers, because only one of them is a validator:
  
  1. Every prose-bearing tool description says what good looks like, with a worked good/bad pair. This is the layer that changes what gets written.
  2. Rejection for what is objectively wrong: empty, a body that only restates its title, a bare TQ-15 or KEEL-96 with no gloss, and a short list of phrases with no legitimate use in a tracker.
  3. Warning for the softer tells, returned with a successful write rather than blocking it.
  
  Fenced code, inline code and block quotes are stripped first. Quoting an error message or what someone actually said is carrying someone else's words, and refusing those would stop the store recording the world as it is.
  
  One `style` module in keel-core so the CLI, MCP and import cannot diverge.
  
  Done when a decision body that restates its title is refused, a note quoting an error message is not, and the tool descriptions carry the register.

  - Done. `keel-core/src/style.rs` is the one place that decides what prose may say, and it is wired into the authoring paths: `keel_create` bodies, `keel_write_doc`, and `Note::validate` — the last of which covers CLI and MCP notes together.  **The exemption is what makes the rule usable.** Fenced code, inline code and block quotes are stripped before any check. Without that, a note carrying an error message becomes unwritable and the store loses the ability to record what actually happened. Three tests hold it, including a decision body quoting a driver's own wording at it.  **Refuse versus warn, and why the line sits where it does.** Thirteen phrases are refused because they have no legitimate use in a tracker. Seven softer tells warn instead and ride back with the write that landed, in the summary and in `style_warnings`. The reasoning is that refusing on a signal teaches the wrong lesson: a model refused for something it does not accept swaps the word and keeps the shape, so the prose ends up both bad and compliant while the check reports success.  **Restatement reuses containment rather than similarity** — the KEEL-65 rule pointed at a different pair of strings. One token set must be a subset of the other, so the difference can only be added words. "Fix the board filter so it survives a reload" passes against the title "Fix the board filter"; a naive overlap score would have refused it.  **`keel import` is deliberately exempt.** It writes revisions too, but it is a person migrating text they did not author — the same carve-out the mirror rule already makes. So the check sits at the two MCP call sites rather than inside `write_revision`. The cost is that the CLI and MCP are not literally identical here; the alternative was that importing any legacy file with "robust" in it fails, which would have made the three phase specs unimportable.  **Both snapshots caught it, which is the point of having them.** `tools_list` for the three descriptions, `keel_create` for the new `style_warnings` field. Reviewed both diffs before accepting: only the intended changes.  Worth saying plainly, and it is in B-46 too: this catches structure. It cannot tell a limp sentence from a sharp one, and the layer that actually changes what gets written is the tool descriptions, not this module.  448 tests, clippy clean, fmt clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-119** A killed test run leaks its temp stores into TMPDIR `p3` · testing, tooling

  Found while clearing a full disk on 2026-08-11: 1,649 directories under `$TMPDIR` matching the `tempfile` pattern, each holding a 4.8 MB `keel.duckdb` plus a `lance` directory, totalling 7.7 GB. Dated from 2026-08-10 onward.
  
  `tempfile::TempDir` deletes on Drop, so this is every run where Drop did not happen — a killed cargo process, a timeout, an aborted test binary. Several today were mine. The store is large enough per instance that the leak is measured in gigabytes rather than megabytes, and nothing sweeps it.
  
  The disk reached 100% and it did not present as a disk problem: the linker failed with `errno=28` and it surfaced as 13 failing daemon tests. That is the expensive part — an out-of-space condition looked like a code regression.
  
  Options worth weighing: have the test helper register a sweep of stale `keel.duckdb` temp dirs on start; use a single shared temp root under `target/` that `cargo clean` removes; or shrink the fixture so a leaked store costs kilobytes.
  
  Done when a killed test run does not leave a multi-megabyte store behind, or something removes stale ones automatically.
  
  Not urgent, but it will recur — it took a session from "tests fail" to "disk is full" once already.

  - Not fixed, and swept by hand again — 315 leaked stores found, 255 of them older than an hour, 1.38 GB freed. Second sweep in two days, which is the argument for doing something rather than tolerating it.  **Preparatory work, so the next session does not re-derive it.** Three approaches, and the two obvious ones both have a catch.  **`[env] TMPDIR` in `.cargo/config.toml`, pointed at `target/tmp`.** One line, applies to every test binary with no code change, and `cargo clean` removes the lot. The catch: `tempfile` requires the directory to exist already, and `[env]` cannot create it. Nothing in the workspace runs before the test binaries — there is no build script — so there is no hook that could.  **A shared `test_store()` helper.** The natural home is keel-core, and it cannot go there: finding `target/` means reading `CARGO_MANIFEST_DIR`, and the standing rule is that keel-core never reads an environment variable. That rule is what keeps the CLI, the daemon and any future surface cheap, and it is not worth bending for a test helper. A dev-only crate would work, and adds a workspace member for one function.  **Shrink the fixture** so a leaked store costs kilobytes. Does not stop the leak, only makes it not matter. Twenty-five test files call `tempfile::tempdir()` and every one opens a DuckDB store; the 4.8 MB is DuckDB's own initial file rather than test data, so shrinking probably means not opening a store in the tests that do not need one.  **Why this is worth doing rather than tolerating.** When the disk filled on 2026-08-10 it did not present as a disk problem: the linker failed with `errno=28` and it surfaced as 13 failing daemon tests. That is the expensive part — an out-of-space condition that reads as a code regression.  The sweep itself is four lines: every `$TMPDIR/.tmp*` directory containing a `keel.duckdb`, older than an hour so nothing a running binary owns is touched. `scripts/sweep-test-stores.sh` is the cheapest thing that would have saved both of those hours. <sub>— claude · 2026-08-11</sub>
  - The disk filled again mid-session and this time it was measured rather than swept. The leaked test stores are the small half of the problem. KB's question — how it can refill after clearing 15 GB with nothing running but Claude — has a specific answer.  **Cargo never garbage-collects.** Every fingerprint change produces a new artifact under a new hash and leaves the previous one on disk forever. Normally a rounding error. Not here, because every test binary statically links DuckDB: `libduckdb.a` is 237 MB, each linked test binary is 100–165 MB, and there are about thirty of them. One full rebuild after a fingerprint change is roughly 8.5 GB of test binaries, and the previous 8.5 GB is still there.  What was actually on disk, counted:  - `target/` at 17 GB. `debug/deps` 12 GB, of which **8.5 GB was 88 linked test binaries**; `debug/incremental` 4 GB. - **Nine generations of `keel_daemon`, five of `keel`, five of `concurrency`, four of `keel_core`.** Three copies of `use_cases` at 165 MB each. - **Four `libduckdb-sys` build directories**, each with its own 237 MB `libduckdb.a`, plus two 238 MB `liblibduckdb_sys` rlibs. - 60,410 loose `.rcgu.o` split-debuginfo files — enormous in file count, only 0.2 GB, so not the problem despite looking like it. - 135 leaked test stores in `TMPDIR`, 813 MB. The KEEL-119 half.  **What triggered this particular round: I did.** Adding `rusqlite` and `sqlite-vec` and changing `unsafe_code` from `forbid` to `deny` are both fingerprint changes at the root of the graph, so every crate rebuilt under new hashes and the entire previous generation stayed. That is the mechanism by which 15 GB comes back after being cleared — not a leak so much as an archive nobody empties.  **How it presented, which is the expensive part.** `rustc-LLVM ERROR: IO failure on output stream: No space left on device`, and before that a bare `linking with cc failed: exit status: 1` across five targets. It reads as a compiler bug or a code regression. It is a full disk. This is the second time that has cost a session — the first is in this task's original body.  Swept to 7.9 GB with all 490 tests still passing, and the sweep is now `scripts/sweep-build-artifacts.sh` rather than a thing done by hand twice. It keeps the newest generation of every artifact and drops the rest, which is safe because Cargo rebuilds whatever it finds missing. `DRY_RUN=1` shows what it would take.  **Phase 9 is the actual fix and this is now the strongest argument for it.** SQLite's amalgamation is about 1.5 MB of object code against DuckDB's 237 MB. The same thirty test binaries cost megabytes rather than gigabytes, stale generations stop mattering, and the fixture a killed test leaks stops being worth a script.  One thing to be honest about in the first sweep: the age filter silently failed — `find -newermt` is not supported by the `bfs` on this machine and errored to stderr, so the guard read as "no output, therefore old" and all 135 stores were removed rather than only those over an hour. No test was running, so nothing was taken from under a live process. The script now uses `-mmin +60`, which `bfs` does support. <sub>— claude · 2026-08-11</sub>
  - Measured it rather than assuming, because the premise had changed under the task. A test binary that **finishes** leaks nothing — ran `documents` to completion, 23 passed, zero temp dirs left behind. `TempDir::drop` does its job. The leak is entirely from processes that get **killed**: I stopped one `cargo test --workspace` partway through and it left 27 stores, 10.8 MB, in one keystroke.  That explains the shape of the accumulation. The 2,318 stores sitting in TMPDIR today are not two days of steady drip — they are one `cargo mutants` run on 2026-08-12. 24 of its 113 mutants timed out, and each timeout kills a test binary that has already opened ninety-odd stores. 24 × ~90 is the number on disk.  Phase 9 also shrank the blast radius without anyone aiming at it: a leaked store was 4.8 MB of DuckDB plus a `lance` directory and is now a 388 KB `keel.sqlite`. Thirteen times smaller. 2,318 of them come to 1.1 GB, where the same count of DuckDB stores would have been 11 GB.  Worth knowing for KEEL-173: the mutant timeouts are the thing that multiplies this, so fixing the 30-second daemon-start wait takes the leak with it. <sub>— claude · 2026-08-13</sub>
  - The sweeper written for this task has never worked anywhere but this laptop, and said nothing about it.  `scripts/sweep-build-artifacts.sh` §4 globbed `"$TMP".tmp*` with `TMP="${TMPDIR:-/tmp}"`. macOS sets TMPDIR with a trailing slash, so that expands to `…/T/.tmp*` and matches. Strip the slash — which is how Linux spells it, and what the `/tmp` fallback gives you — and it expands to `/tmp.tmp*`, matches nothing, and the loop body never runs. Measured both ways on the same 2,382 directories: with the slash, 2,382; without, **0**.  It reported nothing either, because the summary line was guarded by `[ "$leaked" -gt 0 ]`. A sweeper that swept zero looked exactly like a sweeper that found nothing to sweep. That is the same failure the graph-direction rule is about — an empty result that is indistinguishable from a legitimate one.  Fixed by stripping the trailing slash and using an explicit `/` in the glob, so it no longer depends on how TMPDIR is spelled, and by printing the count unconditionally. It also now matches `keel.duckdb` again, since 47 pre-Phase-9 stores are still on disk at 4.8 MB each and the old sweeper could never have reached them. <sub>— claude · 2026-08-13</sub>

- **KEEL-118** 8E — Copy-ready prompts on the task page `p3` · desktop, phase8, 8e

  The app cannot write, so every task page carries a few prompts you can copy and paste into Claude Code: "close KEEL-42 as done with the commit", "what is blocking KEEL-42", "split KEEL-42 into sub-tasks". One click copies.
  
  This is what makes read-only feel deliberate rather than inert, and it is nearly free now the detail view exists.
  
  Done when each task page offers at least three prompts with its own identifier filled in, and clicking one copies it.

  - Done. Four prompts on every task page, each already addressed to that task: close it with the commit, what is blocking it, split it into sub-tasks, add a note.  Copy rather than deep-link, deliberately. There is no URL that puts text into a Claude Code session, and a button that behaved as though there were would be worse than one honest about the clipboard.  A denied clipboard is swallowed rather than surfaced. This is a convenience on a read-only page and the text is on screen either way; an error state would be louder than the thing it is reporting.  One test-writing note: the prompt is a bare text node beside the "copy" affordance, so `getByText` cannot find it as an element. The assertions match the button's accessible name instead, and the clipboard test asserts the shape `what is blocking <ref>` rather than a hardcoded identifier — a prompt addressed to no task is the failure worth catching, not a particular number.  191 desktop tests, typecheck clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-117** 8E — Rate limiting on /mcp `p2` · daemon, mcp, phase8, 8e

  There is none. No `governor` or equivalent anywhere in the workspace, confirmed. Cheap insurance against an agent stuck in a loop, and the MCP specification lists it under what servers must do.
  
  Keep it small. This is one user and one daemon, so a simple per-connection limit is enough and a measurement would have to justify anything more.
  
  Done when a client exceeding the limit gets a proper rate-limit response rather than being served, and a test drives it over the threshold.

  - Done. A token bucket on `/mcp`, checked before the Origin check and before the store is touched — a call that will not be served should cost as little as possible.  **Hand-written, no `governor`.** One process, one user, loopback: no distributed coordination, no per-tenant accounting, no clock skew. The whole problem is "how many calls in the last second", which is a counter and an instant. The standing rule is to leave the machinery out until a measurement asks for it.  **One bucket, not one per client.** Per-IP buckets on a loopback socket all key to `127.0.0.1`, so they would be one bucket in a disguise. What is being protected is the store's single write lock, which is global.  **Tokens rather than a fixed window,** because a fixed window lets a caller spend a whole allowance in the last millisecond of one window and the whole of the next in the first — twice the intended rate, at exactly the moment the limiter should be working.  **The first numbers were wrong, and a test found it.** I set burst 60 / 10 per second. `the_digest_never_truncates_questions_or_terms` creates sixty questions in a loop and started failing. That is not a test artefact — it is what planning a phase looks like, and this project's own Phase 8 went in as roughly fifty creates and links back to back. The limit was sitting on top of legitimate work.  So the numbers moved to 300 burst / 50 per second, and the reasoning is written into the constants: this is a backstop against a *pathological* loop, not a fairness mechanism. A retrying model does thousands per second; real bulk work does hundreds, slowly. At 1–10ms per write, 50/s leaves the write lock mostly free, which is what keeps the human in front of the app served while a runaway session is throttled.  **A poisoned lock fails open.** This is a guard rail, not an authorisation check: failing open costs one unmetered request, failing closed turns a panic elsewhere into a daemon that answers nothing.  The 429 carries `Retry-After` and a message telling the caller to read the error rather than resend it, since resending is what got it here.  454 workspace tests, clippy clean, fmt clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-116** 8F — Let a project use its own word for a milestone `p2` · mcp, storage, phase8, 8f

  This project says "phase" on every screen. `keel_create(type: "phase")` fails with an enum error listing thirteen types, none of which is the word.
  
  Three layers, and none of them adds a type.
  
  A display noun per project: the project records `milestone_noun: "Phase"`, and the interface, the renderer and the digest all use it. The board says Phase 8, `render-status` writes Phases, and `keel_context` tells the session in its first paragraph that this project calls milestones phases.
  
  Aliases on input, applied before validation. phase, epic, sprint, cycle, iteration, release and version resolve to milestone. issue, ticket, story, bug, chore and defect to task. adr, rfc and choice to decision. risk, unknown and open question to question. requirement, design doc and prd to spec.
  
  The resolution is reported, not silent: the response says `created milestone "Phase 9" — this project calls those phases`. A silent success teaches the session nothing and it guesses the same way next time. A narrated one teaches the vocabulary in one round trip.
  
  The rule that keeps this safe: an alias is a spelling, not a concept. It must never create a fourteenth type. "Sprint isn't quite a milestone" is how a schema grows a type it cannot remove.
  
  Third layer: the glossary already does most of this and nobody wired it up. `term` is one of the thirteen types, and glossary terms are the one thing `keel_context` never truncates, for exactly this reason. Seed the milestone noun as a term when a project is created, and have the alias table consult the glossary before the built-in list. That generalises past milestones for free.
  
  Done when `keel_create(type: "phase")` succeeds and says what this project calls those. Phase 8 exit criterion.

  - Exit criterion met, verified against the live daemon: `keel_create(type: "phase")` created Phase 9 as a milestone and came back with `resolved_from: "phase"`.  **The alias layer is done. The other two are split out as KEEL-121** — the stored `milestone_noun` and reading the glossary before the built-in list. Deferred rather than rushed: both are a migration plus four read sites, and the criterion was already met without them. Recording that honestly rather than marking three layers done because one of them shipped.  **`parse_with_alias` returns the alias, and that is the design.** Resolution is narrated, not silent: the response says "You said 'sprint' — in Keel that is a milestone." A silent success teaches the session nothing and it guesses the same way next time; one round trip teaches the vocabulary.  **Two tests exist to stop this becoming a fourteenth type**, which is the real risk the spec names. One asserts every alias resolves to a type already in `ALL` and that no alias shadows a canonical name; the other that no word is listed twice, since a duplicate means whichever comes first silently wins and the loser is invisible. "A sprint isn't quite a milestone" is exactly how a schema acquires a type it can never remove.  **`bug` and `chore` resolve to `task`, deliberately**, even though they are also task *kinds*. A caller writing `type: "bug"` wants a task; the kind is a separate argument they can still set. Resolving to the type is the reading that is never wrong.  The snapshot caught `resolved_from` landing in the create response — reviewed and accepted, it is that one field and nothing else.  463 workspace tests, clippy clean, fmt clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-115** 8B — keel_attach: let the daemon read the file, and tell the truth about the base64 cap `p2` · mcp, daemon, phase8, 8b

  Two things, both about the fact that the shipped image path describes a ceiling nobody can reach.
  
  Base64 inflates by a third, and the model has to emit every one of those characters as tool arguments. A 100 KB image is 35,000 to 45,000 tokens. A 1 MB image, which is the declared cap, is 350,000 to 450,000. A retina screenshot is 300 KB to 2 MB. So the stated ceiling is about ten times the useful one, and the description is a trap: it says 1 MB and means roughly 100 KB.
  
  Fix the description to state the real ceiling and point at the alternative. That needs no decision.
  
  Then add `keel_attach(id, path)` so the daemon reads a local file itself. Same machine, so bytes never enter the model's context. This is what makes a real screenshot possible from Claude Code.
  
  Not the URL fetch. TQ-6 already settled that, this morning, and the answer was no: it would make the daemon issue outbound requests on a model's instruction, which is more capability than this needs. The spec lists it as an open decision but it is closed. TQ-33 records that closure, so the spec's list of open decisions is the stale half of the disagreement.
  
  MCP offers no better route. Tool inputs are JSON only, there is no binary input type, and the proposal to add one is still an open pull request.
  
  Done when a file on disk can be attached without its bytes passing through a model, and the base64 description states a ceiling that is actually reachable.

  - Done, both halves, and the second one arrived as a field rather than as the tool TQ-33 named. The argument is in B-49: TQ-33 approved the capability and TQ-31 set thirteen tools as the ceiling, both KB's, and the form of a capability is the reversible half. `image_path` sits beside `image` on `keel_create`, and in `keel_update`'s `changes` for attaching to something that already exists.  **Verified against a live daemon on a scratch store**, not only in tests: a 683 KB PNG went from a path on disk into the store and came back out of `/api/blob/{id}` as `image/png`, 683,411 bytes, byte-identical. Through base64 that same file would have cost the model roughly 240,000 output tokens.  **The description was the actual bug and it needed no decision.** It advertised 1 MB, which is 350,000 to 450,000 output tokens — about ten times anything reachable. It now states 100 KB as the practical ceiling, says why the cost falls on the caller, and points at `image_path`.  **Three refusals worth having, each with a test:** - Anything URL-shaped, with the reason in the message. TQ-33 predicted this failure mode in as many words: a path argument that quietly starts accepting a URL is that decision reversed by accident. Covered for `https:`, `http:` and `file:`. - A relative path, because the daemon's working directory is its own and a relative path resolves against something the caller cannot see. - A file whose bytes are not an image, even when it is called `.png`. The extension is whatever somebody typed; the magic bytes are what the app will try to render.  **Both arguments at once is refused rather than resolved.** Silently preferring one would mean a caller who sent both got the file sometimes and the payload other times, on an ordering nothing documents.  **Attaching to an existing row does not bump the version twice.** An update whose only instruction was `image_path` has nothing to change on the row, and the store rejects an empty change set — so that case reads the row, writes the blob, and repoints in one update. Getting this wrong would have been a version bump with no image behind it.  The 10 MB ceiling on the file path is ten times the base64 one, and the asymmetry is the point: nothing here enters model context, so the constraint stops being the context window and becomes only "is this a picture or a mistake". <sub>— claude · 2026-08-11</sub>

- **KEEL-113** 8G — keel lint: report the rows that are already unreadable `p2` · cli, phase8, 8g

  Around ninety-four task rows exist without a summary and something has to look at them once. `keel lint` reports and never rewrites.
  
  Not rewriting is the point. A machine inventing a summary would produce exactly the confident, plausible, wrong prose the requirement exists to prevent. Same reasoning that stops the mirror ever reading a file back.
  
  What it reports: missing summaries, and bare identifiers with no gloss beside them. The `unresolved_id_reference` parser in fsck already recognises the TQ-n, B-n, REQ-n and KEEL-n families, so that half is reuse rather than new code.
  
  Done when `keel lint` reports zero unexpanded identifiers across the rows that already exist. That is a Phase 8 exit criterion.
  
  Depends on the summary column landing first.

  - Done, and the exit criterion is met: `keel lint keel --check unexpanded_identifier` reports zero. It reported nine when the check first ran, and those nine bodies were glossed by hand — the sentences were things like "Waiting on TQ-35." and "Decision B-45.", which name a thing and say nothing about it.  **Three rules, not one.** Missing summaries (122 rows), unexpanded identifiers (0 now), and — added because it is the same shape of problem — closes with no reason, which is the 109 rows that predate B-47. All three are enforced on the way in and none can be enforced backwards, which is exactly what a lint is for.  **The identifier check is a word count, and the doc comment says so.** An identifier in a sentence with fewer than six other content words is nearly always leaned on rather than explained. There is no exact test for "did this sentence tell me what TQ-35 is", and pretending otherwise would be worse than admitting the heuristic. It is safe *because* this only reports: a false positive costs a glance, not a paragraph. Backticked identifiers are exempt, so writing about the check does not fail it.  **A bug caught by writing the test after the code.** `by_check` derived its per-rule counts from `self.findings`, which is the *truncated* list — so `--limit 12` printed "12 task_without_summary" under a total of 240. The counts are now computed before the cut and stored. That number is what a person reads to decide what to work on, so it has to describe the project rather than the page.  **Not an MCP tool, deliberately.** Thirteen is the ceiling, and a model handed ninety rows to improve would improve them by inventing prose — the precise failure the summary rule exists to prevent. It is a CLI command reading `/api/lint` through the daemon, for the reason `fsck` does: a report you have to stop the daemon to run is one nobody runs.  **Exits zero.** These rows predate the rules that would have refused them, so a non-zero exit would fail every build until someone had worked through 122 summaries by hand.  **One thing this session's own work exposed.** Fixing the nine glosses turned 240 findings into 231. The summaries are the long tail and they are not this task's job — a machine writing them is the thing being avoided.  Also fixed while here: `a_client_in_a_loop_is_rate_limited` had started failing, and it was the test rather than the limiter. It hammered `tools/list` 1000 times sequentially and relied on outrunning a 50-per-second refill, which needs every call under 20ms — and the response had just grown from ten tool definitions to thirteen. It now fires `ping` in concurrent batches, so the assertion no longer depends on how big some other endpoint's response happens to be. <sub>— claude · 2026-08-11</sub>

- **KEEL-112** 8G — A task cannot be created without a summary `p1` · storage, mcp, phase8, 8g

  One or two plain sentences a colleague could read cold, six weeks from now, without having been in the conversation. What is wrong or wanted, who or what it affects, and what done looks like.
  
  Checked: `Task` (types.rs:354) has `title` and `body` and no `summary`. `Milestone` has one, so the name and its serialisation already have a precedent.
  
  Three parts:
  - A new nullable column on tasks. Nullable because roughly ninety-four rows already exist without one and cannot be made invalid retroactively.
  - `summary` required on `keel_create` when the type is task. This is the mechanism: a model physically cannot complete the call without confronting it, on every surface, whether or not any skill loaded. The description carries the contract and one worked good/bad pair.
  - Synchronous rejection in the storage layer, so the CLI and MCP cannot diverge and the row never exists in a bad state.
  
  The evidence that a rejection works: two gate sessions sent `priority: "high"` against an enum of p0 to p3 and both retried successfully in the same turn. A rejection costs one round trip, not a human.
  
  Also a rendering rule that keeps the requirement felt: lists show the summary, never the body. A task with no summary shows a visible gap rather than falling back to the first line of the body. A silent fallback is how a requirement quietly stops being one.
  
  Waiting on TQ-34 for how much validation sits behind the required field.
  
  Done when no task can be created without a summary, proved by a test asserting both the CLI and MCP paths refuse.

  - Done. A task cannot be created without a summary, on either surface. Migration 14 adds the column, `Task::validate_summary` refuses empty and title-restating, and the tool description carries the contract with a worked good/bad pair.  **Two checks and only two**, per KB's call in TQ-34: empty, and a summary that only reorders its own title. The restatement check reuses the containment rule from KEEL-65 — one token set must be a subset of the other, so added words pass and "Fix the board filter so it survives a reload" is accepted against the title "Fix the board filter".  **Validated on create and deliberately not on update.** Ninety-four rows predate the rule. Running it on every write would mean none of them could ever be touched again — moving one to `done` would be refused for a summary nobody was being asked to write. Freezing a third of the tracker is worse than the hole this leaves, which is that an update can still blank a summary that was required at creation. `keel lint` is what reports whatever falls through. A test asserts a summary-less task stays closable.  **I did this the slow way first, and the lesson is worth more than the change.** I put the check in the storage layer alone and left `Task::new` taking two arguments. So the missing summaries surfaced one failing test binary at a time — and `cargo test` stops at the first failure, so each round was a 3-4 minute build to discover another three files. Forty-nine call sites across thirteen files arrived in about six rounds.  Making `summary` a positional argument on `Task::new` — which is what `Milestone::new` already did for exactly this reason, decided in B-45 two hours earlier — turns that into one build error listing every site at once. The precedent was there and I did not apply it until the third round. **If a field is required, the constructor is where that belongs; a storage-layer check alone tells you where you were wrong one place at a time.**  **The bootstrap and the fixture use their existing body as the summary** rather than getting a second string beside it. Those rows carry real prose written when the work was planned, and two fields would be two answers to one question. Both still go through the create path, so a bad body is refused rather than smuggled past the rule.  **One test needed the bad state on purpose** and my bulk edit broke it — "refuses a task with no summary" got given a summary. It now clears the field explicitly, which is honest: with a positional argument, a task with no summary is a state you have to construct deliberately, and exactly one place in the codebase wants it.  470 workspace tests, clippy clean, fmt clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-110** 8A — Closing a task needs a reason, and done needs evidence `p1` · cli, mcp, storage, phase8, 8a

  Keel's definition of done is a seven-item checklist written as prose in product/CLAUDE.md that an agent is asked to honour. That is a convention. Making the evidence an argument of the transition, enforced in the storage layer so the CLI and MCP cannot diverge, makes it an invariant.
  
  Five reasons:
  - `done` — finished. Message and at least one piece of evidence required.
  - `wont_do` — deliberately not doing it. Message required.
  - `duplicate` — of another task. Writes a `duplicates` link.
  - `superseded` — by another task. Writes a `supersedes` link.
  - `no_change` — looked at it, nothing needed doing. Message required.
  
  Both relations already exist in `Relation` (link.rs), so the automatic links need no schema change.
  
  Evidence is typed and repeatable: `commit:<sha>`, `pr:<url>`, `test:<command>`, `doc:<entity-id>`, `url:<url>`, `image:<blob-id>`.
  
  Open question: `duplicate`, `superseded` and `no_change` are reasons, but only `done` and `wont_do` are terminal statuses. Where the reason is stored needs settling — a column on the task, or all three mapping onto `wont_do` with the reason recorded separately. Worth deciding when this is picked up.
  
  Waiting on TQ-31 for whether this is a tool or stays a CLI wrapper over `keel_update` with the enforcement in the storage layer.
  
  Done when a task cannot reach `done` without a reason, a message and evidence, with a test proving both the CLI and MCP paths refuse. That is a Phase 8 exit criterion.

  - Done, and the exit criterion holds on both surfaces: a task cannot reach `done` without a reason, a message and evidence, proved by a keel-core test and by a daemon test driving real HTTP.  **The open question in this task's body is settled: the reason is a column.** `close_reason` on the task carries all five values, and the status enum stays at five — `done` maps to Done, the other four to WontDo. Mapping all of `duplicate`, `superseded` and `no_change` onto `wont_do` with the reason recorded nowhere was the alternative, and it loses the only part that distinguishes them.  **The enforcement is in `DuckStore::update`, not in `work::close`.** That is what makes it an invariant rather than a second convention: a caller who reaches for `keel_update(status: done)` to get round the tool is refused by the same check. A daemon test drives exactly that path.  **Checked on the transition only, and this is the honest limit.** 107 tasks closed before any of this existed and carry none of it. Running the check on every write would freeze all of them — moving one's priority would be refused for a message nobody was being asked to write. Backfilling would mean inventing reasons for work nobody remembers, and a store that cannot tell an invented reason from a stated one is worse than one with visible holes. Same shape as the summary rule (TQ-34), same escape valve: `keel lint` reports what falls through.  **One hole left open deliberately.** `keel_create` with `status: done` still bypasses the rule, because `bootstrap` and `import` create already-closed rows and enforcing on create would break both. Creating a task already finished is a migration shape, not doing work — but it is a hole, and it belongs on `keel lint`'s list (KEEL-113).  **The message is a column, not a note.** A note cannot be checked: the storage layer has no way to know one was written in some other call. Notes stay the place for everything learned along the way, and `keel_close`'s response says so rather than assuming it.  **Evidence prefixes are validated, not just documented.** A bare sha in the list would make "what shipped this week, with the commits" silently wrong, which is the whole reason for typing it.  This close was itself made with `keel close KEEL-108 --reason done --evidence commit:…`, which is the first use in anger. <sub>— claude · 2026-08-11</sub>

- **KEEL-109** 8A — keel claim: taking a task, atomically `p1` · cli, mcp, storage, phase8, 8a

  "Move a task to in_progress before starting it" is an instruction in the contract that sessions have to remember. Across sixty-six tasks there were once zero transitions into that state, which is what the SessionStart hook now nags about.
  
  `keel claim KEEL-42` replaces the instruction with a mechanism. One call sets the status, records the claiming session and the time. Claiming something already claimed fails, unless the claim is stale (reuse the three-day threshold `fsck` already warns on) or `--force` is passed. Closing releases it.
  
  Waiting on TQ-31 for whether this is an MCP tool as well as a CLI command.
  
  Done when a second claim on a live claim is refused with the claiming session named, a stale claim succeeds, and a test covers both.

  - Done. `keel claim KEEL-42` and `keel_claim` both refuse a task another session holds and name the holder in the refusal.  **There is no lock, and that is the design.** Claiming goes through the ordinary optimistic-concurrency update with the version it read, so two sessions racing both read version 7, the first writes 8 and the second is rejected with a stale-version error carrying the current state. Adding a lock would have been a second answer to a question the store already answers, and it would have needed its own release path.  **A claim with no session is the one write Keel refuses outright.** Everywhere else an anonymous write is merely less traceable. Here the session *is* the content: a claim naming nobody hides the task from `ready --unclaimed` while telling no one who to ask about it. The CLI takes `--session` or `KEEL_SESSION` and says why when neither is set.  **Three days, reusing fsck's existing threshold** rather than choosing a second number. Two thresholds for "this session is probably gone" would eventually disagree, and the disagreement would show as work `fsck` calls abandoned and `keel claim` refuses to take.  **Releasing happens in the store's update path, not in `close`.** Any route into a terminal status clears the claim, so a plain `keel_update(status: done)` cannot leave a claim standing — otherwise `ready --unclaimed` would keep skipping work nobody is doing.  Re-claiming your own task writes nothing and returns the same version, so a retry costs nothing and leaves no second event. <sub>— claude · 2026-08-11</sub>

- **KEEL-108** 8A — keel ready: what can be worked on right now `p0` · cli, mcp, desktop, phase8, 8a

  Open, not blocked by anything live, parents excluded because their children are the real work. The ranking already exists in `next::rank` and is good: it orders by how many other tasks a task unblocks, so a p1 that releases three things beats a p0 that releases nothing.
  
  It has never had a front door. Today the only way to reach it is inside the roughly 3,500-token digest that `keel_context` returns, so asking "what should I do next" costs a full digest.
  
  Make it a CLI command, an MCP tool and a view in the app, all reading the same computation. Filters: `--unclaimed`, `--label`, `--no-label`, `--limit`, and `--milestone` so "what is next in Phase 8" is one flag.
  
  Waiting on TQ-31 for whether this is an MCP tool. It is a CLI command either way.
  
  Done when the same ranked list comes back from one command, one tool call and one click, and a test asserts the three agree.

  - Unblocked — TQ-31 settled as thirteen tools, so all three verbs get their own. Not started.  Two things the next session should know before picking this up, both learned the hard way today:  **The ranking already exists and is good.** `next::rank` in keel-core orders by how many other tasks a task unblocks. This work is a front door onto that computation, not a new computation. Three surfaces reading one function — CLI, tool, app view — and a test that asserts the three agree.  **If a required field is involved anywhere, put it in the constructor first.** KEEL-112 cost several hours because the check went into the storage layer while `Task::new` still took two arguments, so forty-nine call sites surfaced one failing test binary at a time across six rebuilds. `Milestone::new` had already established the pattern. The compiler finding every site at once is worth more than it looks.  Claiming and closing (KEEL-109, KEEL-110) touch the same storage path and are probably one sitting with this. <sub>— claude · 2026-08-11</sub>
  - Done, and the "three surfaces agree" claim is checked rather than asserted. A daemon test drives `keel_ready` over MCP and `/api/ready` over the local API and compares the references in order; the CLI reads the same endpoint, so there is one ranking with three doors. Verified against the live store too: `keel ready keel` and the app both said 11 ready in the same order, 9 under Phase 8.  **The filters live in `ReadyFilter`, in keel-core.** Putting them in the tool handler was the alternative and it would have meant the app filtering client-side against a list the daemon had already cut — so "unclaimed only" would have shown fewer than it should whenever the limit bit.  **Parent exclusion needed the whole task list, not the ranked one.** A parent whose children are all closed is still a parent, and its children may be absent from the ranking. Reading parenthood off the ranked list would have offered a parent as soon as its last child finished.  **Truncation is reported as a flag, not inferred.** `items.len() == limit` cannot tell ten-of-ten from ten-of-ninety, and hard constraint 4 is that a cut list says so.  Found while testing: **KEEL-111 has no milestone**, so it is in the unfiltered list and not in `--milestone "Phase 8"`. That is the filter being right and the row being unplaced — the gap C7 put on the board as "unplaced".  Not done, and left deliberately: `--label` and `--no-label` exist on the CLI and the tool but not as controls on the screen. The screen has unclaimed and milestone, which are the two that get asked; labels are a filter you reach for when you already know the label, which is a board question.  470 workspace tests, 200 desktop tests, clippy clean, fmt clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-107** C4 — Activity: make the rows go somewhere, then decide about grouping `p3` · desktop, phase8, 8c

  Activity is a reverse-chronological feed of up to 300 mutations, filterable by actor. Each row is a timestamp, an actor badge, a one-line summary and a session id, and none of them is a link. Confirmed: Activity.tsx has no anchor of any kind. Its own header says its job is "what did Claude do today", and it answers "what were the last 300 events".
  
  The spec offers a rework as "What changed", grouped by session, and a deletion, and calls it close. Waiting on TQ-35, which asks whether this screen is rebuilt as a session-grouped feed, fixed cheaply, or deleted.
  
  One thing found while checking, which the spec does not mention: TQ-29 established that notes leave no row in `events`. So a per-session change count built from events alone would miss the notes, which is the part most worth reading. Grouping by session means unioning events with notes, not just regrouping what is already fetched. That moves the cost up.
  
  The recommendation in TQ-35 is to make every row a link and add a time range first, which is cheap and removes the most-cited defect, and to decide about session grouping against a screen that works. Also move the "written outside a tracked session" treatment into the tooltip: that is a build-time concern showing as product copy, the same class KEEL-85 cleaned up.
  
  Blocked on TQ-35 until that choice between rebuilding, a cheap fix and deletion is made.

  - Done as the rebuild KB chose in TQ-35, not the cheaper fix I recommended. The screen is "What changed": seven sessions on this store, newest first, each expandable, and every one of the 56 rows inside the first session is a link.  **The union is the whole cost, and it is where the work went.** Notes leave no row in `events` (TQ-29), so a per-session account built from the feed would silently miss them — and this session's own account reads "created 3 things, 48 changes, wrote 5 notes", where the five notes are the part worth reading. `changes::by_session` unions the two streams, and the test that matters asserts a session which wrote a note shows the note. A test that only ever wrote fields would have passed against exactly the version of this that was not worth building.  **Its own endpoint, not a shape on `/api/activity`.** That URL *is* `keel_activity`, and the two answer different questions: the tool pages every mutation from a cursor for a model catching up; this answers "what did each session do" for a person who left Claude working. B-15's rule — the local API has more endpoints than the tool surface has tools.  **The limit counts changes, not sessions**, because a session can be four hundred of them and is still one row. On the live store 500 changes across 7 sessions is a cut list, and it says so.  **The screen kept its old name until it earned the new one.** C1 deliberately left it as "Activity" because "What changed" promised the grouped view it did not have. It is renamed now, along with its route, the palette entry and the shortcut.  **Found while building, and left alone: a close reads as five rows.** `close_reason`, `close_message`, `evidence` and `status` are four events from one call, because the store writes one event per field — deliberately, so "what changed" stays filterable by field. Collapsing them would need something identifying the call that produced them, and an event carries no correlation id. Worth a task if the noise bothers KB; it is not a fault in this screen.  **One thing I could not verify cleanly:** the browser console holds two stale errors from mid-edit — a `shortName` reference from before Ready.tsx reloaded, and a 404 for the deleted `Activity.tsx` from another chat's dev server watching it. Both screens render correctly after a reload and the typecheck is clean, so they are HMR artefacts rather than live faults. <sub>— claude · 2026-08-11</sub>

- **KEEL-106** C6 — A filter box in the Library, and words that say what filtering does `p2` · desktop, phase8, 8c

  The Board has a text filter plus status, priority and label menus and that part works. The Library sidebar has no filter at all, with around sixty documents in it. Cmd-K already does title matching across projects, documents and tasks, which is a good jump-to that most people will never find.
  
  Three changes:
  - A filter box in the Library sidebar, matching the Board's, narrowing titles within the selected type.
  - Say what the box does. "Filter…" does not tell you what it filters or how it differs from Search. "Filter these 94 tasks" and "Filter 62 documents" give the scope and the count, and the verb says it narrows what is already on screen.
  - Move the "Jump to…" button from the bottom of the sidebar to the top next to the project switcher, where it reads as the search affordance it is. Depends on the C1 rework.
  
  The rule to hold: filter narrows what is in front of you, instantly and literally. Search queries the whole store and understands meaning. The interface should never make you guess which one you are using.
  
  Done when both the board and the library have a filter box that names its scope and count.

  - Done. The board reads "Filter 120 tasks", the Library reads "Filter 46 documents", and "Jump to…" sits at the top of the rail beside the project switcher rather than in the footer.  The count is the part that does the work. "Filter…" said neither what it filtered nor how it differed from the Search screen; a number tells you the scope you are narrowing, and the verb tells you it is narrowing what is already on screen rather than querying the store. That is the distinction the two controls were making the reader guess at.  The Library's box landed with C2, since it had to know which kind was selected to count it. This task added the board's wording and the move.  Moving Jump-to was the smallest change and possibly the highest-value one: Cmd-K already indexed projects, documents and tasks, and the button announcing it was at the bottom of the rail where it read as a footer utility. It is now the thing next to the project name, which is where you look when you want to go somewhere.  187 desktop tests, typecheck clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-105** C5 — Search that suggests questions about this project `p2` · desktop, phase8, 8c

  The search box says "Ask a question — 'why is billing slow', 'what did customers say about onboarding'" and the empty state repeats the billing example (Search.tsx:128 and 138). Neither has anything to do with Keel. Both were copied from the MCP tool description, which is written for a generic project. KEEL-85 cleaned up this same pattern elsewhere and missed the one screen whose whole job is to invite a question.
  
  Replace them with starter chips built from the project's own content, which the digest already has: an open question quoted as-is, a recent decision phrased as a why, and a glossary term. Fall back to generic prose only when a project is genuinely empty.
  
  Two smaller things in the same screen: the eleven type facets are eleven chips of equal weight for something used occasionally, and the Menu primitive already used for scope would collapse them. And pressing `/` from the Board should land in search scoped to this project and filtered to tasks, not everything.
  
  Done when the starter queries come from the store, clicking one runs it, and an empty project still gets a sensible empty state.

  - Done. The billing example is gone from the placeholder and the empty state, replaced by three chips drawn from this project: an open question verbatim, a decision framed as a why, and a glossary term. One digest call, which the screen was not making before.  **The first version had three bugs and the browser found all three.** Worth recording, because each is the kind that unit tests with tidy fixtures would have missed:  1. **"why did we the plain-English rule covers every prose field".** The template was `why did we ${title}`, which assumes decision titles are verb phrases. In this store they are statements. Now `why did we decide that ${title}`, which is grammatical against a statement. 2. **"what is a anchor".** Article selection needs more than a vowel check, so the phrasing changed to `what does "Anchor" mean` — the form that needs no article and therefore cannot be wrong. 3. **The truncation leaked into the query.** Chips were shortened before being used, so clicking one searched for a string ending in "…". Truncation is now display-only via `chipLabel`; the query that runs is the full sentence. Verified in the address bar.  The three sources are not arbitrary. Each demonstrates a different thing semantic search is for: a question is prose you find by meaning, a decision-as-why finds reasoning whose title never contains the word "why", and a glossary term shows the store knows the project's own vocabulary.  Not done, and left deliberately: collapsing the eleven type facets into the `Menu` primitive, and scoping `/` from the board to tasks. Both are in the task body and neither is what the screen was failing at — the copy was. Worth their own pass rather than being smuggled in here.  189 desktop tests, typecheck clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-104** C7 — Show which phase a task belongs to, on the row `p1` · desktop, phase8, 8c

  A card shows the identifier, title, priority, kind and labels, but not what the task is part of. So the question asked most often about this project, "what is left in Phase 8", means opening each task or going to another screen.
  
  Checked against the code, and the spec overstates this. Board.tsx does read milestones: it fetches them (line 61), builds an id-to-name map (line 100), and feeds the filter bar (line 112). Grouping by milestone landed in KEEL-77 and filtering by milestone in KEEL-78, both working. The spec's claim that `milestone_id` is never read is wrong.
  
  What is actually missing is smaller than the spec suggests:
  - The chip on the row. A quiet mono chip in the right-hand gutter beside status, dashed and faded when a task has no milestone so an unassigned task looks unassigned. That gap is worth seeing: it usually means something was filed and never placed.
  - Making the chip the filter control, so clicking Phase 8 on a row narrows to that phase.
  - A done-count per group, so a phase heading reads "Phase 8 · 4 of 15".
  - Links both ways between Board and Roadmap. A milestone on the roadmap should reach its tasks; a chip should reach its milestone.
  
  The `keel ready --milestone` flag belongs with the ready work, not here.
  
  Done when every row shows what it is part of, an unassigned one shows that it is unassigned, the chip filters, and the two screens link to each other.

  - Done. Every one of the 120 cards on the board now carries its milestone, nine of them read "unplaced", and clicking "Phase 8" narrows the board to 23 with the filter in the address.  **The card had to be restructured, and that is the part worth knowing.** A card was a single `<a>`, and a chip that filters is a control — interactive content nested inside an anchor is invalid HTML. So the card is now a wrapper holding a stretched link (`after:absolute after:inset-0`) with the meta row `relative` so it sits above the overlay. The whole card stays clickable, the chip is a real button, and neither swallows the other.  **The chip shows "Phase 8", not "Phase 8 — The working loop".** Split on the dash rather than truncated to a width: a milestone with no dash keeps its whole name, where a fixed cut would turn "Q3 Launch" into "Q3 Lau…". The full name is the hover title.  **A second name map, deliberately.** `groupNames` already existed but carries task titles as well, because grouping by parent needs them. Looking a card's milestone up in that map would return a task title for any id that happened to match. `milestoneNames` is milestones only.  **No column in the list view.** `COLUMNS` drives sortable headers and `milestone` is not a `SortBy`, so adding one would have advertised an ordering that does not exist. The chip rides at the trailing edge of the task cell instead.  **A test caught the feature working.** "groups by milestone, and names it" broke on `getByText("Phase 6")` finding two elements — the column heading and the new chips beneath it. Narrowed to the heading rather than loosened, and a second test added for the chips.  The earlier finding stands: the spec was wrong that `milestone_id` is never read. Grouping (KEEL-77) and filtering (KEEL-78) already worked. What was missing was the chip, the done-count per group, and the two screens knowing about each other — the roadmap now links a milestone to its tasks.  177 desktop tests, typecheck clean, build green. <sub>— claude · 2026-08-11</sub>

- **KEEL-103** C3 — One way to show a date, used on every screen `p1` · desktop, phase8, 8c

  The Roadmap says "shipped 8/9/2026" (Roadmap.tsx:93) and Metrics does the same for its latest reading (Metrics.tsx:212). Both call `toLocaleDateString()` directly instead of `when()`, the relative-time helper in ui.tsx:538.
  
  `when()` has three faults of its own:
  - It cannot say anything about the future. Milestone target dates are future dates, and it would render one as "-3d ago". The roadmap is where this bites.
  - It drops the year past seven days, so "Aug 9" gets ambiguous as soon as the project is a year old.
  - There is no way to see the exact timestamp. Only the revision menu in Documents offers one.
  
  Fix: extend `when()` to handle both directions (in 3 days, tomorrow, just now, 4m ago, yesterday, 3d ago, then an absolute date with the year when it is not this year), put the exact timestamp on hover using the Tooltip that already exists, and use it everywhere.
  
  Done when no screen formats a date itself, the roadmap reads "shipped 2h ago" and "due in 3 days", and a test covers a past date, a future date and a date in a previous year.

  - Done. The roadmap reads "shipped 2d ago" and "shipped yesterday" where it used to say "shipped 8/9/2026", and a sweep for `\d{1,2}/\d{1,2}/\d{4}` across the rendered page finds nothing.  All three faults in `when()` were real and are fixed. The future one mattered most: it subtracted with no negative branch, so a milestone target date — the single most likely future date in the app, on the screen built to show them — rendered as "-3d ago". It now reads "in 3 days" and "tomorrow".  **`now` is injectable.** Without it a boundary test either waits for the clock or asserts something vague. With it, "in 4m" and "4m ago" are both exact assertions against a fixed instant.  **A `When` component rather than only the helper**, because "the exact timestamp is always available" is a promise a bare function cannot keep — every call site would have to remember the tooltip. It also emits `<time dateTime>`, so the value stays machine-readable instead of being a string that looks like a date.  **Four call sites, not two.** The spec named Roadmap and Metrics. `Documents.tsx:157` was also formatting its own — the revision tooltip — and that one is the *exact* timestamp the task wanted available everywhere, so it now goes through `exactly()` rather than being left as the one screen with its own opinion.  One conversion worth knowing: Metrics stores `Point.at` as epoch milliseconds because the chart plots it numerically, so the call site converts back to ISO. The alternative was carrying both, which is how the two drift.  176 desktop tests, typecheck clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-102** C2 — The Library: one layout per kind of thing `p1` · desktop, phase8, 8c

  Documents.tsx fetches specs, decisions, questions, feedback and design into one flat sidebar list, every row a type badge plus a status badge plus a title, in creation order. Five genuinely different kinds of artifact rendered identically — so with forty-five decisions and five questions in the store, a decision looks exactly like a spec looks exactly like a design, and there is no way to see the shape of any of them. Design images are invisible text rows.
  
  They are read differently, so they get different layouts under one screen called Library, with a type switcher across the top. A spec keeps the current reader, which is good. A decision becomes a table — number, title, status, when, supersedes — because it is a register you scan or look up by number, and that is the payoff for having numbered them in Phase 7. Questions group by open and answered with the answer inline. Feedback becomes chronological cards with source and sentiment. Design becomes a thumbnail grid.
  
  Clicking a row in any of the alternative views still opens the document reader, so revision history and diff keep working everywhere and nothing is lost.
  
  Done when: each of the five kinds has a layout that suits it; the decision register is scannable as a table; design images render as pictures; and clicking any row still reaches the reader with its revision history intact.

  - Done. Five kinds, five layouts, one screen called Library with a type switcher carrying counts — Specs 11, Decisions 46, Questions 50, Feedback 0, Designs 1.  **The decision register is a table now**, counting down from B-46, with status and a relative decided-date. That was the payoff for numbering decisions in Phase 7 and the flat list threw it away. **Questions group open-first** — 11 open, 39 settled — because "what is undecided" and "what did we decide" are different questions. **A design is a picture.** The thumbnail is one list call, not one per tile, because `blob_id` sits on the design entity itself.  **Which kind you are looking at is in the address** (`?kind=decision`), so a link to the decision register is a link.  **Two things I could not do cheaply, and did not fake:** - **The answer is not inlined under each question.** Bodies live in documents and the list endpoint does not carry them, so inlining would be one request per row against fifty rows. The grouping and the link are there; the inline answer wants a bulk body read. - **`supersedes` is shown as the status, not as a resolved link.** The edge is in the graph and reading it per row is a request per decision. `superseded` in the status column answers "was this overturned"; which decision replaced it is one click away.  **`loading="lazy"` had to come off the thumbnails.** With the tile plainly in the viewport, `currentSrc` stayed empty and the fetch never started, so the grid rendered empty boxes over a blob endpoint answering 200 with a valid PNG. Removing it fixed it immediately (240×120 natural size). At a handful of designs, deferring buys nothing worth a rendering quirk — worth a measurement before it comes back.  **The type badge is gone from the sidebar rows.** The switcher above already names the kind, so repeating it sixty times was the same word sixty times.  The reader is untouched and is still the destination from every index row — verified by clicking a register row through to `#/projects/keel/documents/dec_…` with the body rendered.  187 desktop tests (10 new), typecheck clean. <sub>— claude · 2026-08-11</sub>

- **KEEL-101** C1 — Navigation is inside out: put the project first `p1` · desktop, phase8, 8c

  On launch the sidebar lists eight screens at the top and the project list underneath them. Five of the eight need a project, so they render at 35% opacity with a "Pick a project first" tooltip and refuse clicks — and the one control that would fix that is below them. You arrive, most of the menu is dead, you scroll past it to choose a project, then come back up. Confirmed in App.tsx: `NEEDS_PROJECT`, `opacity-35`, and the Projects block following the SCREENS block.
  
  The fix is project-first. A project switcher at the top under the wordmark showing the current project's name, opening the same list or handing off to Cmd-K which already indexes projects — one row instead of one per project. Beneath it that project's screens (Overview, Board, Roadmap, Library, Metrics), always live because a project is always selected. Below a divider the global things: All projects, Search, What changed. Remember the last project in local storage and select it on launch, so the disabled state stops existing rather than being styled.
  
  Number-key shortcuts stay, renumbered to the new order.
  
  Done when: no navigation item is disabled on a cold launch (a Phase 8 exit criterion); the last project is restored on reload; and a test asserts the disabled branch is unreachable when any project exists.

  - Done. The rail is project-first and nothing in it is disabled — verified in the browser on a cold launch with the memory cleared: eight links, zero with `aria-disabled`, zero without an `href`.  **The mechanism is that a project is always resolved, not that the disabled state is styled away.** `activeProject = route.project ?? fallbackProject`, so the project section is live even while you are on a global screen. `#/` shows Board pointing at `#/projects/keel/board`. The disabled branch and its "Pick a project first" tooltip are gone rather than hidden.  **`defaultProject` takes the live slug list rather than trusting what is stored.** A remembered project can be archived or renamed between sessions, and honouring that would open an empty screen under an address promising content. It falls through to the first project, and returns null only for a genuinely empty store — which is the one case a project screen cannot be shown, and the rail then renders the global items plus an empty state instead of five dead ones.  **A race worth naming.** The fallback is null while the project list is in flight, which is not the same as "there is no project". Without a `projectsLoaded` flag the canonicaliser would bounce a project-scoped address to Home a beat before the answer arrived — a redirect caused by latency rather than by the address being wrong. The flag makes "unknown" distinct from "none".  **`isProjectScreen` is deliberately not `NEEDS_PROJECT`,** and the difference is Roadmap. The router does not require a project for it because the screen renders across all of them, but in the rail it is a project screen and should link to the one you are in. Two different questions that happened to have the same answer for every other screen.  **Activity keeps its name.** §C1 lists it as "What changed", but that name promises the grouped-by-session view C4 would build, and the screen already carries one header claiming a job it does not do. Renaming it now would add a second. TQ-35 decides it along with whether the screen survives.  **Three existing tests asserted the old behaviour and were rewritten, not deleted.** Two only needed the renumbered keys. The third — "does nothing for a project-scoped screen with no project" — was guarding the bug rather than a property: doing nothing *was* the dead-control problem. It now asserts the fallback instead.  Not done here: moving "Jump to…" to the top next to the switcher is C6, which this unblocks.  172 desktop tests, typecheck clean, build green, no console errors. <sub>— claude · 2026-08-11</sub>

- **KEEL-100** C0 — The design pass: Geist, a token layer, and a theme the user chooses `p0` · desktop, phase8, 8c

  The app uses the system font stack, which is the loudest possible signal that nobody chose anything, and the light scheme is a second copy of the palette inside a media query — which is how three status colours came to be missing from it before KEEL-73 fixed them by hand.
  
  Drop in `design/keel-tokens.css` over `apps/desktop/src/styles.css`. Checked: it is additive, not a rename — every token the app already uses keeps its name (`--color-surface`, `--color-ink-muted`, all eight text sizes), so no component needs touching to keep working. What it adds is Geist and Geist Mono self-hosted from `apps/desktop/public/fonts/` (already placed), a fourth sunken surface for the nav rail, a brass identity colour used only for the wordmark and the ready indicator, named spacing/radius/focus, and every colour pair declared once with `light-dark()` so a token cannot exist in one scheme and be absent from the other.
  
  Then a three-way theme control — system, light, dark — as `data-theme` on `<html>`, with a segmented control in the rail. Today the OS decides and there is no control anywhere.
  
  Done when: the app renders in Geist with no network request; the theme control switches all three ways and the choice survives a reload; a test asserts no `--color-*` token is declared in one scheme only; and `grep` finds no raw hex or `oklch(` outside the token block.
  
  Everything else in 8C reads against these tokens, so this goes first.

  - Done. Tokens in, Geist self-hosted, three-way theme control, all verified in a running browser rather than by reading the CSS.  **The token file was a clean drop-in, as hoped.** Every name the app already used survived, so no component needed touching. Verified in the page: `p-4` still resolves to 16px, so defining `--spacing-hair` and friends does not disturb Tailwind v4's numeric scale — the named keys sit alongside the `--spacing` multiplier rather than replacing it.  **A verification trap worth knowing.** My first check injected a `<div class="bg-surface-sunken rounded-card text-brand">` at runtime and read the computed style. Every new token came back unset, which looks exactly like "the tokens do not work". They were fine. Tailwind v4 generates utilities by scanning source files, so a class that exists only in a string created at runtime is a class it has never seen. The honest check is real usage: once the rail actually used `bg-surface-sunken` and the wordmark `text-brand`, both resolved (0.13 sunken against 0.16 body, brass at `oklch(0.55 0.12 70)`).  **The theme is applied in `main.tsx` before `createRoot`,** not in a `useEffect`. Applying it on mount means the first paint uses the default scheme and then jumps.  **Found and fixed, unrelated but blocking:** four `noUncheckedIndexedAccess` errors in `lib/api.test.ts` from commit 61b8582 meant `tsc --noEmit` failed, and `npm run build` runs that first — so the desktop production build has been red since yesterday and nothing said so. `npm test` passes independently of `tsc`, which is why it went unnoticed. Fixed with four non-null assertions matching the file's existing style. The build now produces a bundle, and the fonts land in `dist/fonts/` and are referenced by relative path, so the packaged Tauri app has them with no network.  **Two test-writing notes,** both cases of a test that would have passed against nothing: 1. `import STYLES from "../styles.css?raw"` returns the empty string under vitest, which stubs CSS imports. Every assertion about the stylesheet would have passed vacuously. Reading from disk instead. 2. The "no second palette" check first matched the bare string `prefers-color-scheme`, which appears in the token file's own header explaining what it replaced. Matching `/@media[^{]*prefers-color-scheme/` distinguishes a media query from a sentence about one.  Not done here, deliberately: the rail still shows the C1 dead-nav problem, and `bg-surface-raised` is still used by cards elsewhere. Those are C1 and C2.  162 desktop tests pass, typecheck clean, build green. <sub>— claude · 2026-08-11</sub>

- **KEEL-99** A milestone cannot be created without a plain-English explainer `p0` · mcp, storage, phase8

  The roadmap shows Phase 6 and Phase 7 as bare names with no description, because `keel_create` takes a `body` for a milestone and throws it away. `build_entity` (crates/keel-mcp/src/dispatch.rs:1070) calls `Milestone::new(project, name)` and never reads `body`, while the same function routes it into `description` for a project and `body` for a task. `Milestone.summary` exists (crates/keel-core/src/types.rs:307) and nothing writes it.
  
  Phases 0 to 5 have summaries because `keel bootstrap` builds the struct directly. The two created through the tool surface, 6 and 7, are the two that are empty.
  
  An input that is accepted and discarded is worse than one that is refused: the caller never finds out.
  
  Done when:
  - `Milestone::new` takes the summary as a required argument, so the compiler finds every call site.
  - `keel_create` declares it required for milestones and refuses a missing or empty one, saying what would be valid.
  - The tool description carries the style rule with a good and a bad example: one or two sentences, plain English, no AI register. That register is KB's own standard, written down as B-45.
  - A length ceiling is enforced (the existing summaries run 8 to 15 words; a paragraph is too long).
  - Phase 6 and Phase 7 have summaries written from what they actually shipped.
  - Tests cover a refused create and a round-trip to the roadmap.
  
  KB's call on 2026-08-11, recorded as decision B-45 with the reasoning behind making the field required.

  - Done, and verified against the running daemon rather than only in tests: `keel_create` for a milestone with no explainer now comes back with `invalid milestone field summary`, naming what would be valid, and leaves no row behind.  **The signature is the enforcement.** `Milestone::new(project, name, summary)` takes it positionally, so the compiler found all seven call sites instead of review having to. Two of those — `bootstrap.rs` and `fixture.rs` — were already setting `summary` immediately after constructing, which is what made the gap invisible: the paths a person looked at did the right thing by hand, and the one nobody looked at silently did not.  **Validation is in `keel-core`, on both doors.** `validate_entity` is called on create *before* the idempotency lookup, so a bad write is refused rather than quietly matching an existing row and reporting success. It is also called on update, because enforcing only on create means the requirement holds for the first write and any later call can blank the explainer back out.  **`body` is accepted as well as `summary`.** A caller reaching for the generic prose field means the same thing for a milestone, and silently discarding it is the exact failure this task existed to remove. A daemon test asserts the `body` path lands in `summary`.  **Three things the run turned up that were not the task:**  1. **The disk was full** — 266Mi free, `target/` at 15G — and it presented as *13 failing daemon tests*. The link step was failing with `errno=28`, which surfaces as a test failure rather than as a disk error. Clearing `target/debug/incremental` (1.6G) was enough; it costs incremental-compile speed rather than a rebuild. Worth knowing that this repo has hit the same wall before — B-11 records dropping `--all-features` for filling the disk.  2. **I misread a red suite as green, and the cause was my own shell.** I ran `cargo test --workspace 2>&1 > file`, which sends stderr to the terminal and stdout to the file — the reverse of what was intended — and then checked `$?` on the trailing `grep` rather than on cargo. Both halves were wrong in the same direction: a partially-written file showed "197 passed, 0 failures" while the suite was failing. Corrected to `> file 2>&1` and an explicit `CARGO_EXIT=$?`. The general point is that a test summary read from a file is only evidence if the exit code came from the test runner.  3. **The `insta` snapshot on the tool list caught the schema change**, which is what it is for — the tool list is an API contract. Reviewed the diff before accepting: it is the `summary` property and nothing else.  The daemon test I added asserts the refusal at the MCP boundary rather than only at the store, because the MCP path is where the bug was — the store was never asked, so a store-only test would not have caught it.  436 workspace tests, clippy clean with `-D warnings`, fmt clean. Binaries rebuilt and installed; the daemon is running the enforcing build. <sub>— claude · 2026-08-11</sub>

- **KEEL-98** 8C — Make the app legible `p0` · desktop, phase8, 8c

  Parent for the eight pieces of §8C. The app is what KB looks at every day and none of §8C depends on anything else in the phase, which is why it leads.
  
  Three design attachments accompany this section and are the design rather than a description of it: `design/keel-design-system.html`, `design/keel-screens.html`, `design/keel-tokens.css`, plus the two Geist woff2 files already placed in `apps/desktop/public/fonts/`.
  
  Done when the eight children are done.

- **KEEL-97** SSE never opens in the in-app browser, cause unknown `p3`

  Live refresh: the daemon now emits an SSE comment immediately on connect rather than waiting up to 15s for the first keep-alive, and the Vite dev proxy no longer buffers. Verified with curl both directly and through the proxy — `: keel` arrives at once.
  
  The browser end is NOT verified. In the in-app Browser pane an `EventSource` stays in `CONNECTING` indefinitely: `onopen` never fires, `onerror` never fires, and a real change event emitted while it watched was not received — despite curl reading the same stream through the same proxy successfully.
  
  So the buffering fix is real and the remaining fault is elsewhere: either that browser does not support SSE properly, or something in its request interception holds the response. Untested in a normal browser and in the packaged Tauri build, where there is no proxy at all.
  
  Next step is simply to open the app in Chrome or Safari and watch `readyState`. If it is 1 there, this is a quirk of the test browser and the task closes.

  - Live-refresh probe from real Chrome. <sub>— claude · 2026-08-11</sub>
  - Fixed, and verified in real Chrome. `readyState` 1, a live `change` event received from a write made while the page watched.  **The last cause was my own fix.** Chasing the proxy buffering, I added a `res.flushHeaders()` call in Vite's `proxyRes` hook. That sends the response head *before* http-proxy has copied the upstream headers onto it, so the reply reached the browser with **no `content-type` at all**. `EventSource` needs `text/event-stream` to accept a stream, so it sat in `CONNECTING` forever.  curl did not care about the missing header and printed the body happily, which is exactly why it looked fixed. The measurement that broke it open was `fetch()` in the page reporting `status: 200, content-type: null` — a header the same request showed correctly one layer down.  Three things were wrong in sequence, and only the first two were real:  1. **The daemon sent nothing until the first change or the 15-second keep-alive.** A stream with no bytes is one an intermediary can sit on. It now yields an SSE comment on connect. Genuine, and the fix stands. 2. **The dev proxy compressed the stream**, buffering a whole gzip block. `accept-encoding: identity` on that path. Genuine. 3. **`flushHeaders()` stripped the content type.** Self-inflicted, removed.  Also learned, and worth knowing: `keel_note` writes do **not** announce. The daemon only announces when the latest event id advances, and a note leaves no row in `events`. So the app does not live-refresh on a note. That is arguably right — notes are a stream of their own — but it is undocumented behaviour and I found it by a probe silently receiving nothing. Worth a look separately rather than changing blind.  The earlier claim that this was "a quirk of the test browser" was wrong: real Chrome behaved identically, which is what made it worth chasing rather than dismissing. <sub>— claude · 2026-08-11</sub>
  - TQ-29 verification: this note must announce. <sub>— claude · 2026-08-11</sub>

- **KEEL-95** keel_create fails on decisions written during the number migration window `p2`

  Reported from another project: keel_create with type decision failed reproducibly with `read column number of decisions: Invalid column type Null`, having worked earlier the same morning.
  
  Migration 10 added decisions.number at 18:43:57 and backfilled everything that existed. At 18:45:21 - 84 seconds later - a daemon that had the column but not the struct field inserted a decision, and the row got a NULL. Reading it strictly made every decision in that project unreadable, and since the idempotency check is a read, create failed too.

  - Fixed in a5629fc. Three parts, and the middle one is the point.  **Proportion.** A NULL number now reads as 0 - which already meant "not yet assigned" everywhere else - instead of erroring. One unnumbered row should cost that row's label, not make the whole artifact type unreadable in that project. The strict read turned a single bad row into "decisions are broken", and because the idempotency lookup is itself a read, into "creates are broken" as well.  **Not leniency alone.** Reading NULL as 0 by itself would trade one failure for a worse one: two rows written back at 0 collide on the unique index. The update path now assigns a real number to anything holding 0, matching what create already did.  **Migration 13** repairs rows already written, covering tasks as well - it had the identical shape and survived only because migration 6 landed in a quieter minute.  Verified against the live store before and after: the affected project read 500 and now returns its decision; keel_create in it succeeds; Keel's own B-1..B-43 are untouched.  The generalisable lesson, recorded as B-44: **a column added by a migration is NULL for every writer that has not been restarted yet.** Any read of a newly-added column has to tolerate that window or the migration becomes an outage for whoever writes during it. This window was 84 seconds wide and still caught a real project.  Also on me: the report arrived because another project was using the store while I was mid-migration. Nothing warned either side. Worth considering whether the daemon should refuse to serve when its binary predates the store's schema version, which would have turned a silent corruption into a startup error. <sub>— claude · 2026-08-11</sub>

- **KEEL-94** One decision register: fold the B-n prose table into the decision rows `p2`

  Split out of KEEL-92 because it is engineering, not editing.
  
  The two decision registers do not contain each other, which is why neither can simply be deleted:
  
  - `product/DECISIONS.md` holds a numbered table, B-1 to B-25, with the full reasoning — several hundred words per row for the ones that matter.
  - `.keel/decisions/` holds 39 artifact files, one per decision row, each a short summary. Only 11 of them carry a B-n at all.
  
  So 39 rows and 25 table entries overlap partially in both directions: rows with no table entry, table entries whose reasoning exists nowhere else, and eleven that are genuinely the same decision written twice at different lengths.
  
  What it needs:
  
  1. **A stable readable identifier for decisions.** Only `tasks` has `number` (migration 6). B-n exists solely as prose, which is why `fsck`'s `unresolved_id_reference` check cannot resolve a `B-n` citation and has to be scoped around the whole family — see KEEL-66, where that limitation is the reason the check misses the exact case that motivated it. Giving decisions a number is a schema change and therefore KB's call.
  2. **Migrating the fuller reasoning** from the 25 table rows into the artifact bodies, so the artifacts become authoritative rather than summaries of a document.
  3. **A generated `product/DECISIONS.md`**, rendered from the rows the way `render-status` renders the tracker.
  
  Point 3 raises a question worth answering once for all aggregates: `render-status` writes to `project.status_path`, but the mirror's aggregates (`.keel/questions.md`, `.keel/glossary.md`) have hardcoded paths under `.keel/`. `product/QUESTIONS.md` was archived rather than made generated for exactly this reason — the register moved to `.keel/questions.md`, which is generated and complete but sits in a bookkeeping directory rather than where the documentation lives. Either the aggregates get configurable paths, or `product/` is accepted as the wrong place for two of them.

  - Steps 1 and 2 done. Step 3 — a generated `product/DECISIONS.md` — is not, and is the whole of what remains.  **Done: numbers.** Migration 10 adds `number` to decisions and backfills in creation order, which is correct for every store but this one. Migration 11 maps Keel's own B-1..B-25 explicitly by id, in two phases because the permutation overlaps itself and the unique index would reject the intermediate state. It is a no-op on any other store. `KEEL-B12` resolves through `keel_get` alongside `KEEL-42`, via a separate parser — `KEEL-12` and `KEEL-B12` are both valid and name different artifacts, so an optional `B` would resolve typos to real rows.  The mapping was made by hand and that was not caution for its own sake: fuzzy title matching got 4 of 25 wrong, two of them with high confidence. Three table entries (B-23, B-24, B-25) had no row at all and were written as rows first; one row (Fixture links, now B-26) had no table entry. The two registers genuinely did not contain each other in either direction.  **Done: the reasoning.** All 25 bodies now hold the table's full text, merged rather than replaced — 7 `## Context` sections and 1 `## Superseded` section were carried across. The first attempt read `data.body` where the API returns `data.document.body`, so every existing body looked empty; it would have written the merge as a wholesale replacement and dropped all 8 sections without erroring. Caught by asserting on a shrink report rather than by reading the output.  **Not done: rendering.** Four decisions the next session has to make, none of them mechanical:  1. **Where the renderer writes.** `render-status` writes to `project.status_path`; the mirror's aggregates are hardcoded under `.keel/`. Cheapest is a `decisions_path` column parallel to `status_path`, but that is the second such column and the third aggregate — worth deciding once whether aggregates get configurable paths generally. 2. **The DECISIONS.md spec must be archived first**, as `product/QUESTIONS.md` was, or generation reports the path collision and writes neither. That is correct behaviour, not a bug to work around. 3. **`product/DECISIONS.md` is not only decisions.** It also carries a Format section, the P0-2 dependency-verification findings, and the MCP deltas. Those are measurements, not decisions, and they have nowhere to go — they need their own artifact rather than being deleted or forced into a decision row. 4. **Reversals should derive from `status = 'superseded'`** plus the row's `## Superseded` section, rather than being a hand-kept table. Two rows exist for it today: B-24, and the SPEC §8.1 hook reversal, which currently has no decision row at all.  Until step 3 lands, `product/DECISIONS.md` is still hand-maintained prose and the B-n table in it now duplicates the rows rather than exceeding them — so the file is stale-able but no longer the only copy of anything.  **Not done, and worth its own decision:** `fsck` still scopes the `B-n` family out of `unresolved_id_reference`. The scoping rule is "only check a family where the project titles artifacts that way", and it now *could* check B-n against decision numbers. That is the payoff this task was justified by, and it is a small change to `fsck.rs` that I have not made. <sub>— claude · 2026-08-10</sub>
  - Step 3 done. One decision register, generated.  `product/DECISIONS.md` is now rendered from the 42 decision rows by `render_decisions`, written to the project's new `decisions_path` (migration 12) with the same collision rule as the tracker: a document that has adopted the path wins and the conflict is reported, because prose cannot be regenerated and a rendered view can. The prose spec that used to own the file is archived.  KB chose a second nullable column over a general aggregate-path map: three aggregates exist, two are content under `.keel/`, and a stored map is more machinery than three instances justify. Revisit at a fourth.  The layout is index, then Reversals, then the decisions. Reversals go above rather than at the end — a reversal met after acting on the original is met too late. It derives from `status = 'superseded'` plus the row's `## Superseded` section rather than a hand-kept table, and a row marked superseded with no such section renders as a data problem rather than a heading over silence. B-24 is the first entry.  The non-decision content moved to `product/DEPENDENCIES.md`: the P0-2 verification table and the MCP deltas, headed as a snapshot taken on a date rather than a description of now, with a closing section naming the three things about it that are already false. They are the evidence behind B-2, B-3, B-4, B-12 and B-17, so deleting them would have left five decisions citing a verification nobody could read.  **fsck now checks the B-n family.** `unresolved_id_reference` resolves `B-n` against the `decisions.number` column instead of skipping the family, and every citation in this repository resolves — zero findings. That was the payoff this task was justified by, and it closes the gap named in KEEL-66.  **Two things found on the way, neither fixed:**  1. **The immutability guard is on the wrong door.** `keel_update` refuses a title change on an `accepted` decision — correctly, per SPEC — but `keel_write_doc` rewrote all 25 decision bodies without objection. The title is guarded and the content is not, which is backwards: the reasoning is the decision. Raised as TQ-27. 2. **Seven decision titles are truncated in storage** (B-5, B-7, B-8, B-11, B-18, B-19, B-22), cut at about 80 characters by whatever imported them. B-8 reads "Surface carries five values, not four: chat \". This was invisible while the prose table carried the real titles and is now the heading in the index. The full text survives in each body's `## Decision` line, so nothing is lost — but the headings are wrong, and fixing them is blocked by the immutability rule above, which is the honest reason to settle that first rather than route around it. <sub>— claude · 2026-08-10</sub>

- **KEEL-93** fsck died on its own staleness check, and the test suite was red `p0` · correctness, cli

  Found while running the suite before starting the detail view. `keel fsck` failed outright — not one check reporting a finding, the whole run dying — because `stale_in_progress` binds `updated_at < now() - INTERVAL 3 DAY`, and that is a binder error against the embedded DuckDB. It landed with the staleness guard in ead61c9 and the workspace has been red ever since; the test written for it used the same expression, so it never passed either.

  - Fixed by computing the cutoff in Rust and binding it, rather than by correcting the SQL. `now()` is TIMESTAMPTZ where `updated_at` is TIMESTAMP, so the first attempt was `current_localtimestamp()` — and that failed too, with `-(TIMESTAMP, INTERVAL)` and an *empty* candidate list, even though the identical expression binds fine in the standalone DuckDB 1.5.5 shell. Something about the embedded connection's function catalogue, and not worth chasing: a bound parameter sidesteps it entirely and states the rule more plainly. The threshold is now a named constant instead of a number inside a SQL string.  Two things worth keeping from this:  **It failed at bind time, so one bad check killed all 28.** `fsck` is the integrity audit, and it could not run at all — which surfaced as a crash rather than as a finding, so it read as "the tool is broken" rather than "your store might be". A check that cannot run is worse than a missing one.  **The test never passed.** It backdated a row with the same `now() - INTERVAL 5 DAY` and unwrapped the result, so it failed on the same binder error the check did. Both were written in one sitting and neither was run — which is the specific failure the definition of done's "tests written **and** passing" exists to catch. <sub>— claude · 2026-08-10</sub>

- **KEEL-92** Document reset and the question prune `p2` · docs, phase7

  RESET-PLAN 7.7. Last, so it describes the finished state. Rewrite README.md to say what is true today — the test count is wrong and it claims the gate is unrun after seven runs. Retire PROBLEMS.md, or date it clearly as a historical snapshot; it reads as current and its central claim was refuted by the review it was written for. One decision register: the numbered prose table inside DECISIONS.md and the thirty separate artifact files must become one thing — the product's own integrity checker has to be specially configured to route around the split. Same for the two question registers. Prune the questions from 17 to about seven real ones: two are finished, seven have working answers nobody has objected to. Correct the three known errors in the SPEC and mark the reversed decision as superseded — the Reversals table built for exactly this is empty. Cut the duplication in JOURNAL.md, which re-narrates four other documents nearly section by section.

  - Done, with one half split out as KEEL-94.  **The question prune: 18 open, not 17, and now 8.** Ten had shipped answers nobody had objected to — TQ-2, TQ-4, TQ-5, TQ-8, TQ-14, TQ-15, Q-2, Q-3, Q-4, Q-7. Each got the answer written into its body before the status moved, because "answered" with no answer is the same information loss as a status with no note. Two of them named SPEC errors and were closed by fixing the SPEC, not by asserting the fix.  **One register for questions, achieved by generating it rather than by deleting a copy.** `.keel/questions.md` carried only unresolved rows, which is why `product/QUESTIONS.md` existed at all: something had to hold the settled ones, and that something was hand-maintained prose. The aggregate now emits both halves, Open first, with the reasoning attached to each. `product/QUESTIONS.md` is archived and its file removed; both CLAUDE.md files and HANDOFF.md point at the generated register. HANDOFF used to name specific question numbers and had gone stale within a day — it now names the file and says why.  **KEEL-94, split out.** The decision registers cannot be unified the same way: the B-n prose table and the 39 artifact files do not contain each other. Only 11 artifacts carry a B-n, the artifacts are summaries where the table has the full reasoning, and decisions have no `number` column — only tasks do. That last point is not cosmetic: it is exactly why fsck cannot resolve a `B-n` citation and misses the case that motivated it (KEEL-66). Needs a schema decision, so it is KB's.  **Three things I found while writing rather than while looking:**  1. `.keel/README.md` — generated on every run, into every project Keel touches — still promised that a Claude Code hook converts your edit into an attributed revision. That claim outlived the hook by a day and would have kept propagating. 2. `product/CLAUDE.md`, the contract loaded into every session, said the same thing and cited SPEC §8.1 for it. It also still told sessions to update a changelog that now derives itself, and to use `blocked` as a status that no longer exists. 3. SPEC §8.1 was 15 lines arguing why one read of the mirror was safe. Rewritten as a superseded section that says the hook never ran, why deleting beat repairing, and that the rule now has no exception. The Reversals table — empty since it was built — has its first two rows: this, and B-24.  **JOURNAL: 6,029 words to 4,617.** Four sections re-told the gate runs and now point at GATE.md. What was kept is the part GATE.md cannot carry: that I measured an instrument for five evenings while believing I was measuring an agent, and that what caught it was a rule (`observed == launched`) rather than reading more carefully.  **README: every load-bearing number in it was wrong.** 264 tests → 408 plus 146 desktop; nine tools → ten; the gate described as unrun when it had run seven times and closed; `plugin/` described as containing "the mirror hook". Added a fourth entry to "most likely to bite" — `blocked` is not a status — and the pre-commit install line, since nothing told anyone to install it.  Verified end to end: hand-edited five generated files, `keel import` round-tripped all five, `generate --check` up to date at 51 files. 408 Rust tests, 146 desktop, clippy and fmt clean. <sub>— claude · 2026-08-10</sub>

- **KEEL-91** Freeze the gate — one retrospective in place of six documents `p2` · gate, docs, phase7

  RESET-PLAN 7.7 and KB decision 4. The gate work is frozen: keep the code, stop running it. Phase 2's 18-of-20 result stands as measured, understanding that Phase 7 changes the surface it measured. Collapse the six gate documents (~11,500 words, against 9,888 for the PRD and SPEC together) into one short retrospective, and close the remaining gate tasks as "not now" with the result recorded.

  - Done. Six documents (11,682 words) became one at 1,400 — product/GATE.md, spc_01KZPDVA3THNZG533KZZ6772JX.  The six are archived in Keel with revisions intact, not deleted, and their files are gone from the repo. Archiving is what removes them from generation: the adopted-file pass lists non-archived entities only, so generate stopped writing them and --check is clean at 52 files.  "Close the remaining gate tasks as not now" was a no-op — all seven (KEEL-36, 59-64) were already done. Recording that rather than inventing work: the plan assumed a backlog of unfinished gate steps and there wasn't one. WAY-FORWARD's Steps 4, 5, 7, 8 and 9 were never made into rows at all, which is its own small finding — a nine-step plan that produced six tasks, and the run that met the criterion used one of its five treatments.  Two corrections the writing forced, both from checking rather than remembering:  1. I first wrote that the keep-rate's two precision bugs "live as task rows where they can be acted on". Both were already fixed (KEEL-65, KEEL-66). 2. I then wrote that both were fixed. KEEL-66's own note says the fsck check does NOT catch the case that motivated it — no project titles artifacts D-n, because Keel's decisions live as a numbered table inside a prose document rather than as rows, and a lexical check cannot distinguish "cites a convention documented in prose" from "cites nothing". The page now says partly addressed and names the limitation. That is the two-decision-registers problem arriving as a concrete cost, and it is the argument for KEEL-92's unification.  Left alone deliberately: task notes in STATUS.md still say things like "full write-up in product/RUN-A.md". A note is a dated record of what a session found; rewriting it retroactively to match today's filenames is worse than a stale filename. GATE.md names all six and says where they went. The two shell scripts that referenced them are code, so those comments were updated — gate-run.sh now opens by saying it is frozen and why it is kept. <sub>— claude · 2026-08-10</sub>

- **KEEL-90** Protocol honesty, and one name for the daemon address `p2` · mcp, daemon, phase7

  RESET-PLAN 7.5 and 7.6. Advertise both protocol revisions, since both are accepted. Return "tool not found" as a tool error rather than HTTP 404, which by the protocol's own documentation means "there is no MCP server at this address" — a typo currently looks like the server vanished. Keep the server's introduction text in one place; it exists word-for-word in two source files, so editing one makes the two ways a client can ask "who are you" disagree. And the daemon's address is spelled three different ways across the hooks, hardcoded a fourth time in the plugin config and defaulted a fifth time in the CLI: pick one environment variable and make everything read it.

- **KEEL-89** Make the local API and the MCP tools genuinely one thing `p1` · daemon, mcp, phase7

  RESET-PLAN 7.4. The code claims they can never drift; they already have. Route every endpoint through the same dispatch path — four bypass it. Fix list-type parameters being silently dropped: asking the web API to search only specs returns everything, with no error, while the same request over MCP filters correctly. Stop coercing numeric-looking text into numbers, so searching for "404" stops failing with "query must be a string". One error shape across the API, not three. Allow POST in the CORS rules so the one POST endpoint is reachable from the desktop app. Stop scanning up to 100,000 event rows twice per tool call, under the global write lock, just to read the last event id.

  - Done. Both drift bugs reproduced exactly as the plan described, and both had one cause.  `params_to_json` guessed types from the *value*: anything that parsed as an integer became a number, "true"/"false" became booleans, everything else stayed a string. So `?types=spec` was passed as a bare string, the tool ignored it, and a search restricted to specs returned specs, decisions, questions and tasks with no error at all. And `?query=404` parsed as an integer, arrived as the number 404, and was rejected with "query must be a string" — the one search term guaranteed to be numeric being an HTTP status code, which is exactly what anyone searches a project for.  It now reads the *declared* type out of the tool's own schema. That is what makes the two surfaces the same surface rather than two that resemble each other: the schema being consulted is the one the tool advertises, so this class of drift cannot recur. Verified live — `?types=spec` returns only specs, `?query=404` returns a result set.  **One error shape**, `{code, message}`, the same one MCP returns. There were three: a bare string, `{message}`, and the full form. The desktop client reads `error.message`, so the bare-string form arrived as `undefined` and the app showed "Request failed (400)" — losing the explanation in exactly the case where the daemon had bothered to give one.  **POST is allowed by CORS.** `/api/generate` is the one endpoint the app needs in order to *do* anything rather than read, and it was unreachable from the browser for as long as the rule said GET only.  **`latest_event` is one row.** It fetched up to 100,000 events and took the last, twice per tool call, while holding the global write lock. Now `SELECT max(id) FROM events` — correct only because ULIDs are minted monotonically (B-9), which is the same property the event cursor depends on.  **Not done, and I think the plan is wrong about it:** "route every endpoint through the same dispatch path — four bypass it". `/api/entities`, `/api/notes`, `/api/document` and `/api/graph` bypass because there is no tool for them, and there is deliberately no tool for them — an accepted decision says Keel's local API has more endpoints than the MCP surface has tools, because a UI knows exactly what it wants while a model chooses worse among more options. Routing them through dispatch would mean four new tools and a worse tool surface. What they actually needed was to stop differing in the ways that mattered: parameter typing and error shape. Both now come from one place.  Found while fixing the test for this: the old assertion checked that `include_archived` became a boolean on `keel_search` — a parameter `keel_search` does not take. The value-guessing conversion made it a boolean anyway, so the test passed while describing a parameter that was being discarded one layer down. With typing driven by the schema, the test had to name a tool that really declares it. <sub>— claude · 2026-08-10</sub>

- **KEEL-88** Tidy the tool surface `p1` · mcp, phase7

  RESET-PLAN 7.3. Settle on ten tools and say ten everywhere — the codebase says "nine" in five places and the skill's tool table omits keel_note entirely. Fix the description whose example uses `id` where the parameter is `ids`. Put the seven undocumented parameter names into the schema or stop accepting them. Mark the required fields on keel_note, which declares none. Mark archive and remove as destructive; they claim not to be. Make keel_update's description say when to use keel_write_doc instead — only one side of that pair explains the split today. Delete the dead code: the unread constant, the unused error code, the handling for two unadvertised protocol methods, the test-only lookup, the two phantom session markers. Add one test asserting the tool list and the dispatch code agree, so that class of drift cannot recur.

  - Done, with one correction to the plan's numbers and one finding.  - **Ten is said as ten**, in `tools.rs`, the daemon's comment, the bootstrap text — and the skill's tool table now has `keel_note`, which it omitted entirely. - **`keel_get(id: spec_id)` in the description is now `keel_get(ids: [spec_id])`.** A model copying it verbatim got an error. - **`keel_note` declares `required: ["id"]`** — it declared nothing. Not `body`, deliberately: it is required for the common case but not when listing or retracting, and demanding it would make those two uncallable. - **`destructiveHint` is per-tool** instead of hardcoded `false` for all ten. The old comment justified it as "nothing in Keel is ever deleted", which conflates *recoverable* with *additive*. The annotation a host gates on means non-additive: `keel_update` overwrites fields and can archive, `keel_link` can remove an edge. Those two are true; the rest, including `keel_write_doc` and `keel_note`, are genuinely append-only. - **`keel_update` now says what `keel_write_doc` is for.** Only one side of the pair explained the split, so a model reading the other would put a body in the wrong place.  **The undocumented parameters were four, not seven, and they could not simply be dropped.** `keel_create` accepts `name`, `term`, `slug` and `definition` with none of them in the schema. `name` and `term` were at least mentioned in the `title` prose; `slug` was invisible — and a project *cannot be created without one*. So they are declared now rather than removed.  **Dead code:** `META_CLIENT_CAPABILITIES` (nothing read it) and the `Mcp-Name` validation for `prompts/get` and `resources/read` — this server advertises neither capability and routes neither method, so validating them read as support for them. I could not find the "two phantom session markers" the plan names; if they existed they are already gone.  `strip_banner_for_test` is renamed `strip_banner_public`: it stopped being test-only when the pre-commit check started needing exactly that rule. A name that says "test only" on something the product depends on is worse than no name.  And the drift the plan wanted closed cannot recur: a test dispatches every advertised tool and asserts none answers "no tool named that", plus the reverse — and a second test pins the count at ten, because a number in prose drifts and a number in an assertion does not. <sub>— claude · 2026-08-10</sub>

- **KEEL-87** Delete the file-edit hook, and replace it with a check that fails loudly `p0` · plugin, mirror, phase7

  RESET-PLAN 7.2, KB decision 3: delete, not repair. Two of the hook's three moving parts do not work and its stated safety guarantee is false. It calls `keel mirror`, a command that does not exist, so it fails silently every time — which makes the claim written into it and into the plugin README ("the database wins unconditionally afterwards, the file is regenerated") untrue, and opens the window for silent divergence the design says cannot exist. It also captures the document's heading along with the body, so every round trip adds another copy of the title. And it reads KEEL_SESSION_ID, which nothing anywhere sets, so every revision it writes is attributed to nobody. Delete plugin/hooks/mirror-edit.sh and replace it with a pre-commit check that refuses a commit when a generated file has been hand-edited, telling you to make the change in Keel instead. A mechanism that silently does not work is worse than no mechanism.

  - Done. `plugin/hooks/mirror-edit.sh` is deleted, its `PostToolUse` registration removed, and `scripts/pre-commit` replaces it — installed as this repo's git hook and verified: staging a hand-edited `product/STATUS.md` refuses the commit and names the file.  All three of the plan's claims held, and one of them turned out to have a better explanation than "a command that does not exist". `~/.local/bin/keel` is a build from 9 August with a `mirror` subcommand and no `generate`: **`keel mirror` existed when the hook was written and was renamed underneath it.** The hook then swallowed the failure with `|| true`, so nothing said so for a day. That is a more useful lesson than the original reading — the danger was not a typo, it was a shell script calling a CLI with no test holding the two together.  Two things the replacement does that the original did not:  **It distinguishes "could not run the check" from "the files are wrong."** A daemon holding the store lock, or no usable binary, exits 0 with a message rather than refusing the commit. Reporting one as the other is how a green check comes to mean nothing.  **It prefers the repository's own build over `PATH`.** Found by the first run of my own test: it picked up the stale binary, got "unrecognized subcommand 'generate'", and reported drift that was not there — the same disease as a hook that silently does nothing, in the opposite direction.  The plugin README's guarantee is gone with the hook. It said "the database wins unconditionally afterwards, the file is regenerated", and that was untrue for as long as it was written down. <sub>— claude · 2026-08-10</sub>

- **KEEL-86** One authority per instruction `p0` · plugin, phase7

  RESET-PLAN 7.1. Three files each tell Claude the same three things in slightly different words, and two contradict each other: the skill tells Claude to invent its own session identifier while the session-start hook tells it to use a specific one and not invent one — both are in context at the same time, and the stop hook only recognises the hook's format, so a session that follows the skill, writes correctly, and is then told it recorded nothing. Decide who owns each: session identity → the session-start hook (delete the skill's competing instruction, make the stop hook accept whatever the hook issued); when to write → the skill (the hook points at it rather than restating it); the end-of-session check → the stop hook, in one sentence. Then make the session-start hook fire only at actual session start, not on every compaction — it re-injects a 250-word preamble every time the conversation compacts.

  - Done. Each instruction now has exactly one author, and the plugin README states the assignment in a table so a future edit has somewhere to be checked against.  - **Session identity → the session-start hook.** The skill's "mint one identifier" section is gone, replaced by "use the one the hook gave you". That is what closes the false nag: the stop hook already accepted `ses_<uuid>` and the bare uuid, and the only form it could not recognise was the one the *skill* told sessions to invent. A session could follow the skill, write correctly, and be told at the end it had recorded nothing. - **What to write → the skill.** The hook now points at it instead of restating it. - **The end-of-session check → the stop hook, in one sentence.** It was a paragraph re-teaching what to record and how to phrase it. - **The hook no longer fires on compaction.** `source == "compact"` exits immediately. It was re-injecting the preamble and digest every time context was compacted — spending a few hundred tokens restating what the conversation already knew, at exactly the moment context was scarcest.  **Where I departed from RESET-PLAN 7.1, deliberately.** The plan assigns "when to write" to the skill and says the hook should point at it. Two instructions stay in the hook anyway — "record it, don't offer to" and "claim the task before you start" — because a skill is model-invoked and thirty headless sessions with `keel` installed invoked it zero times (TQ-19). An instruction that only lives in a file nobody opens is not an instruction, and moving those two into the skill would have undone the measured improvement that produced them. The skill keeps everything that elaborates: what belongs where, the artifact vocabulary, conflict handling. One authority per instruction is preserved; the assignment differs from the plan's for one item, and the reason is on the record. <sub>— claude · 2026-08-10</sub>

- **KEEL-85** Speak English in the interface `p2` · desktop, phase6

  RESET-PLAN 6.6. The internals are showing. Remove from the UI: raw ULIDs as primary labels with a tooltip telling you to click and copy; the `digest ≈ 12,400 tokens` readout and its "over budget" warning, which is an agent-context measurement on a human's dashboard; tool names in empty states ("Ask Claude to write one — keel_write_doc"); CLI instructions ("run `keel fixture` against a scratch store"); internal decision references like "D-10" in tooltips. "Unattributed" becomes "written outside a tracked session". The digest size readout moves to a diagnostics screen or disappears.

  - Done. Five pieces of the build showing through, removed:  - The `digest ≈ 12,400 tokens` readout and its "over budget" warning. Both measure what an *agent* reads, in a unit only an agent has — a token count on a human's dashboard is a number nobody can act on. - "Ask Claude to write one — keel_write_doc" → "Ask Claude to write it." - "run `keel fixture` against a scratch store" → gone; an empty store should not answer with a CLI command. - "unattributed", with a tooltip citing decision D-10, → "written outside a tracked session", with a tooltip that says what it means rather than which decision permits it. - The `Id` component's "Click to select, then copy" tooltip. An interface that has to tell you how to use a piece of text is showing you something it should not have had to.  Also: the activity feed no longer prints each event's ULID. The summary already names what changed — "created task 'X'" — so the id was a second identifier for the same thing and the only one a person could not read. And the task page's `Id` row is labelled "Internal id", which at least says why it is a wall of characters.  Verified by sweeping every screen for the offending strings: none left.  **What is left is content, not chrome, and it is real.** The dashboard's open-questions card is a wall of `TQ-8`, `TQ-24`, `Q-4` — and the glossary still defines "Digest" as "budgeted to roughly 3–4k tokens". Those are rows in the store, not interface copy, so this task could not fix them. They are exactly what KEEL-92's question prune and document reset are for, and they are the most conspicuous remaining thing on the screen a human opens. <sub>— claude · 2026-08-10</sub>

- **KEEL-84** Make STATUS.md safe to render, and worth reading `p1` · cli, risk, phase6

  RESET-PLAN 6.5 and KB decision 5. `keel render-status --out` overwrites its target unconditionally — no comparison, no safety check, no backup — and resolves the project by name, slug or ID with fuzzy matching. Point it at a near-duplicate project by accident and it silently writes that project's near-empty tracker over the real one. That is a genuine data-loss path. Compare before writing, require an exact project match, and refuse to write a dramatically smaller file without saying so. Then render readable IDs and the "what's next" section into it — the best computation in the system is missing from the file a human actually reads.

  - Done. `render-status --out` can no longer quietly replace a tracker:  - **An ambiguous project reference is refused**, naming the candidates, instead of resolving to whichever matched first. Two projects sharing a *name* cannot exist — near-duplicate detection catches that on create — so the reachable collision is one project matching by slug while another matches by name, and that is what the test uses. - **It compares before writing**, and says "already up to date" rather than rewriting. - **It refuses a render less than half the size of the file it would replace**, naming both sizes and suggesting the project argument is wrong. `--force` overrides. A tracker does not lose half its content by accident. - **`## What's next` is now in the file**, at the top, before the ninety-three rows — the best computation in the system appeared everywhere except the document a human opens.  Found by my own test: the comparison had to strip the generated banner, because the banner carries a generation timestamp. Comparing bytes would have meant "already up to date" never fired and every render dirtied the tree. That is the same rule `keel generate --check` uses; using a different one here is how the two would have disagreed about whether a file had changed. <sub>— claude · 2026-08-10</sub>

- **KEEL-83** Write closed_at, and backfill it from the event log `p1` · correctness, storage, phase6

  RESET-PLAN 6.5. `closed_at`, the field that records when a task finished, is never written by any live code path. Every done task in the store has no completion date, so nothing about throughput, cycle time or "what closed this week" is computable. Set it when a task reaches a terminal status, and backfill from the event log for tasks already closed.

  - Confirmed while reconciling the stale rows: `closed_at` accepts a write through keel_update and round-trips fine. So the column and the API are already there — what is missing is only the automatic set on a terminal status transition, plus the backfill. The four rows closed today have it set by hand. <sub>— claude · 2026-08-10</sub>
  - Done. `closed_at` is written when a task reaches `done` or `wont_do`, cleared when it comes back out, and an explicit date in the same call wins — which is the case that matters for recording a close that happened earlier than the write.  Backfilled from the event log, which had the answer all along: the most recent status change into a terminal state. All 77 closed tasks in the live store now carry a date; before this, none did. A task closed by a path that left no event falls back to `updated_at` — later than the truth but bounded by it, and a date that is roughly right beats a null that makes every question unanswerable.  "What closed this week" is now a question the store can answer, which it could not this morning. <sub>— claude · 2026-08-10</sub>

- **KEEL-82** One definition of blocked, and the same numbers everywhere `p1` · correctness, phase6

  RESET-PLAN 6.5. Two parts of the system decide what "blocked" means and they disagree: the generated STATUS.md counts a task as blocked if its status field says `blocked`; the what's-next ranking counts it blocked if something links to it with a `blocks` edge. A task can be one and not the other, and then the file and the app state different facts with nothing noticing. Pick the links as authoritative — they say *what* is blocking — derive the rest, and make fsck fail when a task says `blocked` with nothing linked to it, or is linked-blocked while claiming `todo`. Then make the app, the digest Claude reads and the generated STATUS.md count open, urgent and blocked identically.

  - Done, and KB's answer to TQ-25 made it the stronger version: `blocked` is gone as a status, not merely reconciled with the links.  One function, `next::blocked_tasks`, is now the only definition — a task is blocked when something live links to it with `blocks` — and the ranking, the digest's counts, the generated tracker and the board all call it. Before, three of those computed it themselves and two of them used the status column.  Verified against the live store after migrating: the digest reports `blocked=3`, `next_up.blocked` has exactly those 3, and every one names its blocker. The two phantoms are gone — KEEL-45 and KEEL-48 used to appear as "marked blocked, but nothing links to it with `blocks`", and that sentence no longer exists because the state it described cannot be written down. Migration 8 moved them to `todo`, which is what they always were.  The board keeps a blocked column; it is derived, sits first, and a blocked task appears there *instead of* under its status rather than in both — showing it twice would double every count on the screen.  The test that used to assert the contradiction was reported is now a test that the contradiction cannot occur. That felt like the right trade: the old one guarded a behaviour that only existed because the data model allowed a mistake. <sub>— claude · 2026-08-10</sub>

- **KEEL-81** Sub-tasks — a parent link between tasks `p2` · storage, desktop, phase6

  RESET-PLAN 6.4. A parent shows its children with a progress count; a child shows its parent; rollups come from that. Currently impossible: the only edge available is `blocks`, which means "must happen first", not "is part of". A field on the task, not a new artifact type.

  - Done. `tasks.parent_id`, a column rather than an edge — and that choice is the substance of the task.  `blocks` means "must happen first"; composition is a different statement. Modelling "is part of" as a blocking edge is what made rollups impossible, and it would have corrupted the ranking, which reads every inbound `blocks` as something in the way. The detail view now shows both for the same pair: KEEL-92 has KEEL-91 under "Part of — sub-tasks, 0 of 1 done" *and* under "Connected — blocked by". Both true, different things.  Three ways to make an unrenderable tree, all refused on the way in, because the store is the only place that can see the whole chain: a task as its own parent, a cycle of any length, and a parent in another project. Depth is capped at six — deeper than that is a milestone wearing a task's clothes, and a bound is what stops a malformed chain becoming an unbounded walk while something is trying to render it. Two fsck checks audit the same three things for the state a restore can leave.  Found while testing it on real data: `parent_id` took an id but would not take `KEEL-92`, so setting a parent by the readable identifier failed with "no task with id KEEL-92 exists" — about a task that plainly does. A readable identifier is only an identifier if it works everywhere one is taken, so `keel_update` now resolves reference-bearing fields the same way it resolves `id`.  Grouping by parent is in the board and the list, with tasks whose parent is not in the filtered set still grouped under it — narrowing to open work should still say which epic each piece belongs to. <sub>— claude · 2026-08-10</sub>

- **KEEL-80** Task rank — a deliberate order, not reverse-creation-order `p2` · storage, desktop, phase6

  RESET-PLAN 6.4. Add a rank to tasks so a column has a deliberate order. Since the app is read-only, rank is set by Claude ("move that above the auth work") and respected by every view; the app can still offer local sort options on top. A field, not a new artifact type.

  - Done. `tasks.rank` is a DOUBLE, backfilled from the task number so the starting order is creation order rather than arbitrary.  The part worth keeping is the surface, not the column. A model should never have to pick a float, so `keel_update` takes `rank_after` or `rank_before` naming another task — `{"rank_after": "KEEL-87"}` — and the store resolves it to the midpoint of that task and its neighbour. Verified on the live store: KEEL-82 moved after KEEL-87 and landed at 87.5.  A float rather than an integer because a move should touch one row. Renumbering touches every row below the insertion point, and on a store where the daemon owns the single write path that is a lot of events for "put this one higher". The known limit is that repeated midpoints between the *same* pair exhaust float precision after roughly fifty inserts; at a few thousand rows hand-ordered by one person that is out of reach, and `rank_between` refuses rather than inventing a value when two neighbours are already equal.  New work lands at the end, not the start: a task nobody has placed has not been prioritised, and putting it on top would be the store making that claim on someone's behalf.  The app: `sort=rank` is the deliberate order and `sort=next` is the digest's ranking. They were briefly the same word, which is exactly the kind of collision nobody reports — a sort that silently means one when you asked for the other. Two tests assert they differ. <sub>— claude · 2026-08-10</sub>

- **KEEL-79** Search results that go somewhere `p1` · desktop, phase6

  RESET-PLAN 6.3. Search results are currently dead text — a hit tells you what was found and gives you no way to reach it. Every hit becomes a link to the thing it found.

  - Done. Every hit is a link: tasks to their own page, the five prose types to the document reader, milestones to the roadmap, and everything else to its project. Verified against the live store — a search for "readable identifiers" returns 17 hits, all of them reachable.  The routing needs the project *slug* and a hit carries only `project_id`, so the screen resolves it against the project list it already fetches for the scope menu. Where that cannot resolve, the link goes Home rather than nowhere: landing on the right project is a worse answer than landing on the row and a better one than a dead link, which is what every hit used to be. <sub>— claude · 2026-08-10</sub>

- **KEEL-78** Filters that compose and survive, encoded in the URL `p1` · desktop, phase6

  RESET-PLAN 6.3. Today there are exactly two filters, they don't combine, and they vanish on reload. Replace with a proper filter bar: multiple labels, status, priority, kind, milestone, has-blockers, free text. Encode the filter in the URL so a filtered view *is* a shareable link — that gives saved views for free. Also: the ranked Next list is the best thing in the app and it currently disappears the moment any filter is applied. Fix that.

  - Done. `lib/filters.ts` holds the model — status, priority, kind, labels, milestone, has-blockers and free text — and every control writes it to the query rather than to component state, so a filtered view *is* a link and Back undoes a filter change the way it undoes anything else. Bookmarking one is what "saved views" turns out to mean.  Three decisions worth keeping:  **OR within a facet, AND across them.** Any other reading makes "status: todo, blocked" mean nothing.  **A default is written as the absence of a parameter.** An unfiltered board is `#/projects/keel/board`, not a URL trailing four parameters that say "as usual" — otherwise a bookmarked view and a freshly built identical one look like different pages.  **"Has blockers" asks the graph, not the status field.** `blockedIds` comes from the digest's `next_up.blocked`, which is the link-based definition the ranking already uses. The app must not grow a second opinion about what blocked means before KEEL-82 settles it.  Also fixed the regression the plan named: the ranked Next panel used to vanish the moment any filter was applied, so the best thing in the app was only on screen when you did not need it. It now stays, and a test holds it.  The free-text box is the one piece of local state: it commits on submit, because a history entry per keystroke makes Back useless. <sub>— claude · 2026-08-10</sub>

- **KEEL-77** A list view beside the board, with grouping and sorting you choose `p1` · desktop, phase6

  RESET-PLAN 6.3. Keep the board, add a sortable, dense, scannable list/table — most real tracker work happens in a list, not a kanban. Grouping is hardcoded to status today; let it group by status, priority, milestone, label or parent. Sorting is hardcoded and subtly broken: it compares priorities as text, so a task with no priority sorts under the literal word "undefined".

  - Done. The board stays and a list joins it, both fed by the same grouped-and-sorted data — `?view=list` is the only difference in the address. Group by status, priority, milestone, label or nothing; sort by rank, priority, status, updated, title or number, either direction, and in the list the column headers do it with the reverse-on-second-click every table has.  Sorting is no longer subtly wrong: it compares typed keys, so a task with no priority sorts last instead of under the literal word "undefined". That was only ever visible as a column whose order looked arbitrary.  Two things the work decided:  **Grouping by label puts a multi-labelled task in every one of its groups.** The alternative is picking one label arbitrarily and hiding the task from the other two. The consequence is that group sizes sum to more than the task count, which is why the header counts *distinct* matches — grouping by label on this project shows 36 tasks across 21 columns.  **A board asked for no grouping degrades to status.** A board with one column is not a board; offering the option and then silently ignoring it would be worse than visibly falling back.  Empty columns are kept rather than dropped — an empty `review` column is the information that nothing is in review.  Not done, because the field does not exist yet: grouping by parent. That arrives with KEEL-81. <sub>— claude · 2026-08-10</sub>

- **KEEL-76** Readable identifiers — KEEL-42, not tsk_01KZKW28CS… `p0` · storage, desktop, phase6

  RESET-PLAN 6.4. Every project gets a short key; every task gets a number. Displayed everywhere, searchable, usable in conversation with Claude, and usable in the generated STATUS.md so that file becomes readable too. The long ULID stays underneath as the real identity; the short one is a label. A field on the task, not a fourteenth artifact type — the thirteen-type ceiling holds. KB confirmed 2026-08-10.

  - Done — and this note was written by addressing the task as `KEEL-76`, which is the point.  `projects.key` (derived from the slug, `keel` → `KEEL`) and `tasks.number` (assigned in creation order, unique within the project). Migration 6 backfills both: keys de-duplicated by suffix so two projects reducing to the same letters get `KEEL` and `KEEL2` rather than failing the unique index and leaving the store unmigratable, and numbers in ULID order so `KEEL-1` is the oldest task and the sequence reads as the project's history.  Nothing stores the composed string. `KEEL-42` is assembled wherever both halves are in hand, so re-keying a project does not mean rewriting every row that mentions it. The ULID stays the identity — links, events, notes and documents all still point at it.  `resolve_ref` accepts either form, so every tool that takes an id now takes a reference: get, update, write_doc, note, link, and activity's `entity`. The daemon's path parameters too, which is why the app's URLs are `#/projects/keel/tasks/KEEL-76`.  Where it shows: the digest's "Next" list, `product/STATUS.md`, board cards, the detail view's title and properties, and the palette — where typing `KEEL-76` finds the task, ranked below every title match so an identifier can never displace a search for real words.  Two things worth knowing:  **A number is never reused, and a test holds it.** If an archived task's number were handed on, `KEEL-1` would keep resolving and quietly start meaning something else — every note and commit message that used it silently wrong. `MAX(number) + 1` counts archived rows.  **The unique index is on `upper(key)`, not `key`.** References resolve case-insensitively, so `KEEL` and `keel` are one identifier; a plain unique index would have allowed both to exist as separate projects and left the lookup picking one arbitrarily. <sub>— claude · 2026-08-10</sub>

- **KEEL-75** The task detail view `p0` · desktop, phase6

  RESET-PLAN 6.2, the centre of the phase. Today a card is a dead end: plain <article> elements with no click handler, no link, no hover state, not even keyboard focus, and no task detail view anywhere in the app. The function that would fetch what a detail view needs — the task, its body, its linked neighbours — is written, tested and called from nowhere. Clicking a card opens the task at its own URL, showing: readable ID and title; status, priority, kind, labels and milestone as a properties panel rather than a row of badges; the description as markdown; the note stream as a real activity feed with full markdown, chronological, attributed, retracted notes struck through rather than hidden; relationships stated in English and every one of them clickable; the task's own history from the event log, which has never been shown anywhere; and its external links, now more than one. J/K move between tasks without leaving the view, Esc goes back.

  - Done. `apps/desktop/src/screens/Task.tsx` at `#/projects/:project/tasks/:taskId`. Description as markdown; a properties rail with the milestone by name rather than a ULID; the note stream as a real feed with full markdown, session attribution, and retracted notes struck through rather than hidden; relationships stated in English and clickable; the task's own history with before → after. Board cards are now links — 92 of them, verified against the live store — and the palette goes to a task's own page instead of the board.  Two things it needed from the server, both of which were defects rather than gaps:  **Traversal results carried no label.** `Neighbour` was `{id, entity_type, rel, anchor, depth, path}` — so anything rendering the graph had to re-fetch every hop to learn what it had found, and the document reader had simply given up and printed ULIDs under "Connected". Now joined through `v_entities`, which is the view that exists for exactly this. A dangling edge yields an empty label and keeps the row, because dropping it would hide the breakage `fsck`'s dangling-link check reports.  **The event log could only be read a whole project at a time.** "How did this task get here" meant paging the project feed and filtering, which silently misses anything older than the page. `events_for` is now a store method and `keel_activity` takes an `entity` id.  Deliberately not done: more than one external link. That is an `Option<String>` → list change on the tasks table, so it is a storage-format change, and KB's approved field additions were the readable id, rank and parent. Raised as a question rather than assumed. <sub>— claude · 2026-08-10</sub>

- **KEEL-74** Cmd-K command palette `p1` · desktop, phase6

  RESET-PLAN 6.1. Cmd-K opens it; type to jump to any project, task, document or search. The single feature that most makes an app feel like Linear. App.tsx currently discards every modified keypress — `if (e.metaKey || e.ctrlKey || e.altKey) return;` — so that restriction has to be lifted before Cmd-K can be seen at all.

  - Done. Cmd-K (and Ctrl-K) opens a palette over projects, screens, documents and tasks, ranked in four tiers — prefix, word start, substring, subsequence — so "boa" offers Board before it offers a task that merely contains b, o and a in order. The modifier check in App.tsx was `if (e.metaKey || e.ctrlKey || e.altKey) return`, which discarded every modified keypress before anything could see it; narrowed to the combinations the app does not claim, so the system's and the browser's shortcuts are still theirs.  Worth knowing: a task has no page of its own until the detail view lands, so selecting one opens the board at `?task=<id>` with the card outlined and scrolled to. That is the honest half-step — the palette genuinely reaches a task today — and the route becomes the task's own address in the next step. Contents are fetched when the palette opens, not when the app mounts; every task in the store on launch for a dialog nobody has opened is a request for nothing. <sub>— claude · 2026-08-10</sub>

- **KEEL-73** A real design system — named type scale and the missing primitives `p0` · desktop, phase6

  RESET-PLAN 6.1. Eleven different font sizes with no scale and no names; six hand-copied duplicates of the same filter-chip styling that have already drifted apart; no Button component at all, so every clickable thing re-declares its own appearance. The colour system is good and consistent; everything above it is ad hoc. Name the type scale — six sizes, not eleven. Build Button, Chip, Input, Menu, Dialog and Tooltip. Replace the six filter-chip copies with one. Fix the light theme, which keeps its dark-tuned status colours and washes out.

  - Done. Button, Chip, Input, Menu, MenuItem, Dialog and Tooltip added to components/ui.tsx; the six hand-copied filter chips are now one `Chip`. Type scale named — eight tokens in two scales, and `grep "text-\["` over src/ returns nothing but a comment. Light theme fixed on :root rather than in a nested @theme, status colours included.  Two things the work turned up that were not in the plan. The `Menu` earned itself immediately: the document reader was rendering one chip per revision and then a second identical row of them for the comparison, so a twelve-revision document produced twenty-four controls of equal weight to answer a two-part question — now two menus. And search scope was only reachable when a project happened to be selected elsewhere in the app; making scope part of the address meant global search needed a project menu, which it now has. <sub>— claude · 2026-08-10</sub>

- **KEEL-72** One page shell for every screen `p0` · desktop, phase6

  RESET-PLAN 6.1. Seven screens built to seven different rules: the board is full-bleed with horizontal scroll, Project and Home are centred at one width, Roadmap, Search and Activity at a narrower one, Documents is a two-pane split. No shared header, no breadcrumb, no consistent title bar. One shell used by every screen — same header, same breadcrumb, same title area, same width behaviour.

  - Done. `components/Page.tsx` owns the title, the breadcrumb, the toolbar row and the width; all seven screens go through it. Three widths, not seven: `standard` (a reading measure), `wide` (dashboards, where the unit is a card rather than a sentence) and `full` for the two screens that genuinely own their own scrolling — the board's columns and the document reader's split. The breadcrumb is built by one helper, `projectCrumbs`, so no screen can quietly disagree about what the trail says. <sub>— claude · 2026-08-10</sub>

- **KEEL-71** Routing and URLs — every screen, project, task and search has an address `p0` · desktop, phase6

  RESET-PLAN 6.1. The app has no router: which screen you are on and which project is selected are two variables in memory. There is no URL for a project, a board, a task or a search, so you cannot link to one, bookmark one, or press Back, and a refresh drops you at Home with nothing selected. Add routing so every screen, project, task, document and search has a URL, Back and Forward work, and a refresh keeps you where you were. Linear's whole feel rests on every issue having an address.

  - Done. Hash routing, hand-written, in apps/desktop/src/lib/router.ts — eleven patterns, a query string, and `useRoute`/`href`/`navigate`/`setQuery`. Reasoning in the decision "The desktop app routes on the hash, and the router is hand-written": Tauri's asset protocol has no index.html fallback, so a path router would have 404'd on reload in the packaged build only, which is a bug you would ship. Filters, search queries, the document revision and the diff comparison all live in the address, so a diff is now a link — verified against the running daemon at `#/projects/keel/documents/spc_…?diff=3`. Two properties are held by tests rather than by care: toHash/parseHash round-trips for every route the app can build, and an unmatched address falls back to Home while keeping its query. <sub>— claude · 2026-08-10</sub>
  - Found while wiring it: the address could disagree with the screen in two ways, and both look like a place you could send someone. `#/board` matches nothing (only `/projects/:p/board` exists) so it rendered Home under a board URL; and a project-scoped screen reached without a project rendered an empty state under a URL that promised content. App now canonicalises — if `location.hash` differs from `toHash(route)` it is rewritten with `replace`, so neither becomes a Back destination. It cannot loop, because the round-trip property means the corrected address parses to the same route. <sub>— claude · 2026-08-10</sub>

- **KEEL-70** Keyboard navigation `p2`

  - Digits switch screens, `/` jumps to search. **Found and fixed: the navigation keypress leaked into the newly-focused search input** <sub>— human · 2026-08-10</sub>

- **KEEL-69** Typed API client and live refresh `p2`

  - Relative paths in dev (Vite proxies), absolute baked in at build time for the webview. One bundle, different base URL, as SPEC §10 wants. SSE `lagged` is surfaced rather than swallowed <sub>— human · 2026-08-10</sub>

- **KEEL-68** Session-ID threading `p2`

  - Third section of the skill, with the self-check: `keel_context` echoes the id back, and `null` means it is not threading <sub>— human · 2026-08-10</sub>

- **KEEL-67** POST /api/generate `p2`

  - The daemon owns the store, so it owns generation. Resolves the read half of TQ-12; writes from the CLI still need the daemon down, which is now only `keel import` and `keel bootstrap` — both one-time migrations <sub>— human · 2026-08-10</sub>

- **KEEL-66** Check that ID references in artifact bodies resolve `p1` · storage, correctness

  Fixed as an fsck check, unresolved_id_reference.
  
  It scans document bodies for B-n, D-n, Q-n, TQ-n, R-n and P0-n style citations and resolves them against every live entity in the same project via v_entities - not just documents, because an artifact created without a body has no document row and the first version missed most real targets that way, reporting 227 dangling refs in a store of ~250 artifacts.
  
  The scoping that makes it usable: a family is only checked where the project actually titles artifacts that way. Keel's own B-n decisions live in a numbered table inside a prose document, never as titles, so unscoped every citation of them dangles by construction - 182 findings, none a real break.
  
  Scoped, it found six genuine ones: TQ-12 and TQ-13 cited in three documents after those rows were dropped when the open-questions table was edited. Both restored as answered questions, and fsck is clean.
  
  Honest limitation: it does NOT catch the case that motivated it. The Pellet question citing 'append-only (D-9)' is not caught, because no project titles artifacts D-n - those are SPEC decisions living in prose. A purely lexical check cannot tell 'cites a convention documented in prose' from 'cites nothing'. Catching that needs the prose decision log modelled as artifacts, which is the enrich-vs-collapse question.

- **KEEL-65** Near-duplicate titles defeat the idempotency key `p1` · storage, mcp

  Fixed. create now looks for a near-identical title in the same project and type after the exact key misses, and returns the existing artifact with created:false.
  
  The rule is overlap PLUS containment, and containment is what makes it safe: one token set must be a subset of the other, so the difference can only be added words, never substituted ones. 'Validate phases to 0-360' against '...0-360 degrees' is an addition; 'Open question number 4' against 'number 5' is a substitution.
  
  Overlap alone was wrong and a test caught it: sixty questions differing by one digit scored 0.875 similarity and collapsed into two rows. High overlap says 'mostly the same words'; only containment says 'one adds nothing the other lacks'.
  
  An explicitly supplied idempotency key skips the check entirely - that is the caller asserting two same-titled things are different, and guessing over an explicit assertion is the false merge that hides work. Terms, metric observations and artifacts are excluded, terms because Q-4 requires a global and a project-scoped one of the same name to coexist and the COALESCE index already says so exactly.
  
  Seven tests, including the exact pair the two gate runs produced and the sixty-question case that found the flaw.

- **KEEL-64** Step 10 — hand-judge 20 writes for keep-rate `p0` · gate, measurement, decision-needed

  The gate criterion is pure recall and it now reads 18 of 20. That is exactly the condition under which a recall-maximiser scores well while making the store worse, so precision must be measured before anything raising write frequency ships.
  
  Material exists: Runs B and C produced roughly 30 artifacts, recoverable per session from the archived transcripts and stores.
  
  Two findings from my own pass, which is NOT the independent judgement this task needs. Four sessions called create twice with an identical title - idempotency deduplicated them, so REQ-7 worked on organic traffic for the first time, but the retry suggests the success response is not clearly acknowledging the write. And s6 produced three tasks from a prompt about one validation, the first organic instance of R-6 write amplification.
  
  The panel is explicit that this must not be delegated to an LLM judge until roughly 30 sessions are hand-labelled with agreement measured by Cohen's kappa; below about 0.7, don't.

- **KEEL-63** Step 6 — deterministic Stop hook for sessions that recorded nothing `p0` · plugin, gate

  Re-argued against the residual it actually addresses, per TQ-21.\n\nThe justification in WAY-FORWARD.md was the closing-message boundary: an offer is composed after tool-calling has ended, so no amount of 'record it the moment it happens' can reach it. Run A has one offer in ten sessions. That justification is dead.\n\nThe surviving one is better. Three sessions - s2, s7, s9, now confirmed by KB as genuine misses - never noticed Keel at all while heads-down on implementation work. A Stop hook fires exactly when the session knows what became true and the work is finished. It is a forcing function at the only moment that has full information.\n\nDesign, deliberately narrow:\n- No model call. Deterministic text.\n- Guard on stop_hook_active so it can never loop.\n- Fire at most once per session.\n- Fire ONLY if the session recorded nothing. A session that already wrote must not be nagged; that is how a forcing function turns into noise and gets disabled.\n\nThe last point is the one that makes it safe: it is silent for the seven sessions that already do the right thing, and speaks only to the three that did not.

- **KEEL-62** Step 3 — Run A: baseline against the repaired instrument `p0` · gate, measurement

  Done 2026-08-10. 7 of 10, recall == ceiling == 70%, one offer in the whole run (was eleven). Full write-up in product/RUN-A.md. The permission failure the plan was built around is not visible; the residual is three sessions that never noticed Keel during pure implementation prompts, which is a different problem needing a different fix.

- **KEEL-61** Step 2 — repair the instrument before any further run `p0` · gate, measurement

  From WAY-FORWARD.md Step 2, plus one addition promoted by the Step 1 audit. Do not run another gate until this is done.
  
  From the panel:
  - Known-answer fixtures: ten canned transcripts with zero writes must score 0/10, ten with writes must score 10/10. Run before every real run.
  - Assert writes + silent == sessions_launched, abort on violation.
  - Parallelise, one scratch store per session. Largest power multiplier available and removes DuckDB lock contention.
  - Add one neutral continuation turn per session, e.g. "ok, what about the caching thing" - instrument repair, not treatment, since the single-turn harness is why offers had nowhere to land.
  - Assert the store was reachable for the duration of each session, so a wedged daemon is not scored as a non-write.
  - Archive transcripts, tool lists and event log under a run id. Teardown becomes a separate manual step.
  - Replace the binary score with the L0-L5 ordinal rubric; report recall and ceiling and offer count.
  
  Added by the audit, and it is the one that corrupted the headline number:
  - Inject the session id from the launcher rather than asking the model to mint one. The --sessions fix corrected the denominator but the numerator is still distinct model-minted ids, so a session writing under a colliding id remains invisible. The SessionStart hook already receives the payload and can stamp a unique id per session.
  
  Also free, and it should happen here: the L0-L5 rubric can be scored retrospectively against all 41 archived sessions with no new runs. That is a labelled corpus for Step 10's hand-judging, available before Run A rather than after.

- **KEEL-60** Step 1 — validity audit of the gate runs `p0` · gate, measurement

  Done 2026-08-10. Zero build, as specified. Output is product/VALIDITY-AUDIT.md (spc_01KZMGZ01R1JCB09WKKJXFSGRC).
  
  All five checks completed against the archived Claude Code transcripts, which survived teardown — 41 gate sessions recoverable in full, tool calls and results included.
  
  Headline: run 4 was 5 of 10, not 3. keel gate counts distinct session_id values, five sessions wrote, and two pairs collided on date-based ids. Runs 1-3 were reported accurately, because the collision can only bite when more than one session writes. The trend is 1 - 0 - 0 - 5.
  
  Check 1: no keel tool was ever denied at the permission layer. The allowlist confound is dead without spending a run, which was the point of the check. Two writes failed on validation instead - priority "high" and "medium" against an enum of p0-p3 - and both retried successfully.
  
  Check 2: no --permission-mode flag, no .claude/ in either scratch project.
  
  Check 3: harness is single-turn, confirmed. The panel's central claim holds.
  
  Check 4: all four post-run-4 fixes landed 18-84 minutes after the run. One correction to the panel: a one-sentence anti-asking instruction WAS live during run 4, so the weak form has a number and it is 5, not 3. The expanded form is unmeasured.
  
  Check 5: transcripts survived. They are the archive of record, not the tee'd logs.

- **KEEL-59** SessionStart hook: inject keel_context instead of hoping the skill loads `p0` · plugin, gate

  Built, installed, and measured. plugin/hooks/session-start.sh, registered in plugin/hooks/hooks.json and ~/.claude/settings.json (backup at settings.json.bak-keel).
  
  Gate re-run against it, 10 sessions, scratch store with both projects pre-created so the cold-start question (TQ-17) was held constant:
  
      3 of 10 wrote  (was 0-1 across three runs before the hook)
      every write attributed: yes
      duplicate projects: 0
      FAIL - needs 9 of 10
  
  So the hook roughly tripled the write rate and is nowhere near the bar. Orientation is solved: sessions now arrive knowing what the project is. Writing is not: 7 of 10 read the digest and still recorded nothing.
  
  That is the useful narrowing. The failure has moved from 'never heard of Keel' to 'oriented and chose not to record', which is a judgement problem in the remaining SKILL.md rather than a plumbing problem. Worth noting the sessions that did write minted readable ids of their own (tideline-2026-08-09) and wrote 1-3 artifacts each - when they engage, they engage properly.
  
  Also fixed a scorer bug this exposed: a session that writes nothing leaves no event, so the denominator was the number of sessions that appeared in the log rather than the number run. It read '3 of 3' after seven silent sessions. keel gate now takes --sessions (default 10) and reports the silent count explicitly.

- **KEEL-58** Daemon wedge: SIGKILL mid-write corrupted a DuckDB ART index `p0` · daemon, storage

  The daemon gets into a state where every entity read fails with "count matching rows" / "run a <type> lookup" while the data is provably fine — with the daemon stopped, the CLI reads the store normally and `keel fsck` reports clean across 27 checks.
  
  Seen twice. First after archiving a project. Second later in the same session, after which a restart did *not* immediately clear it: health answered but every list still failed, and only a second restart recovered it. That suggests a race on startup — if the previous process has not released the DuckDB lock, the new one comes up with something half-initialised (the fts_entities rebuild is the prime suspect) and then fails every query that touches it, without logging anything more specific than a 500.
  
  What makes it p0: the failure is total, not partial. An agent mid-session gets storage errors on every call with no indication of the cause, and the obvious remedy — restart — is not reliably enough on its own. Soft delete is supposed to be a routine operation, and D-9 makes it the only kind of deletion this store has.
  
  crates/keel-core/tests/archive.rs covers the store layer and passes, which is what localised this to the daemon rather than the data. Needs: the real DuckDB error surfaced instead of a generic context string, a startup check that the FTS index is usable, and a shutdown that waits for the lock to be released.

- **KEEL-57** Route the remaining read commands through the daemon API `p1` · daemon, cli

  From TQ-15, which found that nothing can read the store while the daemon holds the write lock. `keel generate` goes through `POST /api/generate` and works with the daemon up. `keel fsck`, `keel status` and `keel render-status` still open the store directly and fail with a conflicting-lock error whenever the daemon is running — which is always. `fsck` is the one that matters: an integrity check you cannot run without stopping the thing you want to check is not much of a check. Each needs an endpoint, or one generic read-through. Also correct SPEC D-5, which promises a read-only connection that DuckDB does not offer.

  - Another cost of the same gap, hit today: there is no cheap way to append to a long generated document. `keel_write_doc` takes the full body — correctly, since a revision is a snapshot — so adding one section to the 35 KB JOURNAL.md means retransmitting all of it through the tool. The alternative, hand-editing the file and running `keel import`, needs the daemon stopped, which is the thing this task fixes. The practical effect is that the session narrative gets written as notes on rows instead of into the journal, which is the opposite of what the standing rules ask for. An append endpoint, or `import` working against a running daemon, closes it. <sub>— claude · 2026-08-10</sub>
  - Done. `fsck`, `status` and `render-status` now read through `GET /api/fsck`, `/api/status` and `/api/render-status`, falling back to opening the store directly when no daemon answers — the same shape `generate` has had since B-21.  Verified both ways, and the outputs are identical: with the daemon up (which used to be a conflicting-lock error) and with it stopped.  Two details worth keeping:  **"No daemon answered" and "the daemon said no" are different, and are treated differently.** A connection failure falls back, because nothing holds the lock and opening directly is then correct. An HTTP error is raised with the daemon's own message. Falling back on a real error would have produced a confusing lock error a moment later and hidden the actual cause — which is the failure mode this whole task exists to remove, just relocated.  **The project reference is percent-encoded by hand** rather than adding a crate. A slug or key is safe, but a project *name* can hold a space or an ampersand, and an unencoded one truncates the query and renders a different project's tracker. Silent and wrong, which is the class this codebase treats as the dangerous one.  The tests pin `NO_DAEMON` to a port nothing listens on rather than leaving the default. A test that passes only when the developer's daemon happens to be stopped is one that fails in CI for reasons nobody can see.  SPEC D-5's wording was corrected earlier today under KEEL-92, so the other half of this task was already closed.  **Not routed, deliberately:** `backup`, `restore`, `fixture`, `import` and `bootstrap` still open the store directly. They are writes or whole-store operations, and the hard constraint is that the daemon owns the single write path — giving them endpoints would mean write endpoints on the daemon, which is exactly what the read-only app decision forbids. They are also all commands a person runs deliberately, with the daemon stopped, which is the documented flow for `import`. <sub>— claude · 2026-08-11</sub>

- **KEEL-56** Render the tracker from the task rows, not stored prose `p1` · cli, dogfooding

  TQ-14, and the last half-step of the dogfooding switch. keel render-status works and produces a real tracker, but the task rows carry no per-task notes, and those notes are most of what makes product/STATUS.md worth reading. Rendering it today would trade rich prose for a bare task list. The work: migrate the per-task notes into task body fields, teach the renderer to emit them, then archive the prose artifact so one thing owns the file. Nothing structural. Until it is done, the task rows and the tracker can disagree.

  - Done. Task rows now carry an append-only, session-attributed note stream (migration 5, `notes` table) — not a fourteenth artifact type, the same class of thing as `links` and `events`. All 50 per-task notes lifted out of the tracker's prose onto their rows: 39 matched by exact title, 7 by hand map, 4 had no task row at all and got one. `product/STATUS.md` is now rendered by `keel render-status` and the tracker document is archived. <sub>— human · 2026-08-10</sub>
  - Found while doing it: the renderer printed a 'Generated at HH:MM' row into the table body. `generate --check` strips only the banner comment, so that row would have made the check fail at random on a minute boundary — a red CI that means nothing. Removed; a test now asserts two renders are byte-identical. <sub>— human · 2026-08-10</sub>
  - Also found: STATUS.md held 17 session narratives that no task row could ever carry — what was tried, what broke, what a measurement said. Rendering from rows alone would have silently dropped them, so they were split into `product/JOURNAL.md`, which is a genuine document. The tracker is rows; the story is prose. <sub>— human · 2026-08-10</sub>
  - Verified over the wire: keel_note writes through MCP, and a 2025-11-25 client connects again. <sub>— claude · 2026-08-10</sub>
  - keel import fails against the daemon's write lock with only 'Error code 1: Unknown error code', and a generate immediately after then overwrites the hand edit from the stored copy — so the edit is lost twice over, silently. Hit this live while updating the standing rules. TQ-15 (route writes through the daemon) would close it; until then the error itself should name the lock. <sub>— human · 2026-08-10</sub>
  - Reconciled 2026-08-10: this row sat in `todo` while its own first note read "Done." — the work landed in commit 8d4dcf7 and the row was never moved. Moved to done, closed_at set to the render timestamp in STATUS.md. Named in RESET-PLAN.md as one of the stale rows. <sub>— claude · 2026-08-10</sub>

- **KEEL-55** Route reads through the daemon: POST /api/generate `p1` · daemon, cli

  Found by using it: nothing could read the store while the daemon held DuckDB's write lock. Tried the obvious fix first — a read-only connection — and it fails identically, because DuckDB blocks readers too while any writer holds the file. So D-5's "connect read-only" is not a thing that exists, and generation moved into the daemon, which owns the store. The CLI is a client, falling back to a direct open only when no daemon answers, which is safe precisely because nothing else holds the lock then. The spec correction is flagged as TQ-15, because the read-only connection it promises does not exist.

  - **Found by using it:** nothing could read the store while the daemon held the write lock. Tried the obvious fix first — a read-only connection — and it fails identically, because DuckDB blocks readers too. So D-5's "connect read-only" is not a thing that exists, and generation moved into the daemon as `POST /api/generate` (B-21). The CLI is a client, falling back to a direct open only when no daemon answers. Spec correction flagged as TQ-15 <sub>— human · 2026-08-10</sub>

- **KEEL-54** keel generate — the repo files become outputs `p0` · cli, mirror, dogfooding

  KB's call: Keel is the source of truth. A prose artifact records the repository file it is (mirror_path) and generation writes its body there verbatim under an HTML-comment banner. Verbatim because these documents carry their own heading and front matter; an HTML comment because product/CLAUDE.md is loaded by Claude Code exactly as it sits on disk. All seven product/*.md round-trip byte-identical — proved by generating over them and diffing: the only change was the banner. --check exits non-zero on drift, for a pre-commit hook. Found and handled: a path claimed by both a document and the tracker — neither is written and the conflict is reported, because letting the last writer win is how a file silently loses half its content.

  - KB's call: Keel is the source of truth. A prose artifact records the file it *is* (`mirror_path`) and generation writes its body there verbatim under an HTML-comment banner (B-20). All seven `product/*.md` round-trip byte-identical — proved by generating over them and diffing: the only change was the banner. `--check` exits non-zero on drift, for a pre-commit hook. **Found and handled: a path claimed by both a document and the tracker** — neither is written and the conflict is reported, because letting the last writer win is how a file silently loses half its content (B-22) <sub>— human · 2026-08-10</sub>

- **KEEL-53** Rendered markdown in the document reader `p1` · desktop

  The reader was showing document bodies as preformatted text, which made a real spec unreadable — the point of storing it. react-markdown + remark-gfm, with every element mapped by hand. Chosen over a string-to-HTML library because it does not render raw HTML by default: these bodies are written by a model, arrive from the store, and display same-origin with the daemon. Tables get their own scroll container; the decision log and tracker are almost entirely tables. Verified against the imported 51 KB SPEC.

  - The reader was showing bodies as preformatted text, which made a real spec unreadable — the point of storing it. `react-markdown` + `remark-gfm`, no raw HTML (B-19). Verified against the imported 51 KB SPEC: headings, fenced code, blockquotes and the §3.3 direction table all render <sub>— human · 2026-08-10</sub>

- **KEEL-52** keel import — whole markdown files into Keel `p1` · cli, dogfooding

  Puts a markdown file in as a versioned document. Resolves an existing artifact by title before creating one, and write_revision is content-addressed, so re-running on an unchanged file appends nothing — safe in a script. Records where the file came from as mirror_path, which is what closes the round trip. SPEC.md imports at 51,695 bytes and comes back byte-identical, searchable and diffable. Strips a generated banner before storing, or every import-then-generate cycle stacks another banner on the last.

  - KB asked whether specs could live in the store and be read in the app. They can: SPEC.md imports at 51,695 bytes and round-trips byte-identical, searchable and diffable. Re-importable and content-addressed, so an unchanged file appends no revision (B-18). All seven `product/*.md` are now in `~/.keel` <sub>— human · 2026-08-10</sub>

- **KEEL-51** Decide TQ-11: how long to carry the 2025-11-25 handshake `p3` · decision-needed, mcp

  Needed today. Worth revisiting once clients move on.

  - Reconciled 2026-08-10: decided, and decided twice. dec_01KZKMPVS9XTWRN7303BPY0F18 "Serve MCP 2025-11-25 alongside 2026-07-28" was the original call; commit 3d1cc27 then removed legacy support, and 2642f9a put both revisions back after a real client failed to connect. The answer is: carry it. Worth revisiting only when Claude Code stops opening with the legacy handshake — the RESET-PLAN item is to *advertise* both, since both are already accepted. <sub>— claude · 2026-08-10</sub>

- **KEEL-50** Decide TQ-10: BM25 in DuckDB rather than Lance `p2` · decision-needed, search

  A SPEC §5 design change. The swap back is one module if KB prefers to wait for the Lance extension to document its FTS indexing.

  - Reconciled 2026-08-10: decided, and the decision was recorded — "KB confirmed: BM25 stays in DuckDB, Lance does vectors only" (dec_01KZN5H4FFR7VHD92Z1PWRTMRA, accepted 2026-08-10). The task row asking for the decision was never closed behind it. <sub>— claude · 2026-08-10</sub>

- **KEEL-49** Decide TQ-9: idempotency_key on all thirteen tables `p1` · decision-needed

  The one storage-format change made without KB. Dropping twelve columns is a forward-only migration if he disagrees.

  - Reconciled 2026-08-10: decided, and the decision was recorded — "KB confirmed: idempotency keys stay on all thirteen tables" (dec, accepted 2026-08-10). The task row asking for the decision was never closed behind it. Keys stay on all thirteen tables; no migration. <sub>— claude · 2026-08-10</sub>

- **KEEL-47** Metric observations charted against target `p3` · desktop, metrics

  The data model is done; this is the rendering.

  - Done. Screen 8, `/projects/:project/metrics`.  The data model has existed since Phase 0 and nothing ever showed it — the same dead weight the event log was before the task page: recorded faithfully, readable by nobody.  Each metric gets its latest reading, its target, and a line chart of every observation with the target drawn as a dashed line.  **Direction is the part worth having built carefully.** A metric whose target is to go *down* — agent orientation cost, error rate — reads exactly backwards if you assume higher is better. `Agent orientation cost` at 2075 against a target of 4000 renders green, and would render red under the naive reading. Every card says "higher is better" or "lower is better" out loud, because a lone number plus a target is still ambiguous without it.  Two smaller things the chart gets right:  - **The y-axis always includes the target**, even when every reading sits far from it. Scaling to the data alone can put the target off-screen, which hides the one comparison the chart exists to make. - **A flat series gets an artificial range**, or the line renders flush against an edge and reads as a boundary rather than a value.  Hand-drawn SVG, no charting dependency — B-14's reasoning, applied again: this needs a polyline, a dashed line and a few labels.  Four tests, and the one that matters asserts a falling metric does *not* render green. The bug it guards against is a dashboard saying a project is on track when it is not, which is worse than no dashboard.  Found while verifying: `Tests passing` still read 270 against a target of 270 because the last observation was from the bootstrap. Recorded a current one — 425 — which is the loop working as intended rather than a fix. <sub>— claude · 2026-08-11</sub>
  - Live-refresh probe: this note exists to make the daemon emit a change event while the app is watching. <sub>— claude · 2026-08-11</sub>

- **KEEL-46** Design artifacts with stored images `p3` · design

  Blocked behind TQ-6: how an image gets into Keel from a Claude chat session, where there is no filesystem.

  - Done. Base64 in the tool call (TQ-6, KB's call), capped at 1 MB decoded, served raw at GET /api/blob/{id}, rendered in the document view.  The storage half was already built — `lancedb.blobs`, `put_blob`/`get_blob`, and `Design.blob_id` have existed since Phase 0. Only the ingestion path was missing, which is why this sat blocked on a question rather than on work.  Verified end to end with a real 240x120 PNG: created through MCP, stored byte-identical, served with the right content type, and rendered in the app at its true dimensions next to its caption.  Six tests cover the paths that matter, and the two most valuable are the refusals: an oversized image is rejected with its actual size and leaves no design behind, and undecodable base64 says so rather than storing rubbish as bytes. <sub>— claude · 2026-08-11</sub>

- **KEEL-44** Screen 9 — Activity feed `p2` · desktop

  Filterable by actor. Writes with no session_id are marked unattributed.

  - Filterable by actor. Writes with no `session_id` are marked `unattributed` — that count is what Phase 2's gate is about <sub>— human · 2026-08-10</sub>

- **KEEL-43** Screen 6 — Search `p2` · desktop

  Faceted by type, scoped by project. Hits say which index found them.

  - Faceted by type, scoped by project. Hits say which index found them, so "is the semantic half earning its keep" stays answerable (R-3) <sub>— human · 2026-08-10</sub>

- **KEEL-42** Screen 5 — Documents with revision diff `p2` · desktop

  The diff is why this screen exists rather than being a markdown viewer.

  - Reader, revision picker, side-by-side diff, and the link graph. The diff is why this screen exists rather than being a markdown viewer <sub>— human · 2026-08-10</sub>

- **KEEL-41** Screen 4 — Board `p2` · desktop

  Tasks by status, filterable, keyboard-driven.

  - **Found and fixed: a six-column grid with per-column min-widths overflows its tracks rather than scrolling**, so each column's cards landed on top of the next column's heading <sub>— human · 2026-08-10</sub>

- **KEEL-40** Screen 3 — Roadmap `p2` · desktop

  Built from milestones. Dated first in date order, undated after.

  - Built from milestones. Dated first in date order, undated after — a milestone with no target is unplanned, not far-future <sub>— human · 2026-08-10</sub>

- **KEEL-39** Screen 2 — Project dashboard `p1` · desktop

  The same data keel_context gives an agent, so a human and a model cannot be looking at different summaries.

  - The same data `keel_context` gives an agent. If a human and a model see different summaries of one project, one is wrong and nobody knows which <sub>— human · 2026-08-10</sub>

- **KEEL-38** Screen 1 — Home, all projects at a glance `p1` · desktop

  At-risk projects sort first. This is the Sunday-review screen.

  - At-risk projects sort first; every number visible without a click. **Found and fixed: the cross-project roll-up never populated `recent`**, so the Sunday review said "no activity yet" against a store with 500 events <sub>— human · 2026-08-10</sub>

- **KEEL-37** Tauri v2 shell with the daemon as a sidecar `p1` · desktop

  Starts the daemon only if one is not already running, and only kills what it started.

  - Starts the daemon only if one is not already running, and only kills what it started — terminating someone else's daemon on window close would take out whatever they had pointed at it <sub>— human · 2026-08-10</sub>

- **KEEL-36** Run the ten unprompted sessions `p0` · plugin, gate

  Run 2026-08-09. Ten unprompted sessions across two scratch projects, headless, real Claude, skill loaded. Result: 1 of 10 wrote — the gate FAILS against 9 of 10. Every write attributed; 0 duplicate projects. Cause is not the skill failing to fire: sessions called keel_context and said they wanted to write, then stopped because no project existed for the directory and SKILL.md says to confirm before creating one. See TQ-17, which records the cold-start deadlock this run exposed and what it costs.

  - `plugin/README.md`. What to measure, how to measure it, and what each failure mode means <sub>— human · 2026-08-10</sub>

- **KEEL-35** MCP config and install script `p2` · plugin

  The installer prints the configuration rather than editing anyone's settings file.

  - `plugin/.mcp.json`, `plugin/install.sh`. The installer prints the configuration rather than editing anyone's settings file <sub>— human · 2026-08-10</sub>

- **KEEL-34** Project-confirmation behaviour `p0` · plugin

  keel_projects returns requires_confirmation on a near miss; the skill makes this the one place the agent must stop and ask.

  - `keel_projects` returns `requires_confirmation` on a near miss; the skill makes this the one place the agent must stop and ask rather than get on with it <sub>— human · 2026-08-10</sub>

- **KEEL-33** PostToolUse hook for mirror edits `p1` · plugin, mirror

  Verified end to end: an edit typed into a generated spec came back as revision 2, attributed to the session.

  - **Verified end to end against a running daemon:** an edit to a generated spec became revision 2, attributed to the session. Refuses aggregate files, unheadered files and non-mirror paths; reports a down daemon without failing the edit <sub>— human · 2026-08-10</sub>

- **KEEL-32** The skill that teaches Claude when to write `p0` · plugin

  Leads with orientation, then session threading, then a when-this-happens-write-that table.

  - `plugin/skills/keel/SKILL.md`. Leads with orientation, then session threading, then a when-this-happens-write-that table. Names the two most-skipped artifacts (questions, decisions) and the shredding failure explicitly <sub>— human · 2026-08-10</sub>

- **KEEL-31** Markdown mirror generator `p1` · plugin, mirror

  Prose only. The module contains no way to read a mirror as truth, and a test asserts that absence.

  - Prose only (TQ-5). Skips unchanged files by comparing content with the timestamped header stripped, so regenerating does not dirty the tree. A test asserts this module contains **no** way to read a mirror as truth — the absence is the enforcement of D-3 <sub>— human · 2026-08-10</sub>

- **KEEL-30** Serve MCP 2025-11-25 as well as 2026-07-28 `p0` · daemon, mcp, interop

  Claude Code 2.1.185 opens with the legacy initialize handshake. A daemon speaking only the current revision reports Failed to connect and is unusable with its primary client.

- **KEEL-29** Scripted UC-1 to UC-4 harness `p1` · testing

  21 tests driving real HTTP against a real daemon. The automated proxy for a gate that needs a human.

  - 21 tests driving real HTTP against a real daemon. UC-1→UC-4 all pass mechanically. **The human half of the gate is still unverified** — see below <sub>— human · 2026-08-10</sub>

- **KEEL-28** keel render-status `p2` · cli

  The dogfooding switch. Not the §8 mirror, which is prose-only.

  - Renders milestones, tasks by status, open questions, decisions, and a changelog derived from the event log. One-directional, like the mirror <sub>— human · 2026-08-10</sub>

- **KEEL-27** Snapshot tests for every tool response `p1` · testing

  They are an API contract, and drift should show up in a diff rather than as an agent behaving differently.

  - 8 `insta` snapshots covering the tool list, discovery, the digest, creates, search, projects and every error shape. Ids and timestamps redacted so the snapshots do not churn <sub>— human · 2026-08-10</sub>

- **KEEL-26** Concurrency gate: zero duplicates, zero lost updates `p0` · testing

  16 concurrent sessions. Exactly one create wins; all 16 updates land under retry; the event log stays gapless.

  - **Phase 1 exit criterion met (mechanical half).** 16 concurrent sessions: exactly one create wins, all 16 updates land under retry, the event log is gapless and strictly ordered, concurrent identical links produce one edge <sub>— human · 2026-08-10</sub>

- **KEEL-25** Local REST and SSE for the desktop app `p1` · daemon

  Keel's own API, dispatching through the same tool layer as MCP so the two cannot drift.

  - REST + SSE under `/api`, dispatching through the same tool layer as MCP so the two cannot drift <sub>— human · 2026-08-10</sub>

- **KEEL-24** Shared single write path `p0` · daemon

  One store, one mutex, whole process. Held across synchronous work, never across an await.

  - One store, one mutex, whole process. Held across synchronous work, never across an await <sub>— human · 2026-08-10</sub>

- **KEEL-23** Write tools: create, update, write_doc, link `p1` · mcp

  409 carries latest_version, current state and events_since, so a losing writer can usually merge.

  - 409 carries `latest_version`, current state and `events_since`. **Found and fixed: `version` was nested inside `audit` on read but asked for at the top level on write** — an agent had to hunt for it (B-13) <sub>— human · 2026-08-10</sub>

- **KEEL-22** Read tools: search, get, activity, projects `p1` · mcp

  keel_get takes version and diff_against, so REQ-2's diff requirement is met at the API layer.

  - Including `version` + `diff_against` on `keel_get` (REQ-2 at the API layer) and fuzzy project matching with `requires_confirmation` (REQ-8) <sub>— human · 2026-08-10</sub>

- **KEEL-21** keel_context — the digest `p0` · mcp

  3–4k tokens. Questions and glossary terms are never trimmed; everything else degrades and reports what it dropped.

  - Budgeted to 3–4k tokens; trims in order of what an agent can most cheaply re-fetch. Questions and terms are **never** trimmed — verified with 60 of each, which returns them in full and sets `budget_exceeded` <sub>— human · 2026-08-10</sub>

- **KEEL-20** The nine tool schemas `p0` · mcp

  Descriptions say when to reach for a tool, not just what it does. A test enforces that.

  - Nine tools. The descriptions say *when to reach for this*, not just what it does — a test enforces that, because a description that reads like a signature produces an agent that calls the wrong tool confidently <sub>— human · 2026-08-10</sub>

- **KEEL-19** server/discover and tools/list `p0` · daemon, mcp

  Both required. Deterministic tool order for prompt-cache hits.

  - `server/discover` is required in this revision and is implemented. `tools/list` carries `ttlMs` and `cacheScope`, and the order is deterministic for prompt-cache hits <sub>— human · 2026-08-10</sub>

- **KEEL-18** JSON-RPC and the stateless Streamable HTTP transport `p0` · daemon, mcp

  Header/body validation with the renumbered error codes. GET/DELETE answered 405.

  - Header/body validation with the renumbered codes (`-32020/21/22`, not the draft `-3200{1,3,4}`). GET/DELETE → 405 so an older client can tell "wrong protocol" from "no endpoint". **Found and fixed: the `Origin` check used `starts_with`, so `https://localhost.evil.example` passed it** <sub>— human · 2026-08-10</sub>

- **KEEL-17** Monotonic ULID generation `p0` · correctness

  Plain ULIDs are not ordered within a millisecond, which would make the event cursor silently skip rows.

- **KEEL-16** Implement idempotency keys and optimistic concurrency `p0` · correctness

  Derived keys normalise whitespace and case. OCC enforced by WHERE version = ?, not a read-then-write.

  - Derived keys normalise whitespace and case (R-6). OCC enforced by `WHERE version = ?`, not a read-then-write. Stale updates return `latest_version` <sub>— human · 2026-08-10</sub>

- **KEEL-15** Test suite: concurrency, idempotency, OCC, round-trip `p1` · testing

  Concurrency test written in Phase 0 as ignored, made real in Phase 1.

  - Round-trip, idempotency, OCC, events, graph direction, backup round-trip. Concurrency test written and `#[ignore]`d for Phase 1 with the reason on the line <sub>— human · 2026-08-10</sub>

- **KEEL-14** 200-entity fixture across all types and relations `p1` · testing

  212 entities, 31 links, 52 revisions across three projects. Loaded through the ordinary write path.

  - **212 entities, 29 links, 52 revisions** across three projects. Loaded through the ordinary write path, so it exercises validation, idempotency and events — not just the schema <sub>— human · 2026-08-10</sub>

- **KEEL-13** keel fsck — cross-engine referential integrity `p1` · correctness

  27 checks. Every finding says what it breaks and what to do about it.

  - 27 checks. Dangling links, stored `depends_on`, doc pointers to nowhere, duplicate idempotency keys, provenance gaps. Every finding says what it breaks and what to do <sub>— human · 2026-08-10</sub>

- **KEEL-12** Backup: DuckDB and Lance to Parquet, restore `p0` · backup, risk

  Both engines. Restore refuses a backup missing its Lance half, and refuses to overwrite an existing store.

  - **Both engines.** DuckDB `EXPORT DATABASE` plus an explicit Lance→Parquet dump. Restore refuses a backup missing its Lance half, and refuses to overwrite an existing store. Verified by row count per table, not by eye <sub>— human · 2026-08-10</sub>

- **KEEL-11** Hybrid search — BM25 plus vectors, RRF fusion `p1` · search

  BM25 lives in DuckDB, not Lance. lance_hybrid_search's keyword half could not be characterised.

  - Lance hybrid + DuckDB BM25, fused by reciprocal rank. Found: the DuckDB FTS index is a snapshot and silently misses rows created after it was built — now rebuilt off the event-log watermark <sub>— human · 2026-08-10</sub>

- **KEEL-10** Embeddings via fastembed `p1` · search

  Behind an Embedder trait, passed in rather than constructed, so tests use a deterministic hash embedder instead of downloading 130 MB.

  - `fastembed` behind an `Embedder` trait, passed in rather than constructed, so tests use a deterministic hash embedder instead of downloading 130 MB. A failed embed warns and keeps the write <sub>— human · 2026-08-10</sub>

- **KEEL-9** Event log — append, query since cursor `p1` · core

  Cursor paging visits every event exactly once. Only holds because ULIDs are minted monotonically.

  - Append-only, cursor and timestamp queries. Cursor paging test asserts every event is visited exactly once — it only holds because ULIDs are monotonic (B-9) <sub>— human · 2026-08-10</sub>

- **KEEL-8** Links, GraphStore trait, recursive CTE traversal `p0` · graph, correctness

  21 tests: all nine relations, both directions, plus both inversions. An inverted traversal returns an empty set that looks legitimate.

  - **21 tests, all 9 relations, both directions, plus both inversions.** `depends_on` normalises to `blocks` with swapped endpoints and never reaches the table. Cycle guard, depth clamp and shortest-path dedup all covered <sub>— human · 2026-08-10</sub>

- **KEEL-7** Document revisions — append, fetch by version, diff `p1` · storage

  Identical content does not grow the history, because the mirror hook regenerates and re-reads constantly.

  - Append, fetch by version, unified diff via `similar`. Identical content does not grow the history — the §8.1 mirror hook regenerates and re-reads constantly <sub>— human · 2026-08-10</sub>

- **KEEL-6** Entity storage layer — CRUD for all 13 types `p0` · storage

  Field patching goes through a serde round-trip, so enum errors already name the valid values.

  - CRUD for all 13 types behind `EntityStore`. Field patching goes through serde round-trip, so enum errors already name the valid values. 8 round-trip integration tests against real storage <sub>— human · 2026-08-10</sub>

- **KEEL-5** Lance documents and blobs datasets, ATTACH wiring `p0` · storage

  Created through the DuckDB extension; no Lance Rust crate needed. Lance CREATE TABLE rejects all column constraints.

  - Lance `documents` + `blobs` created through the DuckDB extension; no `lance` Rust crate needed (B-2). Found: Lance `CREATE TABLE` rejects all column constraints, so nullability is application-level — consistent with §2.1 <sub>— human · 2026-08-10</sub>

- **KEEL-4** DuckDB schema and forward-only migrations `p0` · storage

  Recorded in _keel_migrations. v_entities built now rather than deferred.

  - Forward-only migrations in `_keel_migrations`. `v_entities` built now rather than deferred (resolves TQ-4). PROVISIONAL: `idempotency_key` on all thirteen tables, not just tasks — see TQ-9 <sub>— human · 2026-08-10</sub>

- **KEEL-3** Domain types, ULID prefixes, the audit block `p0` · core

  Thirteen types, prefixed ULIDs, relations, events, document revisions.

  - Thirteen types, prefixed ULIDs, audit block, relations, events, document revisions. 54 unit tests. Found: ULIDs need a monotonic generator or the §3.4 event cursor silently skips rows (B-9) <sub>— human · 2026-08-10</sub>

- **KEEL-2** Verify fast-moving dependencies `p0` · risk

  Verified against running code, not docs. DuckDB 1.5.5 + Lance work end to end; DuckPGQ confirmed absent for 1.5.x (HTTP 404); MCP 2026-07-28 current. Two SPEC §5 syntax errors found and fixed.

  - **Verified against running code, not docs.** DuckDB 1.5.5 + Lance extension work end to end. DuckPGQ confirmed absent for 1.5.x (HTTP 404). MCP 2026-07-28 current. Two SPEC §5 syntax errors found and fixed in place. Nine MCP deltas recorded for Phase 1. Full table in DECISIONS.md <sub>— human · 2026-08-10</sub>

- **KEEL-1** Cargo workspace scaffold, CI, lint/fmt/deny gates `p0` · build

  Five crates from SPEC §1.1. unwrap/expect/panic are workspace clippy lints, so the definition of done is a build failure rather than review discipline.

  - Five crates scaffolded per SPEC §1.1. CI runs fmt, clippy `-D warnings`, tests and `cargo deny`. unwrap/expect/panic are workspace clippy lints, so the definition of done is a build failure rather than review discipline <sub>— human · 2026-08-10</sub>


### wont_do (5)

- **KEEL-138** generate and the pre-commit check both ignore which worktree you are in `p2` · bug, cli, tooling

  Hit while closing KEEL-133 from a worktree. `keel generate keel` reported five files written and `git status` in the worktree was clean — the writes had landed in `/Users/h8hcn/development/keel`, on the `phase-9-one-database` branch, including a 649-line `product/SPEC.md` belonging to a concurrent session's work. They were reverted by hand.
  
  `generate` takes `--repo`, so the write side has a workaround. The check side does not: `scripts/pre-commit` runs `"$keel_bin" generate "$project" --check` with no `--repo`, and there is nowhere to pass one. So a commit made in a worktree is refused or allowed on the strength of a different tree's files.
  
  Two candidate fixes, and they compose: default the repository root to `git rev-parse --show-toplevel` when the command is run inside the project's repository, and have the hook pass the tree it is committing. Note `--show-toplevel` is the worktree root, which is what is wanted here.
  
  Worth pairing with the observation that two sessions rendering one live store onto two branches will conflict in `product/STATUS.md` whatever happens, because each renders the store as it stood when that session ran. Regenerating once after the merge is the answer to that, not a change to `generate`.

- **KEEL-114** 8B — File an issue from the app, with a pasted screenshot `p1` · desktop, daemon, phase8, 8b

  A New issue button, reachable anywhere with C. Title, markdown body, optional labels and priority. Paste or drop images. Paste URLs, which become typed entries in `external_refs` now that it is a list. Creates a task in `triage`.
  
  What it cannot do: change a status, change priority after filing, close anything, edit an existing task, write a spec or decision or question, delete. The app captures; Claude manages.
  
  One mutating route, `POST /api/intake`, multipart, attributed `actor: human`, `surface: ui`. The second non-GET route after `/api/generate`.
  
  This is the path with no size problem. Bytes go straight to the daemon and no model is involved, so a 2 MB retina screenshot is fine here when it would cost 350,000 to 450,000 output tokens through a tool call.
  
  Blocked on TQ-30: this contradicts hard constraint 7 in product/CLAUDE.md, which says the desktop app is read-only, no write endpoints on the daemon for it, no forms in it. All three clauses. That needs KB to amend the constraint before this is built.
  
  Also depends on the triage status existing.
  
  Done when filing a bug with a pasted screenshot, from a cold start, takes under 30 seconds on KB's own stopwatch. That is a Phase 8 exit criterion.

  - Not being built. KB's call, 2026-08-11: "let's skip the ability to create a new issue at this time." TQ-30 carries the reasoning.  Moved out of Phase 8 rather than closed. This is "not now", not "won't do" — the argument in §8B is sound and the constraint was never amended, so nothing has to be reversed if it comes back.  Two consequences worth having on the record:  **Phase 8 cannot claim its 30-second stopwatch criterion.** Filing a bug with a pasted screenshot from a cold start is the thing that criterion measured, and it does not exist.  **Large images still have no route in from a chat surface.** The app path was the only one with no size problem, since bytes go straight to the daemon with no model in between. Claude Code is covered by the file-path attach settled in TQ-33; a session with no filesystem is not. <sub>— claude · 2026-08-11</sub>

- **KEEL-111** 8A — A triage status, ahead of todo `p1` · storage, phase8, 8a

  One new value on the task status enum, before `todo`. Issues filed from the app land there. It means "a human threw this in, unrefined", and `keel ready` skips it. Without it, everything you file competes with planned work on equal footing, which is what stops the file-rough-and-sort-later loop from working.
  
  The spec calls this a seventh value. It is the sixth. enums.rs:136 declares five: todo, in_progress, review, done, wont_do. There is no `blocked` any more, removed in KEEL-82 and settled in TQ-25.
  
  Separately, and worth fixing at the same time: product/CLAUDE.md tells sessions "Never delete a task. Mark it `dropped` with a reason." There is no `dropped` status. A session following that instruction gets an enum rejection. The intended value is `wont_do`. That line is in the contract loaded into every session.
  
  Waiting on TQ-32, which asks whether a holding pen for unrefined work earns a sixth value on the status enum.
  
  Done when a triage task is excluded from `keel ready` and from the digest's open count by construction, the board shows a triage column, the migration is additive with no existing row changed, and the CLAUDE.md line is corrected.

  - Not being built. KB's call, 2026-08-11, following from TQ-30: with app filing declined, nothing files in a hurry, so a holding pen has nothing to hold. TQ-32 carries the reasoning.  Moved out of Phase 8 rather than closed — it comes back with app filing if that does.  **The `dropped` correction does not depend on this and should still happen.** `product/CLAUDE.md` tells every session "Never delete a task. Mark it `dropped` with a reason", and there is no such status. A session following that instruction gets an enum rejection listing five values, none of them the word. The intended value is `wont_do`. That line is in the contract loaded into every session and has been failing silently for as long as it has existed. <sub>— claude · 2026-08-11</sub>

- **KEEL-48** Deployable daemon with auth `p3` · remote

  Blocked: needs hosting and auth decisions from KB.

  - Dropped by KB, 2026-08-11, after asking for it in plain English.  The daemon binds `127.0.0.1` and has no password because it has no door — the network stack refuses everyone else. "Deployable with auth" would mean running it somewhere reachable and adding a token so not just anyone who finds the address can read the store.  What it would buy: Keel from a second machine, from Claude on the web or a phone, or by another person. None of which is a thing KB has asked for.  What it costs: TLS, token rotation, backups that were not needed before, and losing the ability to run `keel fsck` against a local file. Every "one user, a few thousand rows, do not optimise" decision in this codebase — no caches, no pools, no queues — was made for a local tool.  The deciding argument: GitHub was the only component that genuinely needed a public address, and it was dropped in the same conversation. Nothing now wants to be reachable from outside.  The real trigger to revisit is wanting Keel on a second machine. If that happens it comes back with an actual requirement, and it is worth noting that syncing a file is a much smaller problem than hosting a service — `~/.keel` is already a git repository. <sub>— claude · 2026-08-11</sub>

- **KEEL-45** GitHub App and webhook receiver `p2` · github

  Blocked: registering the App needs KB's GitHub account and credentials.

  - Dropped by KB, 2026-08-11.  The integration was specified for a world this project does not live in. There is no git remote — `git remote -v` is empty — so there is no repository for a webhook to fire about, no pull requests to parse `Closes KEEL-42` out of, and no merges to propose a status change from. SPEC §9 describes decorating a PR flow that does not exist.  It was also the only thing in the system that wanted a public address, which is what made KEEL-48 look necessary. Dropping this dropped that too.  What would bring it back: pushing this repository somewhere, or Keel being used on a project that already has a PR flow. At that point it returns as a fresh task with a real requirement behind it, rather than a specified feature waiting for a world to arrive.  Kept: `crates/keel-github` stays as a six-line stub and SPEC §9 stays as written. Neither costs anything, and deleting the spec section would lose the design — the `resolves` link, propose-don't-auto-close, commits on the timeline — which was argued through properly and is right whenever it becomes relevant. <sub>— claude · 2026-08-11</sub>


---

## Open questions and risks

| Question | Kind | Severity |
|---|---|---|
| What should Keel measure, and should it measure anything by itself? | question | low |
| The house-style backstop guards `body`, but the prose has moved to `summary` | question | low |
| Does a browser-served write/intake endpoint require amending hard constraint 7? | question | — |
| Enforce the single-writer rule with an advisory lock file, or rely on the health probe alone? | question | — |
| TQ-37 — Six rows of SPEC §13 argue from DuckDB and Lance. Reword, annotate, or leave? | question | low |
| TQ-36 — The single write path is now a convention. Enforce it, or accept that? | question | medium |
| TQ-3 — Re-embedding strategy when the model changes: background full pass, or lazy on access? | question | — |
| TQ-1 — Are requirement anchors (REQ-4) parsed from markdown by convention, or declared in… | question | — |
| Q-1 — Auto-close tasks on PR merge, or propose? | question | — |
| Q-6 — Should Keel ingest anything automatically, or only explicit writes? | question | — |
| Q-5 — What is the retention policy on the event log? | question | — |

---

## Decisions

- **Leaked test stores get a working sweeper, not a redirected TMPDIR** — `accepted`
- **A phase's state is derived; only shipped, cut and paused are declared** — `accepted`
- **Superseded decisions stay in search results and carry a label saying what replaced them** — `accepted`
- **Documents are embedded as passages, and the passage table is an index rather than a record** — `accepted`
- **The fixture corpus stays compiled in, ungated** — `accepted`
- **The write-path atomicity fix: &Connection primitives, transaction-of-one, one typed composite on Store** — `proposed`
- **Taking the payload out of a tool result is one named function, not two lines** — `accepted`
- **Phase 9 runs before Phase 10, and DuckDB and Lance come out of the tree entirely** — `accepted`
- **A glossary term can declare which type it is a spelling of** — `accepted`
- **Reading an image off the disk is a field, not a fourteenth tool** — `accepted`
- **A claim is optimistic concurrency, not a lock** — `accepted`
- **The close reason is a column, and closing is checked on the transition** — `accepted`
- **The plain-English rule covers every prose field, not just milestone summaries** — `accepted`
- **Every milestone carries a plain-English explainer, required at creation** — `accepted`
- **A missing readable number is read as unassigned, not as an error** — `proposed`
- **An accepted decision can be corrected; the revision chain is the guard** — `proposed`
- **"Waiting on a human decision" is the decision-needed label, not a new task kind** — `proposed`
- **A task marked blocked with no blocks edge is reported as a data problem, not ranked** — `superseded`
- **keel_context answers "what next" with named, ranked tasks** — `proposed`
- **KB confirmed: blocked is derived from the links, not a status** — `accepted`
- **KB confirmed: a task holds a list of external links** — `accepted`
- **Readable identifiers are composed, never stored** — `accepted`
- **The Tauri shell is suspended; the web build is the surface** — `accepted`
- **Graph traversal carries the neighbour's label** — `accepted`
- **The desktop app gets Vitest and Testing Library** — `accepted`
- **The light scheme overrides :root, not a second @theme block** — `accepted`
- **Eight named type sizes in two scales, not eleven anonymous ones** — `accepted`
- **The desktop app routes on the hash, and the router is hand-written** — `accepted`
- **KB confirmed: BM25 stays in DuckDB, Lance does vectors only** — `accepted`, 2026-08-10
- **KB confirmed: idempotency keys stay on all thirteen tables** — `accepted`, 2026-08-10
- **restore now re-establishes the store's git repository** — `accepted`, 2026-08-10
- **The p0 wedge was SIGKILL mid-write, not the FTS index** — `accepted`, 2026-08-10
- **Phase 2 closed on 18 of 20** — `accepted`, 2026-08-10
- **s2, s7 and s9 are genuine misses, not L0** — `accepted`, 2026-08-10
- **Outside panel: the gate measurement was invalid and the file plan is rejected** — `proposed`
- **Build scope is Phases 0–3; git stays local; session_id is skill-minted** — `accepted`, 2026-08-09
- **projects gets a nullable status_path; a path claimed twice is reported, not resolved** — `accepted`, 2026-08-09
- **Generation runs inside the daemon, exposed as POST /api/generate.** — `accepted`, 2026-08-09
- **Keel is the source of truth; every product/*.md is generated.** — `accepted`, 2026-08-09
- **The document reader renders markdown with react-markdown, mapping every element by hand** — `accepted`, 2026-08-09
- **keel import is a bridge, not a migration: re-importable and content-addressed** — `accepted`, 2026-08-09
- **Dev builds use line-tables-only debug info, and the clippy gate drops --all-features** — `accepted`, 2026-08-09
- **Surface carries five values, not four: chat, cowork, code, ui, cli** — `accepted`, 2026-08-09
- **missing_docs is a workspace lint, not just a keel-core convention** — `accepted`, 2026-08-09
- **unwrap/expect/panic/todo/unimplemented are workspace clippy lints, promoted to errors by CI** — `accepted`, 2026-08-09
- **No vector or FTS index on the Lance dataset initially — brute-force scan** — `accepted`, 2026-08-09
- **Event summaries name artifacts, not ids** — `accepted`, 2026-08-09
- **Keel's local REST API has more endpoints than the MCP surface has tools** — `accepted`, 2026-08-09
- **The desktop app hand-writes its components** — `accepted`, 2026-08-09
- **Fixture links are addressed by name, never by position** — `accepted`, 2026-08-09
- **Tool responses lift version to the top of the entity** — `accepted`, 2026-08-09
- **Serve MCP 2025-11-25 alongside 2026-07-28** — `accepted`, 2026-08-09
- **BM25 moves from Lance to DuckDB** — `accepted`, 2026-08-09
- **Every table gets idempotency_key, not just tasks** — `accepted`, 2026-08-09
- **ULIDs are minted from a monotonic generator** — `accepted`, 2026-08-09
- **Bundled DuckDB is a feature, not a requirement** — `accepted`, 2026-08-09
- **All Lance access goes through the DuckDB extension** — `accepted`, 2026-08-09
- **chrono for time, not jiff** — `accepted`, 2026-08-09

---

## Changelog

| Date | Actor | Change |
|---|---|---|
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:b754662","test:cargo test --workspace --no-fail-fast","test:keel fsck","test:keel doctor"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → Three checks now answer whether the passage index still describes the store, using the `body_hash` carried on each passage row. `document_without_passages` is a warning — search is degraded, not wrong. `stale_passage` is an error, because a passage whose revision was superseded or edited is returned as current and the ranking gives no sign. `passages_from_mixed_models` is neither: it is the ordinary state during a model change, worth reporting only because vectors of another width are skipped by search rather than failing it, so those rows stop being findable quietly. `doctor` gained a `passage_index` check that reads all three out of the fsck report rather than querying again — the duplicate-query mistake it made with embedding coverage earlier today. Every check has a corruption test that trips it and asserts the store was quiet beforehand. Against the live store: 40 checks, only the two known warnings, and every passage matches the revision it was built from. |
| 2026-08-13 | claude | status todo → in_progress |
| 2026-08-13 | claude | claimed_by none → ses_79a909d0-5f2b-4de8-bf6e-88c04ad8d976 |
| 2026-08-13 | claude | claimed_at none → 2026-08-13T13:03:32.703472Z |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:9948ddf","test:keel doctor","test:curl 'http://127.0.0.1:7654/api/search?query=why+is+the+first+startup+slow'"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → Semantic search is running on the live store for the first time. 128 documents became 566 passages, 849 KB of vectors, embedded in 29 seconds once the model was cached; `doctor` now says "all 128 current document(s) have a vector". The daemon runs with `--embeddings` so new revisions get passages on the way in, and its log says "semantic search is live" rather than nothing at all. The exit criterion holds against real data: "why is the first startup slow" returns a decision about DuckDB and Lance coming out, sourced `semantic`, sharing no words with the query, and "how do we stop two writers corrupting things" puts TQ-36 on the single write path at rank one. |
| 2026-08-13 | claude | status todo → in_progress |
| 2026-08-13 | claude | claimed_by none → ses_79a909d0-5f2b-4de8-bf6e-88c04ad8d976 |
| 2026-08-13 | claude | claimed_at none → 2026-08-13T12:53:58.749023Z |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:9948ddf","test:cargo test --workspace --no-fail-fast","test:cargo clippy --workspace --all-targets -- -D warnings"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → Documents are split into passages and each one is embedded, so nothing is truncated away any more. Markdown sections first, then a 1,400-character wrap with 15% overlap, with the heading path carried into the embedded text so a passage from deep inside a spec still says what it is a section of. Vectors moved to `document_chunks` and `documents.embedding` stopped being written; two triggers delete passages when a revision is superseded or its entity archived. Search takes the best passage per document, and the excerpt is now cut from the passage that matched rather than from the top of the document. Measured on the live corpus: 128 documents become 566 passages, none over the model's window, for 849 KB — the technical specification goes from one truncated passage covering 2.5% of it to 69 covering all of it. |
| 2026-08-13 | claude | status todo → in_progress |
| 2026-08-13 | claude | claimed_by none → ses_79a909d0-5f2b-4de8-bf6e-88c04ad8d976 |
| 2026-08-13 | claude | claimed_at none → 2026-08-13T12:33:45.674659Z |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:3218c12","test:cargo test --workspace --no-fail-fast","test:cargo clippy --workspace --all-targets -- -D warnings"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → Already fixed when I picked it up — `3218c12` landed after the task was written and updated every fixture that still declared a milestone status. I verified rather than changed anything: 868 tests across 55 suites, clippy and fmt clean, and all three previously-failing groups now pass. The concern about stored rows was unfounded; migration 3 rewrites `planned`, `active` and `blocked` alike, and applying migrations 2 and 3 to a copy of the live store took unparseable statuses from 3 to 0 and archived rows in the keyword index from 13 to 0, leaving fsck with only its two known warnings. |
| 2026-08-13 | claude | status todo → in_progress |
| 2026-08-13 | claude | claimed_by none → ses_79a909d0-5f2b-4de8-bf6e-88c04ad8d976 |
| 2026-08-13 | claude | claimed_at none → 2026-08-13T12:16:58.223391Z |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:3218c12","doc:dec_01KZX9ZJWEGGFSPXK1MH750G94","test:cargo test --workspace"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → `MilestoneStatus` holds only declarations now — open, paused, shipped, cut — and `MilestoneState::derive` works out planned, active, blocked or complete from a task tally and the blocks edges. Migration 3 rewrites the old stored values and backfills `shipped_at` from the last task to close. The digest, the tracker and `/api/entities` all read the derived state, and the first two report every phase in flight rather than the first one found, which is what named a finished phase for a week. `apply_changes` refuses to write a derived value and writes `shipped_at` with `shipped`. Two fsck checks cover what neither the migration nor the guard can reach.

Writing it turned up a bug that had nothing to do with milestones: `ensure_above` primes a process-wide id generator, so the doctor test that deliberately writes a future event poisoned every test that ran after it, and the failure surfaced on an innocent one. Fixed with `id::reset_for_tests` and a lock. Latent until this week's new tests changed the ordering.

The live store still needs `keel migrate` with the daemon stopped — it is on schema 1 and the code ships three. |
| 2026-08-13 | claude | created task “A doctor test fails only in a full suite run and passes on its own” |
| 2026-08-13 | claude | created task “The B-57 commit is red: 28 tests still set a milestone status that is now derived” |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:217e612","commit:1299bdb","test:cargo test -p keel-core --lib store::search","test:cargo test -p keel-core --test documents reembed","test:cargo test -p keel-cli --bin keel doctor::tests"] |
| 2026-08-13 | claude | close_reason none → done |
| 2026-08-13 | claude | close_message none → Archived documents now leave both halves of search, and the bug turned out to be live rather than latent — the keyword half was leaking too, which is the opposite of what this task was written believing. Three holes closed: the five prose tables got the `_fts_archived` trigger they were never given, the `documents` triggers got a guard so a revision on an archived entity cannot resurrect it, and `reembed_missing` skips archived so the backfill cannot undo an archive. Archiving clears the vector rather than adding a `WHERE` clause to `search_semantic`, so there is no new predicate to forget. Migration 2 took the live store's leaked rows from 13 to 0 on a copy, with live rows untouched. |
| 2026-08-13 | claude | status in_progress → done |
| 2026-08-13 | claude | evidence [] → ["commit:e15dcc7","doc:dec_01KZXA7K5NXDGTVTEG9G26JPBB"] |

*Showing the 40 most recent of 1166 changes. Use `keel_activity` for the rest.*


--- crates/keel-core/src/safe_path.rs
+++ delete match arm (Some(name), Some(parent)) in resolve_existing
@@ -198,20 +198,17 @@
         if let Ok(real) = probe.canonicalize() {
             let mut out = real;
             for part in tail.iter().rev() {
                 out.push(part);
             }
             return out;
         }
         match (probe.file_name(), probe.parent()) {
-            (Some(name), Some(parent)) => {
-                tail.push(name);
-                probe = parent;
-            }
+             /* ~ changed by cargo-mutants ~ */
             // Nothing on this path exists at all. Hand back what we were given
             // rather than inventing something: the caller's comparison then
             // fails closed, which is the right direction.
             _ => return path.to_path_buf(),
         }
     }
 }
 

--- crates/keel-core/src/next.rs
+++ delete ! in ready
@@ -420,17 +420,17 @@
             continue;
         };
         if parents.contains(&candidate.id) {
             continue;
         }
         if filter.unclaimed && task.claim_is_live(now) {
             continue;
         }
-        if !filter
+        if  /* ~ changed by cargo-mutants ~ */filter
             .labels
             .iter()
             .all(|want| task.labels.iter().any(|l| l == want))
         {
             continue;
         }
         if filter
             .without_labels

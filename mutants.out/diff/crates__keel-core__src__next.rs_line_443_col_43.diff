--- crates/keel-core/src/next.rs
+++ replace != with == in ready
@@ -435,17 +435,17 @@
         if filter
             .without_labels
             .iter()
             .any(|skip| task.labels.iter().any(|l| l == skip))
         {
             continue;
         }
         if let Some(milestone) = &filter.milestone
-            && task.milestone_id.as_ref() != Some(milestone)
+            && task.milestone_id.as_ref() == /* ~ changed by cargo-mutants ~ */ Some(milestone)
         {
             continue;
         }
         items.push(candidate);
     }
 
     let total = items.len();
     let truncated = match filter.limit {

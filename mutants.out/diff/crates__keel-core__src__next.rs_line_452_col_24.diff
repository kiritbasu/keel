--- crates/keel-core/src/next.rs
+++ replace match guard total > limit with true in ready
@@ -444,17 +444,17 @@
         {
             continue;
         }
         items.push(candidate);
     }
 
     let total = items.len();
     let truncated = match filter.limit {
-        Some(limit) if total > limit => {
+        Some(limit) if true /* ~ changed by cargo-mutants ~ */ => {
             items.truncate(limit);
             true
         }
         _ => false,
     };
 
     Ok(Ready {
         items,
